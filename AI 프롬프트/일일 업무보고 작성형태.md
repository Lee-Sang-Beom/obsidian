
##### 공통
- 일반 메모장에 작성하듯 기재
- 마크다운형태로 작성하지 않음. 

##### 업무내용
- 일일 업무보고 요약이라는 소제목이 위에 있음
- <프로젝트명> 하위에 요약된 내용이 있음
- 예시는 아래와 같음
```null
일일 업무보고 요약

<ICMS>
- 가공영상통계(제작) 통계 작업 마무리
- 가공영상통계(사용량) vue 코드 분석 및 구현 시작
```

##### 업무결과
- [오늘 한 일]이라는 소제목이 위에 있음
- <프로젝트명> 하위에 기재되는 업무 내용이 있음
- [결과/현재상태], [내일 할 일]의 내용은 비워주고 [이슈/블로커]는 없음으로 고정
- 예시는 아래와 같음
```null
[오늘 한 일]

<ICMS>
- 개발된 모든 페이지의 검색필터 동작 방식 확인 및 리팩토링
 > 가공컨텐츠, 가공영상현황 페이지: 해당 페이지에서만 사용하는 별도 검색 필터 컴포넌트를 생성하여 사용하고 있었음
 > 개선: 기존 공통으로사용하는 검색 컴포넌트를 사용하는 방법으로 교체. 
 > 해당 과정에서 기존 컴포넌트가 필요로하는 각 필터의 위치지정, disabled처리가 가능하도록 기존컴포넌트의 기능 확장작업 진행

- 가공 영상 통계(제작) 개발 진행
 > 통신 및 비즈니스 로직 구현을 위한 types, service, 
 > 검색필터 구현
 > 메인 및 인트로 차트 UI 생성 후, 상용 ICMS 페이지와 비교하여 부족한 차트 옵션 추가작업 진행

- 가공 영상 통계(제작) 테스트케이스 구현
 > 페이지 제목 렌더링 확인
 > 검색 필터 렌더링 확인
 > 차트 영역 렌더링 확인
 > 파트 범례 표시여부 확인
 > 차트에 표시되어야 하는 카테고리 라인이 정상적으로 그려지는 지 확인
 > 데이터가 없을때의 메시지 확인

- 가공 영상 통계(사용량) Vue 코드 분석 및 React 프로젝트 하위 요소 개발
 > 통신 및 비즈니스 로직 구현을 위한 types, service, 
 > 검색필터 구현


[결과 / 현재 상태]
- 가공 영상 통계(제작): 현황 상용데이터 기반으로, UI에 표시되는 데이터가 정상적으로 출력되는지 확인
- 가공 영상 통계(사용량) 페이지 개발 필요

[이슈 / 블로커]
- 없음

[내일 할 일 (2026.03.13)]
- 가공 영상 통계(사용량) 개발 완료, 테스트케이스 작성, 상용데이터 기반 동작확인
- 누락된 기능 추가 필요: 가공 영상 현황의 카테고리 검색어 입력기능 추가 필요
- 전체 통계 vue코드 분석 및 개발 시작
```


##### 작성할 내용
- 아래의 내용을 위처럼 작성해주면 됨
- 그대로 복사 및 붙여넣기 할 것이라, 별도 편집기를 open해서 답변해주었으면 함

```null

#### Work (프로젝트: ICMS)
---
- [x] 제작자템플릿 페이지 네트워크 요청 캐싱문제 분석
	- SPA 환경에서 다른 페이지로 왕복 이동 시, 해당 페이지에서만 재 fetch가 일어나지 않는 문제 발생
	- 원인: 전역 QueryClient 설정에 의한 staleTime 오버라이드로 인한 데이터 미 fetch (5분간)
	- 해결: refetchOnWindowFocus 부여 및 staleTime 0 으로 변경 후 반영 완료

- [x] 제작자 템플릿의 stale 관련 문제가 다른 프로젝트 (Vringo CMS) 및 현재 프로젝트에 잔류해있는지 요청부 전체 테스트 진행


#### Work (프로젝트: 회사 홈페이지)
---
- [x] Next.js 환경변수 및 API 통신을 위한 api-client 생성 및 코드작성
	- api-client는 react 프로젝트 파일과 큰 흐름은 비슷하지만, next.js에서 권장하는 방식인 fetch API의 확장 기능을 활용하도록 구성을 가져감
	- src/app/api는 Next.js 내에서 백엔드 흐름까지 관리하는 것이 아니기에 별도 생성을 진행하지 않음

- [x] 한 파일에 몰려있는 Ui 코드 분리
	- Home 컴포넌트를 중심으로 섹션별 8개 컴포넌트로 분리 (공통 목적이 아닌 파일은 features/home/components로 이동)
	- 레이아웃을 담당하는 3개 파일은 src/app/layout.tsx 기본 파일로 이동
	- Next.js에 잘못 적용된 전체적인 스타일 오류 수정 (shadcn 적용하지 않음)
		- 해당 과정에서 UI 1차수정 진행

	- 오버라이드된 기본 폰트 삭제
		- 폰트 변경으로 인해 발생한 레이아웃 깨짐 문제의 UI 일괄 2차수정을 진행함

- [x] 문의하기의 실제동작 수행을 위한 서비스 파일 설계 및 구현 
	- 문의 시, 유효성 검사를 포함하여 전화번호의 하이픈이 자동으로 UI에 보여지도록 UX 및 기능 개선 (공통 로직 utils/formatters로 분리)
	- 문의하기 폼 제출동작이 서버 액션 -> 서비스 파일의 흐름을 타도록 구성

- [x] README.md 작성

```