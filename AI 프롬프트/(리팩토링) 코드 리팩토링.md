
## 🎯 디렉터리 구조 원칙

- [x] **_views/** - 페이지 레벨 뷰
  - 완전한 화면 또는 큰 페이지 단위
  - Section들을 조합하여 완성된 화면 구성
  - 페이지 레벨의 상태 관리 및 데이터 fetching
  - 라우팅과 직접 연결

- [x] **_sections/** - 기능 단위 섹션
  - 여러 컴포넌트를 조합한 중간 크기의 블록
  - 특정 도메인 로직 포함
  - 하나의 기능 단위 담당
  - 페이지 내에서 재사용 가능

- [x] **_components/** - 순수 UI 컴포넌트
  - 재사용 가능한 작은 UI 조각
  - 비즈니스 로직이 거의 없음
  - props를 통해 데이터를 받아 표시
  - **도메인에 독립적** (UI 타입으로만 분류)
  - 다른 페이지에서도 사용 가능
  - 해당 디렉터리 하위에는 도메인 관련 용어가 포함되면 안됨 (ex: `components/approve` 불가)

===

- 위는 디렉터리 구조 원칙이야. 저걸 기반으로 클린 아키텍처처럼 아래에 첨부한 코드를 수정하려 해.
- 모든 기능과 로직은 '반드시' 유지되어야 하며, 코드 수정이 완료되면 이전코드와 이후코드의 변경점을 확인하여 동작기능이 완전히 동일한지 마지막에 반드시 체크해야 해.
- 추가적으로 컨테이너 컴포넌트/표현 컴포넌트로 나누고 접미사로 (-container.tsx / -presenter.tsx) 파일명을 나누어줬으면 좋겠어


```tsx
import { useOpenSettingsModal } from '@/pages/approve/hooks/modal/use-open-settings-modal.ts'  
import {  
  useSettingLineQuery,  
  useSettingPreviewQuery,  
  useSettingsDetailQuery,  
} from '@/pages/approve/hooks/use-settings-action.ts'  
import { LoaderCircle } from 'lucide-react'  
import { SettingsForm } from '@/pages/approve/sections/approve-settings/settings-form.tsx'  
import { useAudioToggle } from '@/pages/approve/hooks/audio/use-audio-action.ts'  
import { useEffect, useRef } from 'react'  
import { toast } from 'sonner'  
import { ResponsiveModal } from '@/components/modals/response-modal.tsx'  
  
export default function SettingsModal() {  
  const { state: id, isOpen, onClose } = useOpenSettingsModal()  
  
  const audioRef = useRef<HTMLAudioElement | null>(null)  
  
  const { state, onCompleted } = useAudioToggle()  
  
  const { data: settingsDetail, isLoading: isSettingLoading, error: settingError } = useSettingsDetailQuery(id || 0)  
  const { data: lineSettings, isLoading: isLineLoading, error: lineError } = useSettingLineQuery(id || 0)  
  const { data: previewSettings, isLoading: isPreviewLoading, error: previewError } = useSettingPreviewQuery(id || 0)  
  
  useEffect(() => {  
    if (state) {  
      if (audioRef && audioRef.current) {  
        const audio = audioRef.current  
        if (audio.paused) {  
          audio.onpause = () => {  
            onCompleted()  
          }  
          audio.play().catch(() => {  
            toast.error('오디오 파일을 불러올수 없습니다.')  
          })  
        } else {  
          onCompleted()  
        }  
      }  
    }  
  }, [state])  
  
  return (  
    <ResponsiveModal title={'검수 팝업'} open={isOpen} onOpenChange={onClose} className={'md:max-w-3xl'}>  
      {isSettingLoading || isLineLoading || isPreviewLoading || !id ? (  
        <div className="flex items-center justify-center py-8">  
          <p className="text-sm text-gray-500">  
            <LoaderCircle className="animate-spin" size={46} strokeWidth={2} />  
          </p>        </div>      ) : !settingsDetail ? (  
        <div className="flex items-center justify-center py-8">  
          <p className="text-sm text-red-500">{settingError?.message ?? '제작 정보를 찾을 수 없습니다.'}</p>  
        </div>      ) : !lineSettings ? (  
        <div className="flex items-center justify-center py-8">  
          <p className="text-sm text-red-500">{lineError?.message ?? '그룹회선 정보를 찾을 수 없습니다.'}</p>  
        </div>      ) : !previewSettings ? (  
        <div className="flex items-center justify-center py-8">  
          <p className="text-sm text-red-500">{previewError?.message ?? '미리듣기 정보를 찾을 수 없습니다.'}</p>  
        </div>      ) : (  
        <>  
          {state ? <audio src={state.src} className={'none'} ref={audioRef} /> : undefined}  
          <SettingsForm  
            settingsDetail={settingsDetail}  
            lineSettings={lineSettings}  
            previewSettings={previewSettings}  
            onSuccess={onClose}  
          />  
        </>      )}  
    </ResponsiveModal>  
  )  
}
```