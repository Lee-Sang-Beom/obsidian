#### Work (프로젝트: TF_AI_harness)
---
- Step2 테스트베드 환경 점검 및 모델 서버 상태 복구
	- Step2 UI에서 임베드 모델 다운 상태 확인 및 원격 embedding/reranker 서버 복구
		- `embed down`이 Gemma LLM `9080` 문제가 아니라 embedding/reranker model-server `9081` health check 실패임을 확인
	
	- 실행 문서 정리
		- `execute.md`에 Step2 테스트베드 실행 가이드 추가/정리
		- 잘못 기재된 `192.168.1.22` 정보를 실제 compose 기준인 `192.168.1.21`로 수정
		- 빠른 실행, `embed down` 복구, 운영 명령, 로컬 model-server, 상태 점검 기준으로 문서 구조 재정리
		- `no configuration file provided: not found` 발생 원인과 해결 명령 명시 

#### Work (프로젝트: ai 검증코어 프론트엔드)
---
- 검증코어 프론트엔드 랜딩/채팅/대시보드 UI 정리 및 테스트베드 연결
	- 랜딩 페이지 브랜드/애니메이션 개선
		- 프로젝트명을 `베리로(Verify + Law)`로 정리하고 법률 AI 검증 서비스 기획 의도 반영
		- 메인 컬러를 보라색 계열로 조정하고 화이트 테마 기준으로 UI 톤 정리
		- 스크롤 반응형 법령 아이콘 애니메이션 적용 및 아이콘 노출/사라짐 타이밍 조정
		- favicon/icon 리소스 정리 및 랜딩 애니메이션 구간의 아이콘 표시 개선
	
	- 인증 및 라우팅 흐름 정리
		- WorkOS AuthKit 기반 로그인/로그아웃 흐름 재구성 (커스텀 리다이엑트 오류)
		- 로그인 이후 기본 이동 경로를 `/dashboard` 중심으로 조정
		- 로그인 상태에서 랜딩 헤더에 AI 채팅 이동 버튼이 유지되도록 수정
		- `/dashboard`, `/chat` 영역은 헤더 대신 사이드바 기반 앱 셸 구조로 정리
	
	- 채팅 페이지 UI 개선
		- 실제 AI 대화처럼 사용자/AI 메시지가 누적되는 채팅 UI 구성
		- 파일 업로드 영역을 `use-dropzone` 기반 공통 필드 컴포넌트로 분리
		- 긴 텍스트/줄바꿈 표시, 첨부파일 목록 확인, 첨부파일 삭제 UX 개선
		- AI 응답 타이핑 애니메이션 및 채팅 empty state 정리
	
	- 대시보드 UI 정리
		- 채팅 리스트 중심의 간결한 대시보드로 재구성
		- 사이드바에 새 채팅 진입 요소와 사용자 영역 UI 추가
		- empty state, chip, input 등 shadcn-ui 기반 구성 요소 적용
	
	- 테스트베드 기능 연결 및 UI 정리
		- `/testbed` 경로에 검증 API 테스트베드 화면 구현
		- 테스트베드 좌측 메뉴 진입점 추가
		- 테스트베드 상단 중복 설명 영역 제거 및 탭 중심 UI로 정리