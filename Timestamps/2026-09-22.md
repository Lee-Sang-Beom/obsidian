```
**<Star AI - 프론트엔드>**

- 영상 생성 계약 문서 확인  
    : 업데이트된 영상 생성 계약 문서(`swagger-api-overview.md`, `video-generation-request-contract.md`)를 기준으로 완료 영상 목록/상세/재생이 `/api/videos` 보호 API와 `CompletedVideoAsset`/`OwnedVideoAsset` 기준으로 정리된 것을 확인  
    : 프론트 라이브러리 목록이 더 이상 request lifecycle 목록을 직접 사용하지 않도록 구현 방향 확정
    
- 완료 영상 목록 service 및 query 전환  
    : `ListCompletedVideosResponse`, `CompletedVideoAsset`, `OwnedVideoAsset` 타입과 `listCompletedVideos` API 호출을 service 계층에 추가  
    : 기존 `useVideoRequestsQuery`를 제거하고 완료 영상 목록 query hook 추가, 서버 페이지네이션 응답 기준으로 table state 구성  
    : 영상 라이브러리 목록이 사용자 소유 완료 영상 API(`/api/videos`)를 기준으로 렌더링되도록 전환
    
- 영상 라이브러리 mapper 정리  
    : `VideoRequest` 기반 변환을 `CompletedVideoAsset` 기반 변환으로 교체  
    : request id와 asset id를 row model에 함께 유지해 기존 상세 route 호환성 유지  
    : `/api/videos` 응답에는 `prompt_text`, `duration_seconds`, `output_preset`이 없어 추정 가능한 값만 표시하도록 처리
    
- 보호 API 인증 요청 공통화  
    : access token 확인 후 `AUTH_UNAUTHORIZED` 발생 시 refresh 후 원 요청을 1회 재시도하는 흐름을 `use-authenticated-request` 공통 hook으로 승격  
    : 계정 관리와 영상 완료 API가 같은 refresh/retry 정책을 공유하도록 구성  
    : 동시에 여러 요청이 401을 받아도 refresh가 중복 호출되지 않도록 single-flight 처리 추가
    
- 완료 영상 재생 보호 경로 전환  
    : 상세 화면의 완성 영상 재생 요청을 `/api/videos/{asset_id}/content` 보호 API로 전환  
    : 영상 생성 플로우는 유지하면서 완성 영상 조회/재생만 소유권 보호 경로 사용
    
- 영상 생성 workflow 인증 요청 공통화  
    : `/videos/create`의 생성 요청, polling, scenario/image/submit/result 조회 API 호출이 공통 authenticated request adapter를 타도록 정리  
    : 최신 계약의 `Authorization: Bearer <access_token>` 요구사항을 영상 생성 workflow 전반에 반영
    
- 인증 흐름 안정성 보강 (A4 proxy)  
    : `/api/a4` proxy가 사용자 Authorization을 우선 보존하고, 없을 때만 `A4_TEST_AUTH_TOKEN` fallback을 쓰도록 수정  
    : 사용자 access token이 test token에 덮이지 않도록 안정화
    
- 완료 영상 상세 route 기준 전환  
    : 상세 route param 의미를 `videoRequestId`에서 완료 영상 `videoAssetId`로 변경  
    : 상세 진입 시 `GET /api/videos/{asset_id}`를 먼저 조회하도록 정리, 라이브러리 상세 링크도 `/videos/{videoAssetId}`로 변경
    
- 상세 재생 흐름 정리  
    : 기존 master-video descriptor 재조회 hook 제거  
    : 완료 영상 descriptor + `/api/videos/{asset_id}/content` 보호 재생 경로로 단순화
    
- 테스트 및 빌드 검증  
    : mapper, service, 인증, protected content endpoint, A4 proxy 관련 테스트 추가/보강  
    : 전체 lint, build, `git diff --check` 통과 확인
```


```
<Star AI - 백엔드>

- CMS 완료 영상 라이브러리 목록 API 추가  
    : 관리자 화면의 영상 라이브러리 리스트에서 전체 완료 영상을 조회할 수 있도록 `GET /api/cms-videos` 추가  
    : 현재 로그인 사용자의 영상만 보는 `/api/videos`와 분리하여, 관리자 전용 전체 조회 API로 구성  
    : 완료된 영상 기준은 기존 `VideoRequest.master_video_asset_id`가 존재하는 요청으로 설정  
    : 새 DB 컬럼이나 영상 생성 도메인 필드는 추가하지 않고 기존 request/asset 데이터만 활용

- 완료 영상 생성자 정보 포함  
    : 완료 영상과 계정 연결 테이블을 조회해 각 영상의 생성자 정보를 함께 반환하도록 처리  
    : 유저 연결 정보가 없는 과거/예외 케이스는 `creator: null`로 응답하도록 구성  
    : 삭제된 유저도 식별 가능하도록 기존 유저 row 기준의 `deleted_at` 정보를 포함  
    : 관리자 권한이 없는 사용자는 접근할 수 없도록 admin 권한 검증 적용

- CMS 완료 영상 상세 API 추가  
    : 리스트 페이지에서 영상 클릭 시 상세 화면을 구성할 수 있도록 `GET /api/cms-videos/{asset_id}` 추가  
    : 상세 조회 PK는 완성 영상의 고유 식별자인 `video_asset_id(vas_...)` 기준으로 설정  
    : 리스트와 동일한 `CmsCompletedVideoAsset` 응답 구조를 사용해 프론트 연동 부담을 줄임  
    : 존재하지 않는 영상은 `404 VIDEO_NOT_FOUND`로 응답하도록 처리

- 프론트 계약 문서 및 테스트 갱신  
    : `swagger-api-overview.md`, `video-generation-request-contract.md`에 CMS 완료 영상 목록/상세 API 계약 추가  
    : 화면용 새 필드(`title`, `publish_channel`, `aspect_ratio`)는 추가하지 않고 기존 `video_request_input`을 그대로 노출하도록 정리  
    : 관리자 권한, 전체 목록, 상세 조회, 생성자 null 케이스를 테스트로 검증  
    : 전체 backend 테스트 통과 확인 후 Docker Compose를 최신 코드로 재빌드 및 재기동 완료

```