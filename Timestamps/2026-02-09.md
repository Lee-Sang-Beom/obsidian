#### Work (프로젝트: AI Assistance)
---
 - [x] 클린 아키텍처 관점에서 `src/lib`하위의 기존 파일 및 Sentry 관련 코드 분리
	- 집중 원칙: **외부 라이브러리 의존성 분리**와 **관심사의 분리**
	- 기존 파일
		- `src/lib/api-client.ts` -> `src/lib/http/api-client.ts`
		- `src/lib/common-type.ts` -> `src/lib/types/common-types.ts`
		- `src/lib/window.d.ts` -> `src/lib/types/window.d.ts`
		- `src/lib/form-schema.ts` -> `src/lib/validators/zod-schemas.ts`
	
	- sentry 작업 내용:
	    - Sentry 관련 코드를 `src/lib/sentry`로 완전 분리하여 3개 파일로 리팩토링 진행 (config, capture, handlers)
	    - `src/utils/error-utils.ts`는 Sentry 의존성 제거, 순수 유틸리티만 유지 (normalizeError, isChunkLoadError)
	    - `main.tsx` 간소화


#### Work (프로젝트: 보일러 플레이트)
---
 - [x] 클린 아키텍처 관점에서 `src/lib`하위의 기존 파일 및 Sentry 관련 코드 분리
	- 집중 원칙: **외부 라이브러리 의존성 분리**와 **관심사의 분리**
	- 기존 파일
		- `src/lib/api-client.ts` -> `src/lib/http/api-client.ts`
		- `src/lib/common-type.ts` -> `src/lib/types/common-types.ts`
		- `src/lib/window.d.ts` -> `src/lib/types/window.d.ts`
		- `src/lib/form-schema.ts` -> `src/lib/validators/zod-schemas.ts`
	
	- sentry 작업 내용:
	    - Sentry 관련 코드를 `src/lib/sentry`로 완전 분리하여 3개 파일로 리팩토링 진행 (config, capture, handlers)
	    - `src/utils/error-utils.ts`는 Sentry 의존성 제거, 순수 유틸리티만 유지 (normalizeError, isChunkLoadError)
	    - `main.tsx` 간소화

#### Work (프로젝트: Vringo ICMS -  통계)
---
 - [x] ICMS 관리자(통계) 프로젝트 clone
	 - 안내되지 않은 환경변수 탐색 필요
