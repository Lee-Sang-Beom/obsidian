
#### Work (프로젝트: AI Assistance)
---
- [x] layout/sidebar 하위의 앱 사이드바 코드 개선 
	- **DropdownMenuTrigger와 UserTriggerButton 이벤트 리스너 충돌문제**
		- DropdownMenuTrigger 이벤트와 커스텀 컴포넌트 UserTriggerButton에 전달하는 isOpen 이벤트의 충돌이 발생하여, 원하는 isOpen에 대한 동작이 이루어지지 않는 문제 발생
		- DropdownMenuTrigger asChild option 조정 및 framer-motion의 motion.button에 ref를 전달할 수 있도록 구성

- [x] `components/layout` 내 너무많이 구성된 컴포넌트 디렉터리의 구조경로 및 코드분리를 통한 개선
	- `/layout/menu-item`: 메뉴 데이터를 표현하는 것과 관련된 컴포넌트 묶음 
	- `/layout/navigation`: CMS 페이지 상단 영역의 네비게이션 영역을 구성하는 것과 관련된 컴포넌트 묶음
	- `/layout/sidebar`: CMS 페이지 좌측 영역의 사이드 바 영역을 구성하는 것과 관련된 컴포넌트 묶음
	- `/layout/user-menu`: 유저와 관련된 정보, 유저 정보와 관련된 메뉴를 별도로 구성하기 위한 것과 관련된 컴포넌트 묶음
	- `/layout/background`: 컨텐츠 영역의 배경 관리용 컴포넌트 묶음 (향후 여러 파일 추가 가능)
	- `/layout/container`: `<Outlet/>`을 children으로 받으면서 outlet 영역 전체에 기능을 적용하기 위한 컴포넌트 묶음
	- 기타 메인 레이아웃 파일들은 layout 직속으로 배치 (`app-layout`, `contents-top`, `header`)

- [x] animated-background 통합
	- 해당 컴포넌트는 **순수 Presenter 컴포넌트**로 봐야하며, Container/Presenter 패턴으로 분리할 필요가 없다고 판단.
		1. **상태 로직이 없음** - 모든 것이 선언적 애니메이션 설정
		2. **데이터 fetching 없음** - 하드코딩된 아이콘/텍스트 배열만 사용
		3. **비즈니스 로직 없음** - 단순 시각적 효과만 담당
		4. **props도 받지 않음** - 완전히 독립적인 배경 효과
		5. **응집도 높음** - 모든 코드가 서로 밀접하게 연관됨 (아이콘, 텍스트, 원형, 선 모두 하나의 배경 효과)
	
	- 복잡하게 얽혀있던 코드를 아래방식으로 개선
		- **불필요한 import문 제거**
		- **상수 영역 분리**
		    - `AI_ICONS`, `AI_TEXTS`, `AI_KEYWORDS` - 데이터
		    - `COLORS` - 색상 팔레트
		    - `COUNTS` - 요소 개수
		- **유틸리티 함수 분리**
		    - `random()` - 범위 내 랜덤값
		    - `randomPos()`, `randomPosVh()` - 위치 생성
		    - `getTextColor()` - 색상 선택 로직
		- **개별 컴포넌트로 분리**
		    - `FloatingIcon` - 아이콘 애니메이션
		    - `FloatingText` - 텍스트 애니메이션
		    - `BackgroundCircle` - 배경 원형
		    - `FloatingKeyword` - 키워드 애니메이션
		    - `NetworkLine` - 연결선 효과
		- **메인 컴포넌트의 return는 깔끔하게**
		    - 단순히 각 요소들을 배치만 함
		    - 주석으로 섹션 구분
		    - 가독성 극대화


#### Work (프로젝트: 보일러 플레이트)
---
-  files-detail-modal.tsx 개선
	- **common-utils.ts**
	    - `handleFileDownload(fileName: string, filePath: string)` 함수 추가: 파일 다운로드 공통 로직
	    - `getFileIconColor(fileName: string)` 함수 추가: 파일 확장자별 아이콘 색상 반환
	    - `isImageFile(fileName: string)` 함수 추가: 이미지 파일 여부 확인
	    - `getResourceUrl(filePath: string)` 함수 추가: 리소스 URL 생성
	- **files-detail-modal.tsx** (Container)
	    - FileResponse 객체 전체가 아닌 필요한 `fileName`, `filePath`만 handleFileDownload에 전달하도록 수정
	    - 불필요한 래퍼 함수(`handleDownloadFile`) 제거, `handleFileDownload` 직접 전달
	    - `handlePreviewImage` 인라인 함수로 간소화
	    - Container/Presentation 패턴 적용으로 상태 관리 및 비즈니스 로직만 담당
	- **files-detail-modal.presentation.tsx** (Presentation)
	    - 순수 UI 렌더링만 담당
	    - Props로 받은 유틸 함수(`getFileIconColor`, `isImageFile`, `getResourceUrl`) 및 핸들러 사용
	    - `FileResponse` 타입 import하여 타입 안정성 확보
	- **image-preview-modal.tsx** (Container)
	    - 별도로 구현된 파일 다운로드 로직(약 20줄) 제거
	    - 공통 `handleFileDownload` 함수 활용으로 대체
	    - Container/Presentation 패턴 적용
	- **image-preview-modal.presentation.tsx** (Presentation)
	    - 순수 UI 렌더링만 담당
	    - `onDownload` 핸들러를 Props로 받아 사용

- [x]  **animated-background.tsx 통합 및 개선**
    - 해당 컴포넌트는 **순수 Presenter 컴포넌트**로 판단, Container/Presenter 패턴 분리 불필요
        1. **상태 로직이 없음** - 모든 것이 선언적 애니메이션 설정
        2. **데이터 fetching 없음** - 하드코딩된 아이콘/텍스트 배열만 사용
        3. **비즈니스 로직 없음** - 단순 시각적 효과만 담당
        4. **props도 받지 않음** - 완전히 독립적인 배경 효과
        5. **응집도 높음** - 모든 코드가 서로 밀접하게 연관됨 (아이콘, 텍스트, 원형, 선 모두 하나의 배경 효과)

	- 복잡하게 얽혀있던 코드를 아래 방식으로 개선
	    - **불필요한 import문 제거**
	    - **상수 영역 분리**
	        - `AI_ICONS`, `AI_TEXTS`, `AI_KEYWORDS` - 데이터
	        - `COLORS` - 색상 팔레트
	        - `COUNTS` - 요소 개수
	    - **유틸리티 함수 분리**
	        - `random()` - 범위 내 랜덤값
	        - `randomPos()`, `randomPosVh()` - 위치 생성
	        - `getTextColor()` - 색상 선택 로직
	    - **개별 컴포넌트로 분리**
	        - `FloatingIcon` - 아이콘 애니메이션
	        - `FloatingText` - 텍스트 애니메이션
	        - `BackgroundCircle` - 배경 원형
	        - `FloatingKeyword` - 키워드 애니메이션
	        - `NetworkLine` - 연결선 효과
	    - **메인 컴포넌트의 return은 깔끔하게**
	        - 단순히 각 요소들을 배치만 함
	        - 주석으로 섹션 구분
	        - 가독성 극대화

	- [x]  **animated-background.tsx 통합 및 개선** (다른 프로젝트)
	    - 동일한 패턴으로 리팩토링 완료
	    - **상수 영역 분리**
	        - `ECONOMIC_ICONS`, `ECONOMIC_TEXTS`, `ECONOMIC_NUMBERS` - 데이터
	        - `COLORS` - 경제 테마 색상 (blue, emerald, amber)
	        - `COUNTS` - 요소 개수
	    - **유틸리티 함수 분리**
	        - `random()`, `randomPos()`, `randomPosVh()` - 위치/값 생성
	        - `getTextColor()` - 텍스트 색상 선택
	        - `getNumberColor()` - 숫자 색상 (양수/음수/중립 자동 구분)
	    - **개별 컴포넌트로 분리**
	        - `FloatingIcon` - 경제 아이콘 애니메이션
	        - `FloatingText` - 통화 기호 애니메이션
	        - `BackgroundCircle` - 배경 원형
	        - `FloatingNumber` - 주가/환율 숫자 표시
	    - **메인 컴포넌트 간소화**
	        - 각 섹션별 주석 구분
	        - 깔끔한 배치 로직