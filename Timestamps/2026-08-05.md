#### Work (프로젝트: AI 검증코어 harness)
---
- [x] 분리된 Testbed가 Core와 HTTP 계약만으로 연결되는지 확인하는 분리 검증 코드 분석 및 동작 확인
```
Core 직접 import 금지
HTTP-only Core client 검증
/openapi.json 계약 검증
/internal/v1/executions 응답 검증
401 core_unauthorized 검증
dependency-down 503/reject 검증
malformed response fail-closed 검증
```


#### Work (프로젝트: AI 검증코어 프론트엔드)
---
- [x] 영상제작 서비스 방향성 정리
  - 기존 AI 검증 테스트베드 중심 화면에서 실제 사용자용 영상제작 서비스 방향으로 전환 검토
  - 프로젝트 성격을 영상 제작 보조 서비스로 재정의
  - 추천 프로젝트명 및 브랜드명을 `ReelCraft AI`로 정리
  - 랜딩페이지 문구, CTA, 서비스 설명, 기능 설명을 영상제작 도메인에 맞게 변경
  - 기존 검증 콘솔은 사용자용 메인 기능이 아닌 내부 테스트베드 성격으로 분리

- [x] 랜딩페이지 디자인 및 콘텐츠 변경
  - 랜딩페이지 전체 주제를 영상제작 서비스에 맞게 수정
  - 주요 콘텐츠 변경
    - 히어로 섹션
    - 제작 방식 설명
    - 기능 섹션
    - 신뢰/기준 섹션
    - 하단 CTA
    - 제품 미리보기 영역

  - 기존 법률/검증 중심 표현을 영상 기획, 장면 구성, 편집 방향, 제작 브리프 중심 표현으로 변경
  - 히어로 영역에 영상 제작 분위기를 전달하는 배경 이미지 적용
  - 제품 프리뷰를 영상 플레이어, 씬 보드, 타임라인 느낌의 UI로 재구성

- [x] `/chat` 경로를 `/testbed`로 전환
  - 기존 `/chat` 경로는 향후 실제 AI 영상제작 대화 화면으로 사용할 예정이므로 비워두기 위해 이름 변경
  - 기존 검증 콘솔 경로를 `/testbed`로 변경
  - 변경 대상
    - `src/app/chat` 제거
    - `src/app/testbed` 추가
    - `src/features/chat`을 `src/features/testbed`로 변경

  - 내부 파일명 및 컴포넌트명도 `chat`에서 `testbed` 기준으로 정리
  - 주요 변경 파일
    - `testbed-console.tsx`
    - `testbed-console-workspace.tsx`
    - `testbed.config.ts`
    - `testbed.rules.ts`
    - `testbed.types.ts`
    - `testbed-run-button.tsx`
    - `testbed-form-fields.tsx`
    - `testbed-async-result-section.tsx`
    - `testbed-payload.mapper.ts`

  - 기존 `chat` 명칭이 남아 향후 실제 `/chat` 기능과 충돌하지 않도록 정리

- [x] 인증 후 이동 경로 변경
  - WorkOS AuthKit 로그인/회원가입/콜백 이후 이동 경로를 기존 `/chat`에서 `/testbed`로 변경
  - 변경 대상
    - `src/app/sign-in/[[...sign-in]]/route.ts`
    - `src/app/sign-up/[[...sign-up]]/route.ts`
    - `src/app/auth/callback/route.ts`

  - 로그인 후 내부 검증 콘솔로 정상 진입하도록 `returnTo`, `returnPathname` 경로 조정

- [x] 랜딩 CTA 동작 개선
  - 랜딩페이지 하단 `영상 프로젝트 시작하기` 버튼 클릭 시 페이지 전환이 느리게 느껴지는 원인 확인
  - 기존 하단 CTA가 회원가입 흐름으로 이동하면서 WorkOS Hosted UI를 거치기 때문에 헤더 버튼보다 느려 보이는 구조 확인
  - 로그인된 사용자는 바로 `/testbed`로 이동하도록 변경
  - 로그인되지 않은 사용자는 회원가입이 아닌 로그인 페이지 `/sign-in`으로 이동하도록 변경
  - 사용자 상태 확인 중에는 CTA에 비활성 스타일 적용

- [x] 배포 환경 WorkOS Redirect URI 문제 확인
  - 배포 환경 `192.168.1.22:3001`에서 로그인 후 `0.0.0.0:3000/testbed`로 이동하는 문제 확인
  - Docker 컨테이너 내부 실행값
    - `HOSTNAME=0.0.0.0`
    - `PORT=3000`
    - 외부 노출 포트 `3001`

  - WorkOS Redirect URI와 `.env.prod`의 `NEXT_PUBLIC_WORKOS_REDIRECT_URI` 값이 외부 접근 주소 기준이어야 함을 확인
  - 최종 확인 값
    - `http://192.168.1.22:3001/auth/callback`

  - 설정 반영 및 컨테이너 재빌드/재시작 이후 정상 동작 확인

- [x] 향후 실제 `/chat` 화면 설계 방향 정리
  - 기존 `/chat`은 테스트베드가 아닌 실제 AI 영상제작 대화 화면으로 새로 만들기로 결정
  - 사용자용 작업물 관리 화면은 `dashboard`보다 `studio` 명칭이 더 적합하다고 판단
  - 추천 라우팅 구조 정리
    - `/`: 랜딩페이지
    - `/studio`: 내 스튜디오, 제작물 목록
    - `/chat`: 새 영상 제작 대화 시작
    - `/chat?projectId=xxx`: 기존 영상 프로젝트 이어서 제작
    - `/testbed`: 내부 검증 테스트베드

  - `/studio`는 만든 영상과 제작 중인 영상이 보이는 공간으로 설계
  - `/chat`은 영화, 무비, 광고/홍보 카테고리 선택과 자유 입력을 함께 지원하는 AI 대화 화면으로 설계
  - 향후 영상 리스트, 프로젝트 상세, 쿼리스트링 기반 프로젝트 구분까지 고려하여 확장 방향 정리

