#### Work (AI Assistance)
---
- [x] 검수 팝업 (주요기능)
	- [x] 복잡한 settings-form-presenter 코드분리
	- [x] '검수 정보'로 타이틀 변경
	- [x] 다이얼로그 높이수치 개선
		- 검수정보 다이얼로그 높이 90vh로 변경 (10vh 증가)
		- 내부 카드 콘텐츠영역 30rem로 변경 (6.5rem 증가)
	- [x] 맨 위 전화번호가 뜨는 영역에 `회선정보(현재선택된 탭) (회선개수)` 타이틀 라벨 추가, height 스크롤 고정
	- [x] 상담정보 영역, 설정 정보 영역 각각을 동시에볼 수 있도록 스크롤 영역 분리
	- [x] 설정 정보 내 '기존설정' -> '현재 설정 정보(온에어 제작 전, 제작 후)' 타이틀 변경
	- [x] 설정 정보 내 '추가설정' -> 'AI 분석 설정 정보'
	- [x] 설정 정보 내 AI 분석 회선 정보 추가 -> 'AI 분석 회선' 타이틀 하위에 전화번호 리스트들이 올 수 있도록 UI 개선

- [x] 검수 팝업 (상세내용)
	- [x] settings-form-presenter.tsx 수정
		- [x] 전체 스크롤 영역 구조 변경
			- 기존: 전체 영역을 감싸는 단일 ScrollArea (h-[30rem])
			- 변경: 각 영역별 독립 ScrollArea로 분리
			- 배경 그라데이션을 위한 div 래퍼는 스크롤 없이 유지
		- [x] ConsultInfoPresenter 영역
			- ScrollArea 제거 (고정 높이로 변경)
			- 상담 정보는 일반적으로 고정된 크기이므로 스크롤 불필요
		- [x] RingSettingsPresenter 영역
			- 독립 ScrollArea 적용 (h-[15rem], type='always')
			- 세팅 정보만 독립적으로 스크롤 가능
		- [x] 전체 높이 유지
			- ConsultInfoPresenter (고정) + RingSettingsPresenter (15rem) = 약 30rem 유지
			- 두 영역을 동시에 볼 수 있도록 구성
	- [x] product-type-tabs-list-presenter.tsx 수정
		- [x] 안전한 데이터 접근 추가
			- lineSettings.lineList.lines[key] 접근 시 optional chaining 적용
			- const lineData = lineSettings.lineList.lines[key]
			- const numbersLength = lineData?.numbers?.length ?? 0
		- [x] disabled 조건 수정
			- 기존: lineSettings.lineList.lines[key].numbers.length == 0
			- 변경: numbersLength === 0
			- undefined 또는 null 체크 포함
		- [x] 에러 방지
			- numbers 배열이 undefined일 때 크래시 방지
			- lineData가 undefined일 때 크래시 방지
	- [x] product-type-tabs-content-presenter.tsx 수정
		- [x] 현재 선택된 productType 데이터 안전 접근
			- const currentLineData = lineSettings.lineList.lines[productType]
			- const numbersLength = currentLineData?.numbers?.length ?? 0
		- [x] 각 탭의 numbers 배열 안전 접근
			- const lineData = lineSettings.lineList.lines[key]
			- const numbers = lineData?.numbers ?? []
		- [x] 회선 정보 텍스트 표시
			- numbersLength 사용하여 안전하게 표시
			- undefined 케이스에서도 0으로 표시
		- [x] map 함수 에러 방지
			- numbers가 undefined일 경우 빈 배열로 대체
			- map 실행 전 항상 배열임을 보장
		- [x] 기존 동작 유지
			- numbers가 빈 배열이어도 빈 그리드 표시 (에러 메시지 없음)
			- 원래 로직과 동일하게 동작
	- [x] ring-settings-presenter.tsx 수정
		- [x] lineData 안전 접근
			- const lineData = lineSettings.lineList.lines[key]
			- const numbers = lineData?.numbers ?? []
		- [x] validPhones 필터링
			- numbers 배열을 안전하게 필터링
			- settingMap에 존재하는 전화번호만 추출
		- [x] firstValidPhone 체크
			- validPhones[0] 접근 시 undefined 가능성 고려
			- 기존 조건문 유지: firstValidPhone && settingMap[firstValidPhone.phone]
		- [x] phoneList 생성
			- validPhones.map()으로 전화번호 포맷팅
			- 쉼표로 구분하여 문자열 생성
		- [x] 기존 동작 유지
			- validPhones가 비어있으면 undefined 반환 (아무것도 렌더링 안함)
			- 원래 로직과 동일하게 동작
		- [x] TabsContent 조건부 렌더링
			- firstValidPhone이 존재하고 settingMap에 해당 데이터가 있을 때만 Tabs 렌더링
			- 조건 불충족 시 undefined 반환
	