
####  Work (프로젝트: 공통 AI 검증코어)
---
- 주간회의 참여
- TFT 코드작성을위한 git 환경세팅

- TF_AI_harness_v0.7 내 설치한 embedding model과 reranker model을 `192.168.1.22` 서버 환경에 업로드
	- 원격 모델 서버 FastAPI 추가
	- 원격/로컬 자동 선택 builder 추가
		- endpoint가 있으면 -> Remote adapter 사용
		- endpoint가 없으면 -> 기존 Local BGE adapter 사용
		- settings.py에 원격모델관련 설정환경변수 추가 (이 값들이 없으면 기존 models/embedding, models/reranker 로컬 경로를 계속 사용)
		- Step1 파이프라인이 builder를 사용하도록 변경
		- 원격 model-server 추가
			- 이 서버는 원격 호스트에서 모델 파일을 로드하고 HTTP API를 제공
			- GET /health, POST /v1/embeddings, /v1/rerank
			- 즉 Harness 컨테이너가 무거운 BGE 모델을 직접 로드하지 않고, 원격 서버에 요청하도록 만든 구조
		- compose에 원격 endpoint 설정 추가 (런타임 연결 설정)
			- 컨테이너 안에서 실행되는 서비스들이 원격 embedding/reranker 서버 주소를 알 수 있게 하기 위함이 목적
			- step2-testbed, step1-viz, step1-e2e-smoke는 Docker 컨테이너 안에서 실행됩니다. 컨테이너 환경변수에 endpoint를 넣지 않으면, 컨테이너 안의 Step1 파이프라인은 원격 서버가 있는지 모르고 기존처럼 로컬 모델 경로를 보려고 하기 때문
		- 관련 스크립트도 원격 설정을 타도록 변경 (로컬 모델을 참조하려 시도한부분)