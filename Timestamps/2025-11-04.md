
#### Work
---
### 프로젝트: 82ALDA Podcast Service

##### 페이지 : AI 시그널
- [x] 실시간 데이터 조회 로직 변경
	- 전체 시장현황, 종목별 실시간가격 (차트데이터는 제외))의 refetchInterval을 10,000ms(10초)로 변경
	- 데이터 변경에 따른 차트 닫힘 현상 해소를 위한 작업 진행 필요

- [x] 신규기능 '오늘의 시그널' 개발
	- UI 로직 추가 수정
		- 아코디언 형태 개발 (기본 닫힘형태)
		- code, codeName, 실시간 가격 및 등락율, 발굴 후 상승률, 체결강도(new), 즐겨찾기 버튼 표시
		- 시그널 체결강도(`<TopGainersProgressbar />`)는 연속된 평행사변형이 배치된 Progress bar 형태로 구성 (0~100%) 
	
	- 서비스 로직 추가
		- pull-to-refresh 기능 반영
		- 즐겨찾기 이벤트 반영
		- 오늘 시그널, 어제 시그널 조회 (리팩토링 2회를 통해, activetab에 대한 코드 간소화 및 fetch 횟수 감소)
		- 종목 정보는 기존 signal-card 등에서 사용하는 `<StockInfoHeader/>`를 호출할 수 있도록 구현

- [x] 1차개발된 '오늘의 시그널' 리팩토링
	- 중복 API 호출 제거: 기존 2개의 `useGetTopSignals` 훅을 1개로 통합하여 선택된 탭의 데이터만 조회하도록 개선
	- 동적 날짜 설정: `selectedDate` 변수를 통해 활성 탭(`today`/`yesterday`)에 따라 요청 날짜를 동적으로 결정
	- 조건부 쿼리 활성화: `enabled: isExpanded` 옵션 추가로 섹션이 펼쳐졌을 때만 API 호출되도록 최적화
	- 불필요한 상태 변수 제거: `todayData`, `yesterdayData`, `todayLoading` 등 중복된 상태 변수들을 `data`, `isLoading` 등 단일 변수로 단순화
	- 성능 개선: 탭 전환 시 필요한 데이터만 fetch하고, 섹션이 접혀있을 때는 데이터를 가져오지 않아 불필요한 네트워크 요청 감소
	- 기존 UI 수정: 다크/라이트테마 별 css 수정, rank badge 추가
	- 예약어로 인한 경고문 제거
	- 날짜 자동 갱신 기능 추가: `useMemo` 의존성 배열 제거로 매 렌더링마다 최신 날짜로 계산되도록 개선, 앱을 켜놓은 상태에서 자정이 지나도 정확한 날짜 표시
	- Pull to Refresh 시 날짜 업데이트: 새로고침 시 현재 날짜를 재계산하여 항상 최신 날짜의 데이터를 가져오도록 개선
	- today, yesterday 기준을 06시 기준으로 집계 / description 추가
		- ex) 2025.11.04 14:00에 조회 -> (오늘 2025.11.04, 어제 2025.11.05)
		- ex) 2025.11.05 00:00에 조회 -> (오늘 2025.11.04, 어제 2025.11.05)
		- ex) 2025.11.05 05:59에 조회 -> (오늘 2025.11.04, 어제 2025.11.05)
		- ex) 2025.11.05 06:00에 조회 -> (오늘 2025.11.05, 어제 2025.11.04)

- [x] 차트 관련 컴포넌트 리팩토링
	- `any[]`로 지정된 interface property 요소를 `|` 연산자를 이용한 방식으로 수정 및 수정으로 발생한 연계 타입에러 전면제거
	- 필요없는 코드그룹 제거
