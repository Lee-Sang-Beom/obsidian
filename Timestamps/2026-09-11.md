```
일일 업무보고 요약

<제주 렌터카>
- CMS 레이아웃 스크롤 구조 개선: 본문이 길어져도 sidebar가 늘어나지 않도록 구조 분리
- 계정 관리, 영상 생성 입력, 공통 테이블 코드 역할 분리 및 테스트 추가
- 미구현 화면에 공통 준비중 화면(placeholder) 적용
- 영상 라이브러리(/videos/library) 화면 제작
- 영상 생성 API 연동 기반 작업(proxy, service, 요청 흐름 연결) 진행


[오늘 한 일]

<제주 렌터카>
- CMS 레이아웃 스크롤 구조 개선
 > /videos/create처럼 본문이 긴 페이지에서 sidebar가 함께 늘어나는 문제를 확인하고, 전체 레이아웃은 화면 높이에 고정하되 본문 영역에만 스크롤이 생기도록 구조 분리
 > sidebar는 화면 높이에 고정하고 메뉴 목록만 내부 스크롤되도록 처리하여, 상단 로고와 하단 로그아웃 영역이 항상 유지되게 개선
 > 앞으로 CMS 화면은 브라우저 전체 스크롤이 아니라 본문 영역 내부 스크롤 구조를 기본으로 사용하기로 결정

- 계정 관리, 영상 생성 입력, 공통 테이블 코드 정리
 > 화면 UI, 더미데이터, 계산 로직이 섞여 있던 부분을 역할별 파일로 분리
 > 계정 관리의 목록 반영/페이지네이션 계산, 영상 생성 입력의 이미지 첨부 상태 타입, 공통 테이블의 행 선택 로직을 정리하고 테스트 추가
 > 공통 테이블은 페이지네이션/행 선택/행 동작을 포함하는 성격에 맞춰 DataTable로 명명하고, 향후 기능 확장 범위를 관리하기로 결정

- 미구현 화면 공통 placeholder 적용
 > 아직 실제 기능을 만들지 않는 메뉴는 빈 화면 대신 공통 준비중 화면으로 표시
 > 현재 실제 구현 대상은 대시보드, 새 영상 만들기, 영상 라이브러리, 계정 관리로 한정하고, 게시 관리/예산 관리 등은 데이터 없는 placeholder 상태로 유지

- 영상 라이브러리(/videos/library) 화면 제작
 > 생성된 영상 목록을 테이블과 우측 미리보기 패널로 확인할 수 있는 화면을 구성
 > 현재는 더미 데이터 기준이며, 목록 페이지네이션 계산 로직을 분리하고 테스트 추가

- 영상 생성 API 연동 기반 작업
 > 프론트엔드가 백엔드를 직접 호출하지 않도록 Next.js API proxy를 추가
 > 현재 백엔드 계약 문서 기준으로 영상 생성 요청 DTO와 service 계층을 작성
 > 새 영상 만들기 화면에서 입력값 검증 후 생성 요청을 보내고, 처리 대기열에 등록하는 흐름까지 연결
 > API 요청 코드는 요청 단위 mutation으로 분리하고, 여러 요청의 순차 실행은 별도 business hook에서 관리하도록 정리
 > 현재 공개 API에는 이미지 업로드 endpoint가 없어, 우선 미리 등록된 image asset id를 입력받아 테스트 가능한 형태로 처리 처리해야 하는 경우는 별도의 상위 코드에서 묶어서 관리


[결과 / 현재 상태]
- CMS 기본 레이아웃, 주요 화면 구조, 공통 테이블, 영상 생성 입력 UI와 API 요청 흐름까지 1차 구현 및 검증 완료
- 현재 영상 생성은 미리 등록된 이미지 asset id 기반으로 테스트 가능한 상태이며, 실제 이미지 업로드 연동은 백엔드 API 확정 후 추가 필요

[이슈 / 블로커]
- 없음

[내일 할 일]
- 이미지 업로드 API 계약이 확정되면 새 영상 만들기 화면에 실제 업로드 흐름을 연결
- 영상 라이브러리와 영상 생성 요청 상태를 실제 API 데이터 기준으로 조회/갱신하는 구조 검토
```