#### Work (보일러플레이트 프로젝트)
---
- [x] common-utils 수정
	- utils 함수 추가: runAfterStateUpdate (상태 업데이트 함수를 실행하고, 다음 렌더링 사이클에서 콜백을 실행)
	- utils 함수 추가: buildQueryParams (객체를 URL 쿼리 파라미터 문자열로 변환)
	- utils 함수 추가: handleApiResponse (Effect Either 타입의 API 응답을 처리하고 성공 데이터를 반환하거나 에러를 던짐)

- [x] authService 리팩토링 (2개 메서드)
	- Either, getErrorMessage import 제거
	- handleApiResponse import 추가
	- login 메서드: 5줄 에러 처리를 1줄로 간소화
	- logout 메서드: 4줄 에러 처리를 1줄로 간소화
	- API_SERVICE_URL을 템플릿 리터럴로 변경


#### Work (AI Assiantance 프로젝트)
---
- [x] 검수 목록 체크리스트 작업 진행 마무리
	- 설정 영역 전화번호 그룹핑 통합
	- 구타입 요일시간대 시간대 노출작업 진행
	- 온에어 바로가기 작업 마무리 

- [x] 검수 목록 리스트 화면 한정: 페이지 클린 아키텍처 리팩토링
    - [x] columns-approve의 No 추출방법을 단순 rmIdx를 사용하는 방식에서 데이터 수를 계산하는 방식의 `<RowCell/>`을 사용하도록 개선
    - [x] 디렉터리 구조 재설계
        - _views/ 생성: 페이지 레벨 컴포넌트 (container, presenter, index)
        - _sections/ 생성: 기능 단위 섹션 (approve-search-section, approve-status-filter-section)
        - 도메인 레벨 파일 분리: constants.ts, types.ts
    - [x] Container/Presenter 패턴 적용
        - approve-list-page-container.tsx: 상태 관리 및 비즈니스 로직 처리
        - approve-list-page-presenter.tsx: 순수 UI 렌더링
        - Props를 통한 데이터와 이벤트 핸들러 전달
    - [x] 상수 및 타입 분리
        - constants.ts: APPROVE_SEARCH_OPTIONS, APPROVE_STATUS_OPTIONS 추출
        - types.ts: ApproveListFilters, ApproveSearchField 인터페이스 정의
        - as const 제거 및 readonly 타입 문제 해결
    - [x] 섹션 컴포넌트 분리
        - ApproveSearchSection: SearchFilter와 상태 필터 영역 통합 관리
        - ApproveStatusFilterSection: 8개 상태 버튼 그룹 렌더링
        - formatCount 유틸 함수 적용: 카운트 포맷팅 로직 간소화
    - [x] 기존 코드 대비 변경점
        - 인라인 상수를 별도 파일로 분리
        - 복잡한 JSX를 섹션 컴포넌트로 분리
        - 상태 관리와 UI 렌더링 완전 분리

- [x] 프로젝트 전체 서비스 파일 리팩토링: 공통 유틸 함수 적용
    - [x] 공통 유틸 함수 생성
		- utils 함수 추가: runAfterStateUpdate (상태 업데이트 함수를 실행하고, 다음 렌더링 사이클에서 콜백을 실행)
		- utils 함수 추가: buildQueryParams (객체를 URL 쿼리 파라미터 문자열로 변환)
		- utils 함수 추가: handleApiResponse (Effect Either 타입의 API 응답을 처리하고 성공 데이터를 반환하거나 에러를 던짐)
		- 반복되는 코드 제거 및 일관성 확보
    - [x] authService 리팩토링 (2개 메서드)
        - Either, getErrorMessage import 제거
        - handleApiResponse import 추가
        - login 메서드: 5줄 에러 처리를 1줄로 간소화
        - logout 메서드: 4줄 에러 처리를 1줄로 간소화
        - API_SERVICE_URL을 템플릿 리터럴로 변경
    - [x] approvalService 리팩토링 (1개 메서드)
        - Either, getErrorMessage import 제거
        - buildQueryParams, handleApiResponse import 추가
        - getApprovals 메서드: URLSearchParams 수동 생성 로직을 buildQueryParams로 변경
        - 30줄 이상의 파라미터 생성 로직을 9줄로 간소화
        - 5줄 에러 처리를 1줄로 간소화
        - API_SERVICE_URL을 템플릿 리터럴로 변경
    - [x] settingsService 리팩토링 (8개 메서드)
        - Either, getErrorMessage import 제거
        - buildQueryParams, handleApiResponse import 추가
        - 8개 메서드 모두 에러 처리 로직 간소화 (5줄 → 1줄)
        - getSettingRingById 메서드: URLSearchParams 로직을 buildQueryParams로 변경
        - JSDoc 주석 개선: 빈 주석 설명 추가, @Param 오타 수정, @param 추가
        - API_SERVICE_URL을 템플릿 리터럴로 변경
    - [x] adminService 리팩토링 (6개 메서드)
        - Either, getErrorMessage import 제거
        - handleApiResponse import 추가
        - 6개 메서드 모두 에러 처리 로직 간소화 (5줄 → 1줄)
        - JSDoc 주석에 @param 태그 추가
        - API_SERVICE_URL을 템플릿 리터럴로 변경
