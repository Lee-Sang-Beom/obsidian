#### Work (프로젝트: AI 검증코어 harness)
---
- [x] Core 로컬 실행 및 model-server 기동 오류 수정
  - Core/Testbed 분리 검증을 위해 `localhost:9000` Core 실행 상태 확인
  - `model-server` 컨테이너가 `jsonschema` 누락으로 기동 실패하는 문제 확인
  - `deploy/model-server/Dockerfile`에 `jsonschema>=4.0` 설치 항목 추가
  - `model-server` 이미지를 재빌드하고 컨테이너 재생성
  - `core`, `chroma`, `model-server`가 모두 healthy 상태로 올라오는지 확인
  - `http://localhost:9000/health/live`, `/health/ready` 정상 응답 확인

#### Work (프로젝트: AI 검증코어 프론트엔드)
---
- [x] 영상 제작 서비스 화면 구조 추가 및 정리
  - 기존 테스트베드 중심 화면과 별도로 실제 서비스용 화면 구조 추가
  - `/studio` 경로 추가
    - 제작 중인 영상 프로젝트를 확인하는 스튜디오 화면 구성
    - 새 영상 만들기 기능 유지
    - 로그아웃 기능 유지
    - 테스트베드 진입은 개발자 모드 성격의 별도 버튼으로 분리
  - `/chat` 경로 추가
    - 영상 제작을 위한 AI 대화 화면 구성
    - 영화, 뮤직비디오, 광고/홍보 카테고리 기반 진입 흐름 고려
    - 프로젝트 ID 기반 대화 진입 확장성 고려

- [x] 스튜디오 대시보드 UI 구성
  - 구성단계
	  - 모바일스러운 카드 배치 진행
	  - 데스크탑 화면에 맞는 제작 대시보드 구조로 변경
	
  - 좌측 영역 구성
	  - 제작 시작 카테고리
	  - 개발자 모드 진입 영역

  - 중앙 영역 구성
	  - 이어 작업할 제작 
	  - 진행 중인 제작
	  - 제작 완료 영상

- [x] 채팅 제작 화면 UI 구조 재설계
  - `/chat` 화면의 전체 레이아웃 재구성
  - 페이지 전체 스크롤을 제거하고 채팅 내부 영역만 스크롤되도록 조정
  - 좌우 사이드바 중심 구조에서 영상 제작용 작업 공간 중심 구조로 변경
  - 오른쪽 영역은 제작 결과와 장면 구성 확인 중심의 read-only 성격으로 정리
  - 이미지/영상 업로드 성격의 입력 요소는 채팅 흐름 안에서 처리하는 방향으로 정리
  - 제작 전 단계 화면에서 영상 유형 선택 UI를 재구성
  - 뒤로가기 버튼, 상단 배치, 빈 공간, 줄바꿈 문제 수정

- [x] 공통 로그아웃 버튼 스타일 개선
  - 프로젝트 내 로그아웃 버튼이 비활성화된 것처럼 보이던 스타일 개선
  - 클릭 가능한 버튼임이 명확하게 보이도록 hover, border, 색상, 아이콘 표현 정리
  - 스튜디오, 채팅 등 인증된 화면의 로그아웃 버튼 스타일 일관성 개선

- [x] 브랜드 아이콘 리소스 정리
  - 브라우저 탭 favicon은 `src/app/favicon.ico` 유지
  - 화면 내 브랜드 아이콘은 `public/img/favicon.png`를 사용하도록 변경
  - 프로젝트에서 사용하지 않는 public 하위 SVG 리소스 정리
  - 헤더와 브랜드 표시 요소의 아이콘 흐림 문제 개선
