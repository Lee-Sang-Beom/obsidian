#### Work (프로젝트: AI Assistance)
---
- [x] 검수 정보
	- [x] 오디오 상태관리 코드 리팩토링
		- `settings-modal-container` 하위 모든로직 관련: 미리듣기(API 요청 후 재생되어야하는 오디오정보), 현재 설정 정보, AI 분석 설정 정보 등에서 관리되는 모든 오디오 상태관리 로직이 `settings-modal-container`에서 이루어질 수 있도록 개선
			- 불필요하게 분리된 상태관리 로직을 통합
			- container/presenter 패턴에 맞는 파일구조를 목표로 개선 진행

- [x] ErrorBoundary UI 수정
	- 이전 페이지 이동 가능하도록 개선

- [x] ErrorBoundary 미작동하는 범위 분석
	- **분석이유**: `onairBizState`가 `OnAirBizStateEnum`에 없는 검수 리스트의 경우 ErrorBoundary 발생이 되어야하는데, 미발생하는 원인 탐색
	- **분석결과**
		- ErrorBoundary가 캐치하는 경우
			1. 렌더링 중 발생하는 에러
			2. 생명주기 메서드에서 발생하는 에러
			3. 컴포넌트 트리 전체의 constructor에서 발생하는 에러

		- ErrorBoundary가 캐치하지 못하는 경우
			1. 이벤트 핸들러 내부의 에러
			2. 비동기 코드 (setTimeout, Promise 등)
			3. 서버 사이드 렌더링
			4. **조건부 렌더링으로 인한 빈 값** ← 지금 상황
