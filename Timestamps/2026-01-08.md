#### Work (프로젝트: AI Assistance)
---
- [x] ErrorBoundary 적용 및 에러 처리 개선
    - [x] ErrorBoundary 컴포넌트 구조 이해 및 작동 조건 파악
        - React ErrorBoundary는 렌더링 중 발생하는 JavaScript 에러만 캐치
        - 조건부 렌더링으로 인한 빈 값(undefined, null)은 에러가 아니므로 ErrorBoundary 미작동
        - 명시적으로 `throw new Error()`를 사용해야 ErrorBoundary가 작동함을 확인
    - [x] React Router와 ErrorBoundary 통합
        - 문제: React Router의 기본 ErrorBoundary가 우선적으로 에러를 캐치하여 커스텀 ErrorBoundary UI가 표시되지 않음
        - 해결: `error-boundary.tsx`에 `RouterErrorBoundary` 컴포넌트 추가
            - `useRouteError()` 훅 사용하여 라우터 에러 캐치
            - 기존 `ErrorFallback` 컴포넌트 재사용
        - `app-route.tsx` 수정: 각 라우트에 `errorElement: <RouterErrorBoundary />` 추가
            - LOGIN, BASE 라우트에 errorElement 추가
            - 모든 하위 라우트(APPROVE.LIST, STATISTICS.APPROVE, ACCOUNT.MANAGEMENT, ADMIN.ADMIN_LIST)에 errorElement 추가
    - [x] Enum 유효성 검증을 위한 유틸리티 함수 생성
        - `utils/error-handler.ts`에 `assertEnum` 함수 생성
        - JSDoc 형식으로 상세한 주석 작성 (함수 설명, 제네릭 타입, 파라미터, 반환값, 에러 발생 조건, 사용 예시 포함)
        - Enum 객체에 존재하지 않는 값이 들어올 경우 명시적으로 에러를 발생시켜 ErrorBoundary 작동
    - [x] `columns-approve.tsx` 리팩토링
        - `assertEnum` 함수를 사용하여 모든 Enum 필드 유효성 검증 추가:
            - `productType` 컬럼: `PascalCaseProductTypeEnum` 검증
            - `vocPartRing` 컬럼: `VocPartEnum`, `OnAirIntroEnum` 검증
            - `onairBizState` 컬럼: `OnAirBizStateEnum` 검증 (기존의 조건부 처리 제거)
        - 백엔드에서 예상치 못한 Enum 값이 전달될 경우 ErrorBoundary UI 표시
    - [x] `ProductTypeTabsContentPresenter` 컴포넌트 개선
        - `assertEnum` import 추가
        - `ProductTypeEnum[productType]` → `assertEnum(ProductTypeEnum, productType, 'ProductType')` 변경
        - 잘못된 productType 값 전달 시 ErrorBoundary 작동 보장

- [x] Ring Container 컴포넌트 리팩토링    
    - [x] 코드 중복 패턴 분석
        - 4개의 Ring Container 컴포넌트(`DefaultRingContainer`, `SpecificDateRingContainer`, `CampaignRingContainer`, `WeeklyRingContainer`)에서 동일한 로직 반복 확인
        - 공통 패턴: `settingsDetail`에서 `approvalStatus`, `onairBizState` 추출 및 변환
        - 공통 패턴: `isShowAiAnalysisSettingInfo` 조건 계산 로직
        - 공통 패턴: "현재 설정 정보"와 "AI 분석 설정 정보" 두 개의 Presenter 렌더링
    - [x] `CommonRingContainer` 공통 컴포넌트 생성
        - 파일 위치: `common-ring-container.tsx`
        - 제네릭 타입 활용: `<T, P extends BasePresenterProps<T>>`로 다양한 Ring 타입 지원
        - `BasePresenterProps<T>` 인터페이스 정의: 모든 Presenter가 공통으로 받는 props 정의
        - `extraProps` 패턴 도입: `Omit<P, keyof BasePresenterProps<T>>` 타입으로 Presenter별 추가 props 유연하게 전달
        - 공통 로직 통합:
            - `assertEnum`을 사용한 `onairBizState` 변환
            - `approvalStatus` 기반 `isShowAiAnalysisSettingInfo` 계산
            - 두 개의 Presenter 렌더링 (현재 설정 / AI 분석 설정)
    - [x] `DefaultRingContainer` 리팩토링
        - 기존 중복 코드 제거 (약 30줄 → 8줄)
        - `CommonRingContainer`에 `DefaultRingPresenter` 전달
        - 추가 props 없이 기본 props만 사용
    - [x] `SpecificDateRingContainer` 리팩토링
        - 기존 중복 코드 제거 (약 30줄 → 8줄)
        - `CommonRingContainer`에 `SpecificDateRingPresenter` 전달
        - 추가 props 없이 기본 props만 사용
    - [x] `CampaignRingContainer` 리팩토링
        - 기존 중복 코드 제거 (약 35줄 → 10줄)
        - `CommonRingContainer`에 `CampaignRingPresenter` 전달
        - `extraProps={{ isNewProductType }}`로 필수 prop 전달
        - TypeScript 타입 안정성 확보: `isNewProductType`을 옵셔널이 아닌 필수로 처리
    - [x] `WeeklyRingContainer` 리팩토링
        - 기존 중복 코드 제거 (약 35줄 → 12줄)
        - `isNewProductType`에 따라 `WeeklyRingStandardPresenter` 또는 `WeeklyRingGroupedPresenter` 동적 선택
        - `CommonRingContainer`에 선택된 Presenter 전달
    - [x] 리팩토링 검증
        - 원본 코드와 기능 동일성 단계별 검증 완료
        - 모든 Container의 로직, 조건문, props 전달, 렌더링 결과 100% 일치 확인
        - Props 전달 순서 차이는 React의 props 객체 특성상 동작에 영향 없음 확인
        - 조건부 렌더링 로직 (`isShowAiAnalysisSettingInfo`) 정상 작동 확인
    - [x] 코드 품질 개선 효과
        - 중복 코드 약 120줄 제거 (4개 컴포넌트 총합)
        - 유지보수성 향상: 공통 로직 수정 시 한 곳만 변경
        - 타입 안정성 유지: 제네릭과 `Omit` 유틸리티 타입 활용
        - 확장성 개선: 새로운 Ring 타입 추가 시 `CommonRingContainer` 재사용 가능

- [x] `approve-types.ts`에 정의되어 있는 obj 객체 리팩터링 시도
	- `approve-types.ts`에 섞여있는 constants 정보를 `approve-constants.ts` 파일로 이동하는 것이 목적
	- 빌드 환경에서  파일 적용 시도 시, `approve-constants.ts`로 옮긴 obj 객체와 `approve-types.ts`의 참조관계에서 호이스팅 에러가 발생함
	-  `approve-constants.ts`에 포함된 객체정보를  `approve-types.ts`가 직접참조하지 않는 요소들만 이동과정 진행
