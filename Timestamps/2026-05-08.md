
#### Work (프로젝트: ICMS)
---
- [x] 페이지 상단 BreadCrumb 기능개선
	- 페이지 별 상세 페이지 진입 시 BreadCrumb 영역이 빈 영역으로 출력되는 문제 발생: 기존 메뉴 경로에 '상세' 세부경로 추가
	- 리팩터링
		- 이전: 깊은 조건문/ push 메소드로 인해 가독성이 떨어지고 긴 코드
		- 이후: 메뉴정보 매칭 헬퍼함수 분리/재구성, spread를 이용한 구조 평탄화

- [x] 업로드 이미지 개수 페이지 기능 구현
	- 총 업로드 완료 이미지 수량 표시 영역 추가
	- 항목별 업로드 이미지 수량 영역 추가(카테고리 분류별 업종/이미지수 계산 로직 별도로 분리)
	- 초기화 버튼 클릭 시, 같은 payload여도 fetch UI가 확인되도록 변경
	- 페이지 전체 UI 스타일 재구성

- [x] 테스트케이스 작성
	- 업로드 이미지 개수
		- 테이블 헤더(항목, 업로드 업종 수, 이미지 수, 업종 콘텐츠 이미지 수량, 업종 외 콘텐츠 이미지 수량)가 모두 렌더링되는가
		- 전체 데이터의 특정 값(족발)이 전체통계 테이블에 렌더링되는가
		- 기간통계 테이블에 기간 데이터 중 cnt > 0인 값이 렌더링되는가
		- 업종 콘텐츠 테이블에 기간 데이터의 대분류명이 렌더링되는가
		- 업종 외 콘텐츠 테이블에 기간 데이터의 기타 중분류명이 렌더링되는가
	
	- 인원 별 업로드 현황
		- 초기 렌더링 시 "월별 현황" 탭이 선택된 상태로 표시되는가
		- 월별 현황 캘린더에 API 데이터 이벤트가 렌더링되는가
		- "기간 내 현황" 탭 클릭 시 테이블(계정명, 개수, 총 업로드 완료 이미지 수량)이 표시되는가
		- "기간 내 현황" 테이블에 API 데이터가 올바르게 렌더링되는가
		- 총 업로드 완료 이미지 수량 합산 값이 올바르게 표시되는가
		- "기간 내 현황" 탭에 기간 검색 필터와 초기화 버튼이 존재하는가

- [x] 프로젝트 구조기술 README.md 작성

