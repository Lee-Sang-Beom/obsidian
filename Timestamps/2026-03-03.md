
#### Work (프로젝트: ICMS)
---

- [x] 가공 컨텐츠 개발 마무리 
	- 클릭 시 이미지가 출력되는 MediaLightBox 컴포넌트 구현
	- 개발 서버 내에 UI 출력 가능한 이미지가 존재하지 않아, 로컬 이미지로 페이지 동작 테스트 진행

- [x] 가공 컨텐츠 (`/settings/contents`) 테스트 케이스 작성
    - 가공 컨텐츠 목록이 정상적으로 렌더링되어야 한다
    - 검색 버튼 클릭 시 데이터를 다시 불러와야 한다
    - 초기화 버튼 클릭 시 필터가 초기화되어야 한다

- [x] 가공 영상 페이지 구현 시작 
	- 개발서버 미동작으로 인해, 실제 API 통신이 잘 이루어지고 프론트엔드 내부 로직이 그에 따라 잘 동작하는지 검토 필요
	- API 통신로직 및 프론트 호출부 구현
	- 프론트엔드 검색로직 구현

- [x] 가공 영상 (`/settings/video-detail`) 테스트 케이스 작성
	- 개발서버 미동작으로 인해, 실제 API 통신이 잘 이루어지고 프론트엔드 내부 로직이 그에 따라 잘 동작하는지 검토함에 따라 수정할 가능성이 있음
	    - 가공 영상 목록이 정상적으로 렌더링되어야 한다
	    - 영상 상태 변경이 정상적으로 이루어져야 한다
	    - 검색 필터 변경 시 데이터를 다시 불러와야 한다

- [x] 기타 공통 (보일러 플레이트 포함)
	- 테이블 스켈레톤 UI가 최대수 leaf만큼 열을 차지하도록 개선 (테이블 헤더의 row가 2개 이상일 때, 테이블 스켈레톤 UI가 최대열개수를 감지하지 못한 문제가 있었음)
	- 테이블 컴포넌트 디렉터리에 데이터 없음을 표현하는 table-not-row-comp 컴포넌트 추가 및 해당 컴포넌트를 공통 테이블 컴포넌트에 적용 진행

- [x] 보일러 플레이트
	- 매주진행: 패키지 주요버전 확인 및 업데이트 가능한 패키지 업데이트 진행
