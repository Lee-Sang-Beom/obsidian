#### Work (프로젝트: 영상 제작 프로젝트)
---
- 프론트엔드
  - `/chat` 영상 생성 API 연동 흐름 보완
    - 영상 생성 요청 생성 → 분석 시작 → 분석 상태 조회 → 제작안 검토 → 제출 → 실행 상태 조회 → 결과 조회 흐름 정리
    - 상태 조회는 `/submissions`가 아니라 요청 단건 조회 API를 사용하도록 확인
    - 제출 성공/실패 이후에도 요청 단건 상태를 다시 조회하도록 보강

  - 제작안 검토 단계 답변 구조 개선
    - 백엔드 `clarification_questions` 응답 기준으로 답변 UI 분기 처리
    - 강도 계열 답변은 자유 입력이 아니라 `NONE`, `LOW`, `MEDIUM`, `HIGH` 중 선택하도록 변경
    - 출연자 구성 답변은 `SOLO`, `GROUP`, `UNSPECIFIED` 중 선택하도록 변경
    - 출연자 수 입력은 `1~20` 범위로 제한
    - `wardrobe_en` 같은 제안값 기반 항목은 사용자가 수정하지 못하도록 읽기 전용 자동 적용 방식으로 변경

  - 제작안 검토 제출값 보정
    - 사용자가 `"네"` 같은 자유문장을 보내 schema 오류가 발생하지 않도록 처리
    - 현재 제작안의 JSON path 값을 읽어 제출 payload에 반영
    - 제안값이 있는 항목은 별도 입력 없이 기본 제출값으로 포함되도록 정리

  - 구간별 영상 생성 UI 보완
    - 긴 `asset id`, `segment id`가 카드 영역 밖으로 벗어나지 않도록 줄바꿈 처리
    - 구간별 실행 상태 카드의 텍스트 overflow 문제 수정
