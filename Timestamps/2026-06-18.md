
#### Work (프로젝트: 공통 AI 검증코어)
---
- `trustlayer/app/api.py` 파일 내 `app`이라는 FastAPI 인스턴스 기반 스웨거 구동
	- /health, /strategies, /query, /search, /reindex
- 파일별 실제 역할을 유지보수 기준으로 리팩토링
```
> 기존
trustlayer/retrieval/ 하위 공통
search_service.py
- 1차 검색 실행
- Chroma 벡터 검색
- Elasticsearch/BM25 키워드 검색
- merge + rerank 호출

retrieval.py
- 검색 후보 병합
- ResultPackage 생성
- 이름은 너무 넓은데 실제 역할은 candidate merge/package 쪽

retrieve_more.py
- 추가 검색이 필요한지 판단
- 추가 검색 계획 생성
- 추가 검색 결과 구조 정의

retrieve_more_service.py
- 실제 추가 검색 실행
- 보강 후보 병합
- 다시 rerank

retriever.py
- 예전 검색 함수
- embed_query
- in-memory vector_search/hybrid_search
- 기존 import 호환용


---

> 현재
trustlayer/retrieval/ 하위 공통
  __init__.py

  search_service.py              # 1차 검색 실행 서비스
  supplemental_service.py        # 부족할 때 추가 검색 실행 서비스

  candidate_fusion.py            # 후보 병합, 점수 통합
  result_package.py              # 검색 결과 패키징

  supplemental_policy.py         # 추가 검색 필요 여부 판단, 계획 생성

  reranker.py                    # rerank
  scoring.py                     # 점수 계산
  strategies.py                  # 검색 전략 설정

  in_memory_search.py            # 과거/테스트용 in-memory 검색
  
```

####  Work (프로젝트: Vringo ICMS, CMS)
---
- [x] 상용환경 정적 리소스 주소 m4->m5.ringo 로 변경
