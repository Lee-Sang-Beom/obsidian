
#### Work (프로젝트: 영상 제작 프로젝트)
---
- 프론트엔드
  - `/studio` 새 영상 만들기 흐름 복구
    - 새 영상 만들기 클릭 시 영화형/뮤직비디오/광고 및 홍보 중 하나를 선택하는 흐름 복구
    - 카테고리 선택 후 `/chat?category=...`로 이동되도록 진행

  - 다이얼로그 상태 관리 구조 정리
    - 다이얼로그 open/close 상태를 zustand hook으로 분리
    - 공통 dialog 상태 타입 추가
    - 생성 카테고리 선택 다이얼로그와 영상 생성 확인 다이얼로그 구조 정리

  - `/chat` 영상 생성 요청 화면 구조 정리
    - 한 파일에 몰려 있던 비즈니스 로직을 hook으로 분리
    - 생성 모드 선택 구조를 백엔드 대비 변경 적용
      - t2v->T2V
      - i2v->I2V 
      - r2v->R2V
    - 영상 길이 선택값 변경 적용
      - 15초(15)
      - 30초(30)
      - 1분(60)
      - 3분(180)
    - 영상 생성 요청 전 최종 확인 다이얼로그의 UI 변경

  - 공통 헤더 구조 적용
    - 로그아웃상태, 로그인 상태의 studio, 로그인 상태의 chat 경로에서 공통으로 호출하는 헤더컴포넌트 구현
	- 랜딩, 스튜디오, 채팅 화면의 헤더 스타일과 로고/타이틀 표시 방식 정리
    - `/chat` 화면에서 불필요한 뒤로가기 버튼 제거
    - 로고 클릭을 통한 스튜디오 이동 흐름 정리

  - 랜딩페이지 영상 생성 흐름 반영
    - 실제 `/chat` 생성 흐름에 맞춰 랜딩페이지 문구와 UI 구성 변경
    - 요청 → 생성 중 → 생성 완료 흐름을 보여주는 애니메이션 적용
    - 단순한 영상 생성 경험을 강조하는 방향으로 콘텐츠 정리

  - 영상 생성 요청 계약 정리 및 업데이트
    - 기존 UI 내 작성/관리되던 계약 프로퍼티의 업데이트 진행 
    - 계약명세 파일 업데이트 (`docs/contracts/video-generation-request-contract.md`)
    - 백엔드 전달용 multipart/form-data 구조 수정
      - `data`: 요청 JSON
      - `images`: 첨부 이미지 파일

  - 영상 생성 API 연동 준비
    - 영상 생성 domain service 파일 추가
    - 영상 생성 요청/조회용 React Query hook 추가
    - API 호출 전 단계에서 사용할 계약 타입과 요청 구조 정리
