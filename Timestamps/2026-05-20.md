

#### Work (프로젝트: 딸깍)
---

- [x] 캐릭터 스토리 제작 요청 코드 동작의 사용자 동작 및 반응 큰 흐름 분석
	- 1.UI — cs-create-wizard.tsx•다단계 폼 입력을 Zustand 스토어(cs-create-store.ts)에 임시 저장•최종 단계에서 [생성하기] 버튼 클릭 시, 스토어 데이터를 취합하여 Server Action 호출
	- 2.Server Action — character-story-action.ts•createCsProject 함수가 인증/검증 후, 캐릭터 스토리 도메인 서비스 호출
	- 3.Domain Service — character-story-service.ts•일반 프로젝트(projects, cuts) 생성 로직과 캐릭터 스토리 전용(character_story_*) 테이블 저장 로직을 하나의 트랜잭션으로 처리
	- 4.Drizzle ORM & DB — src/lib/db/schema/...•projects, cuts, character_story_characters 등 연관된 다수 테이블에 데이터를 동시에 INSERT
	- 5.응답 및 화면 전환•성공 시 생성된 projectId를 기반으로 캐릭터 스토리 전용 스크립트 페이지(/cs/script)로 이동

- [x] 캐릭터 스토리 제작 요청 코드 동작의 사용자 동작 및 반응 흐름 내 상세코드 분석
	- QueryClient 관리 (`query-client.ts`)
	    - 서버/클라이언트 환경별로 다른 싱글톤 전략 사용
	    - 서버: React `cache()`로 요청(request) 단위 싱글톤 → 요청 간 캐시 오염 방지
	    - 브라우저: 모듈 레벨 변수로 앱 전체 싱글톤 → 페이지 이동 시 캐시 유지
	- SSR 데이터 프리페치 (`create-cs-page.tsx`)
	    - 페이지 진입 시 서버에서 `prefetchQuery`로 `userCategoryQueries.list()` 미리 호출
	    - `dehydrate`로 QueryClient 캐시를 JSON 직렬화 후 `HydrationBoundary`로 클라이언트에 전달
	    - 클라이언트 컴포넌트에서 `useSuspenseQuery` 호출 시 캐시 재사용 → 추가 API 호출 없음
	- 쿼리 팩토리 (`user-category-queries.ts`)
	    - `queryOptions()`로 `queryKey`와 `queryFn`을 하나의 객체로 묶어 서버/클라이언트 양쪽에서 동일한 키로 재사용
	    - `userCategoryQueries.lists()`는 계층 구조 키(`['userCategory', 'list']`)로 관리 → 무효화 시 상위 키로 일괄 처리 가능
	- Zustand 스토어 (`cs-wizard-store.ts`)
	    - `create()`가 `set`/`get`/`api`를 자동 주입하는 훅을 생성
	    - `createCoreSlice`와 `createCSSlice`를 슬라이스 패턴으로 분리 후 합성
	    - 컴포넌트에서 `useCSWizardStore((s) => s.step)` 형태로 필요한 상태만 선택적으로 구독
	- 캐릭터 생성 뮤테이션 (`cs-character-setup-step.tsx`)
	    - 사용자가 사진 크롭 완료 시 `createMutation.mutate(blob)` 호출
	    - `mutationFn` 내에서 `uploadCharacterPhotoAction` → `createCharacter` 순서로 서버 액션 2개를 순차 호출
	    - `onSuccess`에서 스토어 업데이트 + `invalidateQueries`로 캐릭터 목록 캐시 무효화
	- 크레딧 차감 로직 (`credit-repository.ts`)
	    - `deductCredits` 함수에서 CTE SQL 한 방으로 지갑 잔액 UPDATE + 거래 기록 INSERT 원자적 처리
	    - `idempotencyKey`로 중복 요청 방지, `RETURNING *` 결과가 0이면 지갑 재조회로 실패 원인 구분 (지갑 없음 vs 잔액 부족)
	    - DB row(`snake_case`) → 도메인 객체(`camelCase`) 변환은 `toTransactionFromRaw`가 담당
