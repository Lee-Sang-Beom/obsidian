

#### Work (프로젝트: 딸깍)
---

- [x] QueryClient 관리 방식 분석
	- Next.js App Router 환경에서 서버/클라이언트 간 QueryClient 공유 구조 파악
	- `src/lib/query-client.ts`에서 환경별 싱글톤으로 정의
	    - **서버**: React `cache()`로 감싸 요청(request) 단위 싱글톤 → 요청 간 캐시 오염 방지
	    - **브라우저**: 모듈 레벨 변수로 앱 전체 싱글톤 → 페이지 이동 시 캐시 유지
	- 페이지 파일(`create-cs-page.tsx`)에서 `getQueryClient()`를 호출해 SSR 시점에 데이터 prefetch 후 `dehydrate`로 클라이언트에 전달하는 방식으로 활용