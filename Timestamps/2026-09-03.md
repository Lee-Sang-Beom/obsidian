#### Work (프로젝트: 영상 제작 프로젝트)

- 프론트엔드
  - `/studio` 완료 영상 목록 API 계약 변경 반영
    - 목록 화면에서 필요한 최소 response projection 기준으로 타입/mapper 수정
    - `prompt_text` 대신 `prompt_preview`, `video.size_bytes` 대신 `size_bytes`를 사용하도록 변경
    - 완료 영상 카드에서 `request_id` 기준 상세 페이지로 이동하는 흐름 추가

  - 완료 영상 상세 페이지 추가
    - `/studio/videos/[videoRequestId]` 상세 페이지 라우트 추가
    - 선택한 완료 영상의 상세 정보 조회 service/query hook 추가
    - 상세 화면에서 영상 재생, 다운로드, 제작 요청, 결과 정보, 작업 기록, 식별 정보를 확인할 수 있도록 구성
    - 상세 페이지 헤더 및 로딩 UI 정리

- 백엔드
  - 완료 영상 목록 API를 카드 목록용 최소 응답으로 분리
    - `GET /v1/video-requests/completed` 응답을 `request_id`, `title`, `prompt_preview`, `size_bytes`, `completed_at` 등 카드 렌더링 필드 중심으로 축소
    - 목록 응답에서 `prompt_text`, `progress`, `error`, `video`, `created_at`, `updated_at` 제거
    - 완료 영상 목록 mapper를 상세 mapper와 분리

  - 완료 영상 단건 상세 조회 API 추가
    - `GET /v1/video-requests/{video_request_id}/completed-video` 라우트 추가
    - `request_id` 기준으로 완료 master video 상세 정보, 전체 prompt, progress/error, video descriptor 반환
    - 미완료 요청, 타 사용자 요청, 누락된 master asset에 대한 404 오류 계약 추가
    - 관련 API 테스트와 OpenAPI/프론트 연동 문서 갱신