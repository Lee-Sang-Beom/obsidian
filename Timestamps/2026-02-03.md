#### Work (프로젝트: AI Assistance)
---
- [x] Sentry.io 기본 설정 변경
	- 개발 환경에서는 Sentry.io에 에러 메시지가 전달되지않도록 변경
	- 성능 모니터링 비활성화: 무료 플랜에서 쿼터가 일주일 내에 소모될 확률 높음 (에러 처리가 우선적인 목표이니 비활성화)
	- 브라우저 확장 프로그램 에러의 경우 sentry.io에 쌓이지 않도록 변경

- [x] API 에러 관련 Sentry.io 로직 개선
	- 클라이언트/비즈니스 로직 에러를 DomainError로, 기타 에러(서버/인프라 에러, 네트워크 에러)를 GlobalError로 분류하여 Sentry에 전송 진행
		- DomainError는 Sentry에 기록되는 에러 level을 warning으로 지정 (GlobalError는 level을 error로 지정)

- [x] React ErrorBoundary 기반 프론트엔드 에러 추적 시스템 구현
	- 프론트엔드 내부 런타임환경 에러를 감지할 수 있도록 `ErrorBoundary` 컴포넌트에 Sentry 연동 추가
	- 전역 에러 핸들러 추가 (window.onerror, unhandledrejection)로 `ErrorBoundary`가 체크하지 못하는 런타임 에러 감지
	- Sentry 유틸리티 함수 모듈화 (sentry-utils.ts)
	- 전역 에러 핸들러 추가 (main.tsx)

- [x] Sentry.io 제공 통계관련 기능 조사
	- Metrics: 카운터, 게이지, 분포 값을 전송하여 애플리케이션 헬스 시그널을 추적하며, 모든 메트릭이 trace_id를 포함해 에러, 로그, span과 연결되어 근본 원인 분석이 가능함
	- Logs: 실시간으로 모든 속성으로 검색 가능한 구조화된 로그를 제공하며, errors 및 traces와 연결하여 전체 컨텍스트를 파악하고 알림 및 대시보드를 구성할 수 있음
	- Traces: 요청의 전체 실행 경로를 span 단위로 추적하여 성능 병목 지점을 파악하고, API payloads, 캐시 크기 등 상세 정보를 조사해 사용자 불만의 정확한 원인을 찾을 수 있음

- [x] Sentry 에러 전달 시, Fingerprint 그룹핑 진행
	- 목적: 동일한 원인의 에러가 여러 번 발생해도 하나의 Issue로 묶어서 Sentry의 Issue 개수를 줄이고 관리를 용이하게 함 (isssue가 계속 쌓이는게아니라 최근 이슈가 계속 같은 fingerprint를 덮어씌움)
	- API 에러: HTTP 메서드, URL, 상태 코드로 세밀하게 그룹핑하여 동일한 엔드포인트의 같은 에러를 하나로 묶음 (예: `['api-error', 'DomainError', 'GET', '/api/users', '401']`)
	- React 에러: 청크 로드 에러는 모두 통합(`['chunk-load-error']`)하고, 일반 에러는 에러 타입별로 분류(`['react-error', 'TypeError']`)하여 관리
	- 전역 에러: 발생 위치(unhandledError/unhandledRejection)와 에러 타입을 조합하여 그룹핑 (예: `['global-error', 'unhandledRejection', 'ReferenceError']`)

- [x] Sentry 에러 전달 시, Tags 분류
	- 목적: Sentry 대시보드에서 원하는 조건으로 에러를 검색/필터링하기 위한 라벨 역할
	- API 에러: errorType(DomainError/GlobalError), errorCode(상태 코드 또는 에러 식별자), source('api')로 태깅하여 API 관련 에러를 필터링하고 분류
	- React 에러: errorType('ReactError'), isChunkError(true/false), source('frontend')로 태깅하여 청크 에러 여부와 프론트엔드 에러를 구분
	- 전역 에러: errorType(unhandledError/unhandledRejection/GlobalRuntimeError), source('frontend')로 태깅하여 전역 런타임 에러의 발생 유형을 분류