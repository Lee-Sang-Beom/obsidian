#### Work (프로젝트: 제주 렌터카 프론트엔드)
- 계정 table 상태 파생 로직 분리 및 테스트 추가
    - 목록 query와 삭제 mutation error를 table UI state로 변환하는 로직을 `useAccountsTable` hook에서 `account-table-state.mapper.ts`로 분리
    - business hook은 query/mutation orchestration에 집중하도록 정리하고, stale data 숨김 정책은 테스트 가능한 순수 함수로 분리
    - 권한 차단 시 stale 목록 숨김, row-level 삭제 실패 시 목록 유지, list error 우선순위 규칙을 검증하는 테스트 추가(`account-table-state.mapper.test.ts`)
- 계정 목록 error alert 컴포넌트 분리
    - `AccountsTableSection`에 몰려있던 error alert UI와 summary 문구 계산 로직을 `AccountTableErrorAlert` 컴포넌트로 분리
    - section 컴포넌트는 header/action/table 조립 책임에만 집중하도록 단순화
- 계정 관리 권한별 UX 검토 및 보강
    - 관리자/운영자 권한별 목록 접근, 생성/수정/삭제 오류 표시, cache stale data 차단 흐름을 코드 기준으로 점검
    - 권한 확인 query가 pending인 짧은 구간에도 계정 추가 버튼이 비활성화되도록 보강하여, 권한 확인 전 조작 가능성 차단
- 프론트엔드 화면설계서 작성
    - 로그인, 영상관리, 계정관리 화면의 구성/입력값/상태/예외 UX/API 연동 내용을 현재 구현 기준으로 `docs/screen-specs/`에 문서화
    - 신규 작업자나 백엔드 협업자가 주요 CMS 화면의 동작 범위를 빠르게 확인할 수 있도록 정리
- 영상 프롬프트 가드 1차 구현 및 문서화
    - 개인정보/인증정보, prompt injection, 불법 행위, 성적/혐오/폭력, 유명 IP 복제 요청 등을 프론트 hard block rule로 추가
    - 백엔드 가드 이전 단계에서, 사용자가 제출 전 수정 가능한 위험 입력을 화면에서 차단하도록 구현
    - hard block 범위, 현재 차단하지 않는 항목, 백엔드 가드와의 역할 분리를 문서화하고 영상관리 화면설계서에 연결
- 검증
    - 관련 mapper/schema 테스트, lint, build 전체 통과 확인
    - 권한별 UX 점검 및 pending 버튼 보강 후 재검증, 프론트 프롬프트 가드 추가 후 관련 스키마 테스트까지 재검증 완료

#### Work (프로젝트: 제주 렌터카 백엔드)
---
- Auth 운영 데이터 자동 정리 정책 수립 및 반영
    - `cms_user_sessions`, `cms_login_audit_logs` 테이블 용도와 적재 가능성 검토
    - 만료/로그아웃 처리된 사용자 세션은 운영 확인을 위해 60일 보관 후 자동 삭제하도록 정책 설정
    - 로그인 감사 로그는 보안 이슈 추적과 운영 감사를 위해 365일 보관 후 자동 삭제하도록 정책 설정
    - auth store 초기화 시 오래된 세션/감사 로그를 자동 정리하는 cleanup 로직 추가

- Auth 데이터 정리 성능 및 설정 보강
    - `AUTH_SESSION_RETENTION_DAYS`, `AUTH_LOGIN_AUDIT_RETENTION_DAYS` 환경 설정 추가
    - 세션 정리 조회를 위한 `cms_user_sessions.revoked_at` 인덱스 추가
    - 감사 로그 정리 조회를 위한 `cms_login_audit_logs.created_at` 인덱스 추가
    - `.env.example`에 기본 보관 기간 반영

- 영상 요청 소유권 기반 master video 접근 경로 추가
    - 기존 `/v1/video-assets/{asset_id}/content` 호환 경로는 유지
    - 현재 로그인 사용자의 video request 소유권을 검증하는 `/api/video-requests/{request_id}/master-video/content` endpoint 추가
    - `GET /api/video-requests/{request_id}/master-video` 응답에 `owned_content_url` 추가
    - 프론트 결과 화면에서는 `owned_content_url`을 우선 사용하도록 계약 정리

- 영상 생성/사용자 정보 연결 안정성 검토
    - 기존 영상 생성 및 asset 생성 로직을 직접 수정하지 않는 wrapper 방식으로 구현
    - terminal 상태의 master video 조회/재생 구간에서만 사용자 소유권 검증이 개입되도록 범위 제한
    - 기존 공개 asset content endpoint 동작은 유지하여 기존 영상 파이프라인 영향 최소화
    - master video content 응답이 기존과 동일하게 `video/mp4` bytes를 반환하도록 확인

- 문서 및 테스트 갱신
    - Auth API 계약 문서에 세션/로그 보관 정책 추가
    - 영상 생성 Swagger/API overview 문서에 `owned_content_url` 및 신규 master video content endpoint 반영
    - 영상 생성 request contract 문서에 프론트 재생 경로 우선순위 반영
    - 기본 보관 기간과 설정 override 동작을 검증하는 설정 테스트 추가
    - master video descriptor 및 protected content endpoint 검증 테스트 추가
    - 변경 Python 파일 문법 검사 및 diff check 수행

- 커밋 정리
    - `refactor: add auth data retention cleanup` 커밋으로 Auth 운영 데이터 정리 정책 반영
    - `feat: add owned master video content endpoint` 커밋으로 영상 요청 소유권 기반 content 접근 경로 반영
