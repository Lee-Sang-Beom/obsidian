
#### Work
---
- [x] 팟캐스트: 과도한 다중 역할을 하는 use-podcast-list-data hooks의 기능 분리
	- 기존 `use-podcast-list-data.ts`가 굉장히 많은 기능을 처리하고 있었음. (조회, 무한스크롤, 오디오 관련 source state 기본관리, 재생제어, 상세보기 핸들러) 
		- 따라서,단일 책임 원칙에 따라 5개의 독립적인 hook으로 분리
	- `use-podcast-list-data.ts`: 팟캐스트 데이터 조회만 담당 (useInfinitePodcasts 호출 및 반환값 정규화)
	- `use-podcast-infinite-scroll.ts`: 무한 스크롤 처리 (기존 useInfiniteScroll 활용하도록 변경, 제거됨)
	- `use-podcast-audio-helper.ts`: 오디오 소스 및 재생 상태 체크 (getAudioSrc, isCurrentlyPlaying, isCurrentlyLoading)
	- `use-podcast-playback-control.ts`: 팟캐스트 재생 제어 (이미지 클릭 시 재생/일시정지 처리)
	- `use-podcast-detail-handler.ts`: 팟캐스트 상세 보기 핸들러 (상세 모달 열기 전 상태 관리)
	- `news-bottom-section.tsx`에서 분리된 5개 hook을 각각 호출하여 사용
	- 기존 기능 동작은 완전히 동일하게 유지하면서 코드 가독성 및 유지보수성 향상

- [x] overall-market 영역 리팩토링 진행
	- 주요 내용
	    - **overall-market-container 변경내용**
	        - 비즈니스 로직과 상태 관리를 담당하는 컨테이너 컴포넌트로 분리
	        - `useGetLiveStockInfo` 훅을 통한 실시간 주식 정보 조회
	        - `useInView` 훅을 사용하여 뷰포트 내 진입 시에만 데이터 로딩 (성능 최적화)
	        - `INDEX_NAME_MAP`을 통해 영문 코드를 한글 이름으로 매핑
	        - 데이터 정렬 로직: `INDEX_NAME_MAP`에 정의된 순서대로 지수 데이터 정렬
	        - AI 분석 다이얼로그 오픈 기능 (`useOpenAiSignalAnalysisDialog`) 통합
	        - Presenter 컴포넌트에 props 전달: `indexData`, `isLoading`, `error`, `onRefresh`, `onHelpClick`
	    - **overall-market-presenter 변경내용**
	        - UI 렌더링만 담당하는 프레젠테이셔널 컴포넌트로 분리
	        - `forwardRef`를 사용하여 ref를 외부에서 전달받아 intersection observer 적용 가능
	        - 그라디언트 배경 (`from-blue-600 to-blue-800`)과 장식 요소로 시각적 디자인 구현
	        - 헤더 영역: 제목("전체 시장 현황")과 도움말 버튼 배치
	        - 로딩/에러/빈 데이터 상태를 `MobileLoadingState` 컴포넌트로 처리
	        - 데이터가 있을 경우 `MarketCardList` 컴포넌트로 위임
	        - 가로 스크롤 지원 (`overflow-x-auto`)
	    - **market-card-list 변경내용**
	        - 시장 데이터 배열을 받아 개별 카드로 렌더링하는 중간 레이어 컴포넌트
	        - `getPrecision` 함수로 코드별 소수점 자릿수 결정
	            - BTC/ETH: 가격 2자리, 변화량 2자리
	            - USDKRW: 가격 1자리, 변화량 1자리
	            - 기타 지수: 모두 2자리
	        - 가로 스크롤을 위한 flexbox 레이아웃 (`flex gap-2`)
	        - `minWidth: max-content`로 카드가 줄바꿈 없이 가로로 나열되도록 설정
	        - 각 항목마다 `MarketCard` 컴포넌트 생성
	    - **market-card 변경내용**
	        - 개별 시장 지수를 표시하는 최소 단위 컴포넌트
	        - `getCardStyle` 함수로 등락률에 따른 동적 스타일 적용
	            - 상승(+): 빨간색 그라디언트 배경
	            - 하락(-): 파란색 그라디언트 배경
	            - 보합(0): 회색 그라디언트 배경
	        - 카드 크기: 고정 `75px × 130px`
	        - 상승/하락 아이콘: `Play` 아이콘을 회전시켜 방향 표시 (상승 270도, 하락 90도)
	        - `AnimatedNumber` 컴포넌트로 숫자 애니메이션 효과 적용
	        - 호버 시 확대 효과 (`hover:scale-105`) 및 그림자 강조
	        - 다크 모드 지원

- [x] hooks 하위 파일별로 jsDoc 주석 추가 (역할)
	- news domain 하위 hooks (8개)
	- signal domain 하위 hooks (21개)

- [x] 서비스 파일 내 메소드 별 역할에 대한 JSDoc 주석 추가 / 불필요한 v1 코드 삭제(signal, podcast)
	- auth-service.ts: 인증 관련 함수들(로그인, 소셜 로그인, 비밀번호 찾기, 로그아웃 등)에 JSDoc 주석 추가
	- log-service.ts: 로깅 서비스 함수에 JSDoc 주석 추가
	- notice-service.ts: 공지사항 조회 함수에 JSDoc 주석 추가
	- payment-service.ts: 결제 초기화 및 Google/Apple 결제 검증 함수에 JSDoc 주석 추가
	- podcast-service.ts: 팟캐스트 목록 조회(V1/V2), 실시간 발언 목록 조회, 상세 정보 조회 함수에 JSDoc 주석 추가
	- signal-service.ts: 시그널 조회, 즐겨찾기 관리, 재무 정보, 차트 데이터 조회 함수에 JSDoc 주석 추가
	- user-service.ts: 회원가입, 사용자 정보 조회/수정, 비밀번호 변경, 회원탈퇴 함수에 JSDoc 주석 추가
	- 모든 함수에 @param, @returns, @throws 태그를 포함하여 매개변수, 반환값, 예외 정보 명시
	- 각 서비스 객체에 전체 기능 설명 추가

- [x] 프로젝트 내 import 문에 대한 경로 지옥(Patch Hell/Relative Path Problem) 문제 해결
	- Path Alias을 사용하여 문제가 있는 모든 파일 정리