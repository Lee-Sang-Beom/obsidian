
#### Work (프로젝트: 영상 제작 프로젝트)
---
- 프론트엔드
  - `/chat` 페이지 영상 생성 API 연동 흐름 보완
	- 영상생성 전과정 흐름분석
    - 영상 생성 요청 생성 → 분석 시작 → 분석 상태 조회 → 제작안 검토 → 제출 → 실행 상태 조회 → 결과 조회 흐름 정리
    - 상태 조회는 `/submissions`가 아니라 요청 단건 조회 API를 사용하도록 확인
    - 제출 성공/실패 이후에도 요청 단건 상태를 다시 조회하도록 보강

  - 제작안 검토 단계 응답 처리 개선
    - 중복된 `clarification_questions` path가 내려오는 경우 하나로 병합되도록 처리
    - 동일한 path의 질문이 여러 번 표시되어 React key 중복 경고가 발생하는 문제 수정
    - 검토 화면에서 실제 응답값이 어떤 방식으로 `reviewDisplay`에 매핑되는지 문서화

  - 영상 생성 실행 단계 구조 정리
    - `GenerationWorkspace`와 `useExecutionPollingFlow` 흐름 문서화
    - 요청 단건 조회와 구간별 실행 목록 조회의 역할 분리 정리
    - 최종 완료 기준이 구간별 상태가 아니라 `master_video_asset_id`가 되도록 개선

  - 영상 생성 단계 UI 정리
    - 사용자에게 노출되던 요청 ID, query 상태, raw status, segment id, asset id 등 디버깅성 정보 제거
    - 서비스 화면에는 분석 중, 생성 준비 중, 생성 중, 결과 준비 등 사용자용 문구로 표시
    - 디버깅 정보는 브라우저 콘솔과 개발자 로그 다이얼로그에서만 확인 가능하도록 분리
    - 생성 구간 카드의 상태 뱃지 줄바꿈 문제 수정
    - 생성 방식은 사용자가 선택한 `T2V`, `I2V`, `R2V` 값 그대로 표시되도록 정리

  - 결과 화면 보완
    - 결과 영상 Blob object URL 생성 로직에서 `setState in effect`  오류 수정
    - 결과 화면의 내부 asset id 노출 제거
    - 영상 재생/다운로드 기능은 유지하면서 결과 정보 표시를 서비스용 문구 중심으로 정리