
####  Work
---
### 프로젝트: 82ALDA Podcast CMS

##### 페이지: 공지사항
- [x]  **FileInput 직접 구현**
    - 드래그 앤 드롭, 파일 선택, 파일 크기/타입 검증 등 기본 파일 업로드 기능 구현
    - 기존 파일(서버)과 신규 파일(로컬)을 구분하여 관리하는 상태 관리 로직 구현
    - 파일 삭제 기능 및 중복 파일 검증 로직 추가
    - 다중 파일 업로드 지원 및 실시간 파일 목록 UI 표시 기능 구현

- [x]  **FileInput 개선을 위한 라이브러리 탐색 및 적용**
    - 직접 구현한 FileInput의 유지보수 어려움 및 브라우저 호환성 이슈 파악
    - react-dropzone 라이브러리 선정 (npm드래그 앤 드롭, 파일 검증, 접근성 지원)
    - 기존 FileInput의 인터페이스와 동작 방식을 유지하면서 라이브러리로 전환
    - 에러 처리 및 사용자 피드백(toast) 개선으로 UX 향상

- [x]  **공지사항 등록/수정 기능에 파일 관리 로직 추가**
    - 공지사항 등록 시 다중 파일 업로드 API 연동 (`/notices/upload`)
    - 임시 폴더(`/temp/YYYYMM`)에 파일 업로드 후, 등록 완료 시 `/notice/YYYYMM`로 이동하는 2단계 업로드 프로세스 구현
    - 공지사항 수정 시 기존 파일 유지/삭제 및 신규 파일 추가 로직 구현
    
- [x]  **FileUploadDropzone 컴포넌트 및 useFileUploadManager 훅 구현**
    - react-dropzone을 활용한 FileUploadDropzone 컴포넌트 구현으로 코드 재사용성 향상
    - 파일 관리 로직을 useFileUploadManager 커스텀 훅으로 분리하여 관심사 분리
    - NoticeForm에서 60줄 이상의 파일 관련 로직 제거, 단일 훅 호출로 간소화
    - 기존 파일(existingFiles)과 업로드된 파일(uploadedFiles)을 명확히 분리 관리
    - 다른 폼(FAQ, 이벤트 등)에서도 재사용 가능한 범용 파일 업로드 솔루션 완성
    - 파일 상태 관리, 업로드, 삭제, 초기화 등 모든 파일 작업을 통합 관리
    - Toast 알림을 통한 업로드 성공/실패 피드백 및 업로드 중 상태 표시로 사용자 경험 개선

- [x] 공지사항 수정사항 반영
	1. data-table에 cell 컴포넌트 3개 추가
	2. modals 디렉터리 추가  
	3. file-upload-dropzone.tsx 추가
	4. pnpm add react-dropzone 추가
	5. hooks/에 use-detail-view-files, use-file-upload-manager.ts 추가
	6. lib/types에있던 file관련 타입추가
	7. axios-data 업데이트
	8. modal-provider에 files-detail-modal 추가

