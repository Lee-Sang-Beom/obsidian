
```

WorkOS AuthKit이 좀 특이하네요.
UI 설계랑 Provider를 로컬 코드에서 하는게아니라 공식 홈페이지 로그인하고, CMS처럼 대시보드에서 하는방식이군요

프론트엔드 측에서 stale 라우팅 조작이랑 인증콘솔 관리/초기화
계정 생성하고 아래 절차가 필요할 것 같습니다.

1. WORKOS_CLIENT_ID, WORKOS_API_KEY 확인 및 실무자 공유
2. Authentication/Providers: Google만 enable
3. Authentication -> Related Section/Branding: 인증관련화면 UI 조작


```

#### Work (프로젝트: AI 검증코어 프론트엔드)
---
- [x] Clerk 인증 구조 제거 및 WorkOS AuthKit 도입
  - 기존 Clerk 기반 인증 구조를 WorkOS AuthKit 기반으로 대체
  - `pnpm`으로 WorkOS AuthKit 패키지 설치
    - `@workos-inc/authkit-nextjs`
    - `@workos-inc/node`
  - Clerk 관련 패키지 제거
    - `@clerk/nextjs`
    - `@clerk/localizations`
  - 앱 전역 Provider 변경
    - 기존 `ClerkProvider` 제거
    - `AuthKitProvider` 추가
  - 로그인/회원가입 라우트 추가
    - `/sign-in`
    - `/sign-up`
  - WorkOS callback route 추가
    - `/auth/callback`
  - Next.js 16 기준 `proxy.ts`에서 WorkOS `authkitProxy()` 적용
  - `/chat`, `/api/verification/*` 등 서비스 영역은 인증 필요 영역으로 유지
  - `/`, `/sign-in`, `/sign-up`, `/auth/callback`은 비로그인 접근 허용

- [x] WorkOS AuthKit 환경변수 분리
  - 로컬 개발 환경은 `.env.local`을 사용하도록 구성
  - 배포 환경은 `.env.prod`를 사용하도록 구성
  - 로컬 callback URI 설정
    - `NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback`
  - 배포 callback URI 설정
    - `NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://192.168.1.22:3001/auth/callback`
  - WorkOS 필수 환경변수 정리
    - `WORKOS_CLIENT_ID`
    - `WORKOS_API_KEY`
    - `WORKOS_COOKIE_PASSWORD`
    - `NEXT_PUBLIC_WORKOS_REDIRECT_URI`
  - `WORKOS_COOKIE_PASSWORD` 생성 방식 안내
    - Windows PowerShell에서 32바이트 랜덤 base64 문자열 생성

- [x] WorkOS 대시보드 설정 정리
  - WorkOS Application Redirect URI 설정 안내
  - 로컬/배포 callback URI 모두 등록
    - `http://localhost:3000/auth/callback`
    - `http://192.168.1.22:3001/auth/callback`
  - Initiate login URI는 하나만 가능하므로 배포 주소 기준으로 설정
    - `http://192.168.1.22:3001/sign-in`
  - Sign-out redirect 설정 안내
    - `http://localhost:3000`
    - `http://192.168.1.22:3001`
  - 로그인 방식은 WorkOS Dashboard의 `Authentication > Methods`, `Authentication > Providers`에서 관리되는 구조로 확인
  - 이메일 로그인과 Google 로그인만 남기기 위한 설정 정리
    - `Email + Password`: Enabled
    - `Google`: Enabled
    - `Single Sign-On`: Disabled in AuthKit
    - `Magic Auth`: Disabled
    - `Passkeys`: Disabled
    - `Microsoft`, `GitHub`, `Apple`: Disabled

- [x] WorkOS 로그인/회원가입 라우트 안정화
  - 초기 구현에서 `/sign-in`, `/sign-up`을 Server Component page에서 처리하던 구조로 인해 쿠키 수정 오류 발생
  - 오류 내용
    - `Cookies can only be modified in a Server Action or Route Handler`
  - WorkOS `getSignInUrl()`, `getSignUpUrl()` 호출 시 PKCE 쿠키가 설정되는 구조 확인
  - `/sign-in`, `/sign-up`을 `page.tsx`에서 `route.ts` GET Route Handler로 변경
  - 로그인/회원가입 진입 시 stale PKCE 쿠키 정리 로직 추가
    - `wos-auth-verifier-*` 쿠키 삭제
  - OAuth 시작 redirect 응답에 캐시 방지 헤더 추가
    - `Cache-Control: no-store, max-age=0`
    - `Pragma: no-cache`
  - redirect status를 `302`로 명시
  - 로그인/회원가입 CTA는 Next `<Link>` 대신 일반 `<a>`로 변경
    - OAuth state/cookie 생성 endpoint에 Next prefetch/client routing이 섞이지 않도록 개선

- [x] WorkOS 로그인/로그아웃 오류 대응
  - 로그인 후 `/chat` 이동 시 `431 Request Header Fields Too Large` 오류 원인 확인
    - 반복 로그인 시도 과정에서 PKCE 쿠키가 누적된 것이 원인으로 판단
    - 브라우저의 `localhost:3000` 사이트 데이터 삭제 필요 안내
    - 코드상으로도 stale PKCE 쿠키 정리 로직 추가
  - 로그아웃 시 WorkOS `app-homepage-url-not-found` 오류 원인 확인
    - `returnTo: "/"` 상대경로 전달로 WorkOS가 로그아웃 후 복귀 URL을 해석하지 못하는 문제
  - 로그아웃 시 현재 origin을 포함한 절대 URL 전달로 변경
    - 로컬: `http://localhost:3000`
    - 배포: `http://192.168.1.22:3001`

- [x] 로그인 사용자 정보 디버그 로그 추가
  - WorkOS AuthKit 로그인 상태 확인용 디버그 logger 추가
  - `src/components/providers/workos-auth-debug-logger.tsx`에서 `useAuth()` 값을 브라우저 콘솔에 출력
  - 이메일, 세션 id, 프로필 이미지 URL 등 개인정보가 포함되므로 운영 환경에서는 제거 또는 개발 환경 한정 필요성 정리

- [x] 인증 관련 문서 정리
  - Clerk 관련 문서와 샘플 JSONC를 `docs/auth/clerk` 하위로 정리
  - WorkOS AuthKit 사용자 정보 가이드 추가
    - `docs/auth/work-os-authkit/workos-authkit-user-guide.md`
  - WorkOS 로그인 사용자 샘플 JSONC 추가
    - `docs/auth/work-os-authkit/google-user.jsonc`
  - WorkOS 응답의 각 프로퍼티 의미 주석으로 정리

- [x] 배포 운영 문서 추가
  - `docs/deploy/frontend-docker-restart.md` 추가
  - `192.168.1.22` 서버에서 프론트엔드 Docker 컨테이너를 재시작하는 방법 정리
  - 단순 재시작 명령 정리
    - `docker compose -f docker-compose.prod.yml restart tf-ai-frontend`
  - 최신 코드 반영 후 재배포 명령 정리
    - `git pull`
    - `docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build`
  - 컨테이너 상태 확인 및 로그 확인 명령 정리
    - `docker compose -f docker-compose.prod.yml ps`
    - `docker compose -f docker-compose.prod.yml logs -f --tail=100 tf-ai-frontend`

- [x] WorkOS AuthKit 로그인 화면 브랜딩 방향 정리
  - Hosted AuthKit UI는 Next.js 코드에서 직접 HTML을 수정하는 방식이 아니라 WorkOS Branding 설정으로 커스터마이징하는 구조 확인
  - 프로젝트 랜딩 페이지 색상 기준으로 WorkOS Branding 색상 제안
    - 메인 검정: `#101114`
    - 호버 검정: `#2B2C30`
    - 브랜드 블루: `#3B49DF`
    - 다크 링크: `#B1A9FF`
    - 회색 텍스트: `#6B7078`
    - 보더: `#EBECEF`
    - 연한 배경: `#F6F7F9`
  - WorkOS Custom CSS 입력용 스타일 가이드 제공
    - Background
    - Header
    - Card
    - Primary button
    - Secondary button
    - Text field
    - Label
    - Callout
    - Organization selection
