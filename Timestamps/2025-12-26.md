#### Work (AI Assistance)
---
- [x] 검수 리스트
	- [x] PRE_READY, PENDING 상태 둘 다 클릭이 안되도록 변경
	- [x]  시작일, 종료일 선택 시 바로 닫힐 수 있도록 개선
	    - date-filter 내부적으로 useState를 통한 Popover의 열림/닫힘을 제어할 수 있도록 리팩토링
	- [x]  시작일/종료일을 '검색기간' 하나의 라벨로 통합하고 `[시작일] ~ [종료일]` 형태로 개선
	    - `DateRangeUnifiedFilter` 컴포넌트 신규 생성
	    - `FilterField` 인터페이스에 `dateRangeUnified` 타입 추가
	    - `ApproveSearchField` 인터페이스에 `dateRangeUnified` 관련 속성 추가 및 `placeholder`, `value`, `onChange`를 옵셔널로 변경
	    - 하나로 묶여있는 날짜선택핸들러 `handleDateChange`를 2개 함수로 분리 
	- [x]  시작일/종료일 입력 필드의 너비 조정
	    - `w-full`에서 `w-2/3`으로 변경
	- [x] 요청 설정 정보 column명을 요청 설정 유형으로 변경
	- [x] 온에어 제작 상태 내에서, '온에어 처리완료' 를 '처리완료'로
	- [x] 접수자, 처리자를 아이디(이름) 형태로 개선
	- [x] 초기화버튼이 새로고침되도록 변경

- [x] 검수 정보
	- [x] 멘트 한줄 말줄임표 처리 진행
	- [x] 멘트 호버 시, 전체 텍스트 띄울 수 있는 shadcn-ui tooltip 적용
	- [x] 상담내용 -> 상담메모로 변경
	- [x] 멘트 -> 요청멘트로 변경
	- [x] AI 분석 회선(상담 내용 분석 포함) -> 회선정보(상담 메모 분석 포함)로 변경
	- [x] 하위 '현재 설정 정보'에 현재 온에어 제작상태 전달하여 출력할 수 있도록 변경
	- [x] '추가는 n개까지 가능' 요소 전체 주석


- [x] 검수 통계
	- [x] 시작일, 종료일 동작이 검수 리스트와 동일하게 되도록 변경
	- [x] 시작일, 종료일 선택 시 바로 닫힐 수 있도록 개선
	    - date-filter 내부적으로 useState를 통한 Popover의 열림/닫힘을 제어할 수 있도록 리팩토링
	- [x] 시작일/종료일을 '검색기간' 하나의 라벨로 통합하고 `[시작일] ~ [종료일]` 형태로 개선
	    - 검수 목록과 동일하게 `DateRangeUnifiedFilter` 적용
	    - `statistics-filter-presenter.tsx`에서 `dateRangeUnified` 타입 사용
	- [x] 시작일/종료일 입력 필드의 너비 조정
	    - `w-full`에서 `w-2/3`으로 변경
	- [x] 검색 시작일, 종료일의 default 설정이 검수 리스트와 동일하게 작동되도록 변경
	- [x] 초기화버튼이 새로고침되도록 변경
	- [x] 차트 코드 수정
		- [x] 불필요한 highchart 라이브러리 대신 shadcn-ui 제공 chart 라이브러리를 사용한 방식으로 리팩터링
			- Highcharts → Recharts + shadcn/ui 마이그레이션
			- 100+ 줄의 복잡한 options 객체를 선언적 JSX 컴포넌트 구조로 변경하여 가독성 향상
			- 차트 설정(색상, 레이블)을 `STATS_CHART_CONFIG` 상수로 외부화하여 재사용성 및 유지보수성 개선
			- 하드코딩된 색상값을 CSS 변수(`var(--color-*)`)로 변경하여 테마 시스템과 통합
			- shadcn/ui Card 컴포넌트 활용으로 일관된 UI 구조 확립
			- 데이터 변환 로직 최적화 (map 제거, tickFormatter 활용)
			- LINE_CONFIG 배열로 반복되는 Line 컴포넌트 추상화
			- accessibilityLayer 추가로 웹 접근성 개선
			- 타입 정의를 별도 파일(`statistics-types.ts`)로 분리하여 타입 안정성 강화
		- [x] yAxis의 소수점자리출력문제 확인 및 해결
			- 통계 카운팅은 소수점으로 출력될 필요가 없음
			- yAxis allowDecimals 수치 조정으로 축에 표시되는 숫자가 소수점을 허용하지 않도록 개선