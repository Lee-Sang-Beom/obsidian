#### Work (프로젝트: 제주 렌터카)
---
- 영상 생성/수정(revision) API 계약 반영
    
    - 업데이트된 영상 생성 API 계약 문서 확인, revision(수정 요청) 관련 신규 기능 파악
    - service 계층에 완성 영상 수정 요청 생성/조회/boundary frame 준비 API 함수와 타입 추가
    - 계약 반영 범위에 대한 테스트 작성
- 영상 요청 상세 화면(`/videos/[videoRequestId]`) 구축
    
    - 동적 route와 feature 구조 추가, 실제 API 기반 상세 조회 query 및 polling 연결
    - 백엔드 DTO를 화면용 view model로 변환하는 mapper 추가
    - CMS 사이드바/breadcrumb가 상세 화면에서도 기존 메뉴 구조와 자연스럽게 매칭되도록 보정
    - master video 재생(blob object URL) 연결
- 영상 라이브러리 실 API 연동
    
    - 더미 데이터 의존 제거, 실제 영상 요청 목록 API로 전환
    - DTO → 화면 표시용 view model 매퍼 추가
    - 목록에서 상세 화면으로 이동하는 탐색 흐름 추가, 10초 간격 polling 우선 적용
- `/videos/create`를 단계형(step) workflow로 리팩터링
    
    - 백엔드 status를 화면 단계(step)로 변환하는 순수 함수(rule)와 polling 대상 상태 판정 로직 작성
    - 공통 진행 상태 카드를 분리한 뒤, analyzing / compiling / failed / ready / result / scenario-review / image-review 등 각 단계를 전용 화면으로 구현
        - scenario-review: 시나리오 확인·수정 피드백 검증 및 confirm/revise API 연동
        - image-review: 이미지 승인/재생성 API 연동 (생성 이미지 미리보기는 API 미제공으로 보류)
        - ready: 최종 생성 제출 UI 및 submit API 연동
        - result: 완료 안내 및 master video 재생 화면 연결
    - 임시 fallback 상태 화면 제거, `video-create-client.tsx`를 명시적 switch 기반 분기 구조로 정리
    - 단계별 코드(폼, mutation, mapper, rule)를 `steps/compose` 등 하위 디렉터리로 옮겨 루트에는 workflow 공통 코드만 남도록 정리
    - 영상 생성 성공 시 상세 페이지로 바로 이동하던 동작을 제거하고, workflow 내에서 단계 전환하도록 변경
- 개발 문서 정리
    
    - 다른 프로젝트의 `/chat` 단계 문서를 `old` 하위로 이동해 경계 정리
    - 현재 백엔드 공개 API 기준으로 가능한 step, status 매핑, 디렉터리 구조, 구현 순서를 정리한 신규 workflow 계획 문서 작성
- 이슈 대응 (개발 중 발생한 문제와 해결)
    
    - revision API가 아직 최종 수정 영상 생성까지 제공하지 않아, service 계층에는 공개된 endpoint만 반영하고 UI 확장은 보류
    - toast 호출 방식이 프로젝트의 Base UI toast manager와 맞지 않아 `toast.add({ title, description, type })` 형태로 수정
    - 라이브러리 목록 길이 변경 시 페이지를 effect로 보정하려던 방식을 제거하고, 보정된 page 값 기준으로 slice하도록 바꿔 React hooks lint 통과
    - imageReview mapper 테스트가 Map 구성용 tuple과 화면 summary 타입을 섞어 써서 처음 실패 → 생성 이미지 summary list와 map 생성 로직을 분리해 해결
- 결정사항
    
    - revision 기능은 우선 service 계층까지만 반영하고, 실제 UI/hook은 영상 상세 화면 설계 이후 추가하기로 함
    - 계약에 아직 노출되지 않은 revision 최종 생성 API는 프론트에서 임의 구현하지 않기로 함
    - 영상 상세 route는 완료 영상이 아니라 video request id 기준(`/videos/[videoRequestId]`)으로 구성하기로 함
    - 영상 라이브러리는 완료 영상 API가 아닌 video request 목록 API를 기준으로 연결하기로 함 (화면 이름과 실제 데이터 기준이 다르다는 점 유지 필요)
    - `features/videos/create` 루트에는 workflow 공통 코드만 남기고, 단계 전용 코드는 `steps/` 하위로 분리하기로 함
    - 1차 구조 리팩터링에서는 기존 "생성 성공 후 상세 이동" 동작을 그대로 유지해, 구조 이동과 동작 변경을 한 커밋에 섞지 않기로 함
    - 현재 공개 API로 구현 가능한 step만 우선 문서화·구현하기로 함 (이미지 업로드, retry/cancel, revision 최종 생성 등은 제외)
- 다음 단계
    
    - `/videos/library` 또는 영상 상세 화면 기준으로 revision 기능을 붙일 위치 결정
    - revision UI 구현 시 segment/frame 선택, 수정 지시문 입력, revision 생성, prepare 상태 표시 범위까지만 설계
    - `/videos/create`에 남아있는 입력 UI/form/mutation/mapper/rule을 각 step 하위로 마저 이동
    - 라이브러리 상태별 조건부 polling 최적화


```
일일 업무보고 요약

<제주 렌터카>
- 영상 수정 요청(revision) 관련 API 연동 작업
- 영상 신청 상세 화면과 목록 화면을 실제 API 데이터와 연결
- 영상 만들기 화면을 단계별로 나누어 재구성


[오늘 한 일]

<제주 렌터카>
- 영상 수정 요청 기능 준비
 > 새로 추가된 영상 수정 요청 관련 API 문서 확인
 > 영상 수정 요청을 생성/조회/준비(prepare)하는 API 연동 로직과 관련 데이터 타입 추가 (화면은 아직 없음)
 > 구현한 내용이 정상 동작하는지 테스트 코드로 확인

- 영상 신청 상세 화면 구축
 > 영상 신청 건 하나를 클릭하면 볼 수 있는 상세 화면 신규 제작
 > 실제 API 데이터를 불러오고, 상태 값을 일정 주기(polling)로 자동 확인하도록 구현
 > API로 받아온 데이터를 화면에 보기 좋은 형태로 변환하는 작업(mapper) 진행
 > 상세 화면 진입 시 메뉴(사이드바)와 경로 표시(브레드크럼)가 기존 화면들과 자연스럽게 이어지도록 보정
 > 완성된 영상을 상세 화면에서 바로 재생할 수 있도록 연결

- 영상 목록(라이브러리) 화면 실제 API 연동
 > 그동안 임시로 넣어둔 샘플 데이터를 제거하고 실제 영상 신청 목록 API로 교체
 > API 데이터를 목록 화면에 맞게 변환하는 작업(mapper) 추가
 > 목록에서 항목을 선택하면 상세 화면으로 이동하는 기능 추가
 > 상태 값이 바뀔 수 있으므로 10초 간격으로 목록이 자동 갱신(polling)되도록 설정

- 영상 만들기 화면을 단계별로 재구성
 > 백엔드에서 내려오는 상태 값을 화면 단계로 변환하는 규칙(rule)을 작성
 > 분석 중 / 검토 필요 / 컴파일 중 / 실패 / 완료 / 시나리오 확인 / 이미지 확인 등 각 진행 단계를 각각의 화면으로 분리해서 제작
 > 시나리오 확인 단계: 생성된 시나리오를 확인하고, 수정 요청 시 입력값을 검증한 뒤 확인(confirm)/수정(revise) API로 전달
 > 이미지 확인 단계: 생성된 이미지를 승인(approve)하거나 재생성(regenerate) 요청하는 API 연동 (이미지 미리보기는 관련 API가 아직 없어 다음 기회로 보류)
 > 마지막 확인 단계: 최종 영상 생성을 요청하는 제출(submit) API 연동
 > 완료 단계: 완료 안내와 함께 최종 영상을 바로 재생할 수 있는 화면 연결
 > 더 이상 쓰지 않는 임시 화면 코드를 제거하고, 단계별로 명확하게 나뉘도록 화면 코드 정리
 > 관련 코드(입력 폼, API 처리, 데이터 변환 로직 등)를 단계별 폴더로 이동해 이후 유지보수가 쉽도록 개선
 > 영상 생성 요청 후 곧바로 상세 화면으로 넘어가던 로직을 제거하고, 단계별 화면 안에서 자연스럽게 이어지도록 변경


[결과 / 현재 상태]
- 다른 프로젝트 문서를 정리하고 현재 API 기준으로 향후 화면 개발 계획 문서를 작성했으며, 개발 중 발견된 알림 메시지 처리 방식, 페이지 번호(pagination) 처리, 데이터 변환(mapper) 관련 이슈는 모두 원인을 찾아 해결함
- 영상 수정 요청 기능은 API 연동 부분까지만 우선 반영하고 화면 작업은 상세 설계 이후 진행하기로 했으며, 영상 상세·목록 화면은 신청 건(ID)과 API 데이터를 기준으로 동작하는 구조를 유지하기로 결정함

[이슈 / 블로커]
- 없음

[내일 할 일 (2026.09.15)]
- 영상 목록 또는 상세 화면 중 어디에 영상 수정 요청 기능을 넣을지 위치 결정
- 영상 수정 화면을 만들 경우, 구간/프레임(segment/frame) 선택, 수정 지시문 입력, 수정 요청(revision) 생성, 준비(prepare) 상태 표시까지만 우선 설계
- 영상 만들기 화면에 아직 남아있는 입력 폼, API 처리, 데이터 변환 관련 코드를 각 단계별 폴더로 마저 이동
- 영상 목록 화면의 자동 갱신(polling) 방식을 상태별로 더 효율적으로 개선
```