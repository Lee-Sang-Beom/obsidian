#### Work (프로젝트: 딸깍)
---
- 캐릭터 스토리 제작 요청 코드 동작의 사용자 동작 및 반응 흐름 내 상세코드 분석
	- 스크립트 생성(로딩) 과정부터의 로직 분석 (`ScriptGenerationPreview`)
		- 실패 케이스를 제외한 3개의 STEP
		- 3가지 케이스를 기반으로 한 콘텐츠 영역 렌더링
		- 1.2초마다 현재 컷 인덱스 조회 (나레이션)
		  
    - 캐릭터 생성 로직 (`character-story-service.ts`)
        - `createUserCharacter`에서 `getCharacterCreationPrompt`로 스타일별 프롬프트 생성
        - `runFallbackChain`으로 AI 엔진 순차 시도 (`fal-gpt-image-i2i` → `fal-seedream-5-lite-i2i`)
        - `withTimeout`으로 엔진별 타임아웃 제한, 성공 시 즉시 반환 / 전부 실패 시 `AllEnginesFailed` 반환
        - AI 생성 이미지 URL을 `user_characters` 테이블에 INSERT 후 `{ id, imageUrl }` 반환
    - 프로젝트 생성 로직 (`production-repository.ts`)
        - `createProject`에서 CTE SQL 한 방으로 `projects` 테이블 INSERT + `cuts` 테이블 다중 INSERT 원자적 처리
        - `cuts`는 프로젝트 생성 시점에 `id`/`project_id`/`index`만 INSERT, 나머지 컬럼은 이후 생성 단계에서 순차적으로 채워짐
        - `sql.join`으로 컷 수만큼의 INSERT 값 배열을 콤마로 이어붙여 단일 SQL로 처리
    - 스크립트 에디터 컴포넌트 (`cs-script-editor.tsx`)
        - 페이지 진입 시 스크립트 자동 생성 → 완료 후 콘텐츠 생성 자동 시작 흐름 파악
        - 각 단계별 `useEffect` 트리거 조건 및 중복 실행 방지 로직 분석
        - 크레딧 부족 / 생성 실패 시 재시도 처리 흐름 분석
    - 스크립트 생성 로직 (`character-story-service.ts`)
        - `generateScript` 함수 내 AI 지시문 조립 → 스크립트 생성 → 컷 저장 → 상태 변경 흐름 분석
        - `saveScriptResult` CTE SQL 구조 분석 (스크립트 INSERT + 컷 일괄 UPDATE 원자적 처리)
    - 콘텐츠 생성 시작 로직 (`character-story-service.ts`)
        - `startProduction` 함수 내 비용 견적 → 크레딧 잡 생성 → Inngest 이벤트 발송 흐름 분석
        - Inngest를 통한 백그라운드 AI 생성 작업 위임 구조 파악
    - 콘텐츠 생성 진행 상황 모니터링 페이지 (`cs-generate-page.tsx`)
        - `useQuery`의 `refetchInterval`을 활용하여 프로젝트가 최종 상태(`completed`, `failed` 등)에 도달할 때까지 2초 간격으로 상태를 실시간 폴링
        - 폴링된 전체 데이터를 `ProgressOverall`, `CSCutResultCard` 등 하위 UI 컴포넌트로 전달하여 화면 전체를 동기화
    - 프로젝트 상태 조회 로직 (`production-action.ts` 내 `getProjectStatus`)
        - `getProjectWithCuts` 쿼리를 호출하여 DB에서 프로젝트 정보와 연관 컷 목록을 한 번에 조회하고 유저 소유권을 검증
        - UI 렌더링에 최적화되도록 원본 DB 데이터를 각 컷의 상태, 미디어 URL(이미지, 영상, TTS), 메타데이터 포맷으로 정제하여 클라이언트에 반환
    - 전체 진행률 표시 컴포넌트 (`progress-overall.tsx`)
        - 전체 컷 수 대비 완료 및 실패(`completed`, `failed`) 컷의 비율을 계산하여 막대그래프(Progress Bar)로 시각화
        - 미디어 유형별(TTS, Image, Video) 완료 건수와 서버의 Inngest 배치(Batch) 단위 처리 진행 상황을 계산하여 상세 정보 제공
    - 개별 컷 결과 표시 및 상호작용 컴포넌트 (`cs-cut-result-card.tsx`)
        - 각 컷의 생성 결과(미리보기 이미지, 영상)를 카드로 렌더링하고 다중 비디오 재생 시 오디오 충돌을 방지 (`globalVideoManager` 활용)
        - `useMutation`을 통해 배경 재생성, 장면 이미지 재생성, 영상 재생성 등의 컷별 서버 액션을 개별적으로 호출
        - 컷 수정 서버 작업이 완료(`onSettled`)되면 `queryClient.invalidateQueries`를 호출해 부모 페이지의 전체 데이터 갱신을 트리거

#### Work (프로젝트: Ascolta)
---
- 게스트 로그인 기능 구현
	- 게스트 로그인 버튼 UI 추가
	- 별도 게스트 로그인용 type 정의, API를 사용하여 게스트 로그인 진행
	- 기존 로그인과 동일한 형태로 코드흐름 작성
	- 로그인된 유저의 ROLE(역할) 추가: ROLE_GUEST