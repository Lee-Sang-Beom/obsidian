
# 프로젝트 : 82ALDA Podcast Service
## 📋 주요 변경사항

### 1. Container/Presenter 패턴 적용

- [x] **signal-card.tsx**
    - [x] Container로 유지, `signal-card-presenter.tsx` Presenter 생성
    - [x] 데이터 로직(훅 호출, 상태 관리) Container에서 담당
    - [x] 순수 UI 렌더링은 Presenter에서 담당
    - [x] `forwardRef`를 사용하여 ref 전달 처리
- [x] **signal-profit-card.tsx**
    - [x] Container로 유지, `signal-profit-card-presenter.tsx` Presenter 생성
    - [x] `useSignalCardData` 훅 사용으로 실시간 데이터 관리
    - [x] 즐겨찾기 토글에 3초 딜레이 적용 (기존 기능 유지)
    - [x] InView threshold 0.5로 설정 (기존 기능 유지)
- [x] **signal-favorite-card.tsx**
    - [x] Container로 유지, `signal-favorite-card-presenter.tsx` Presenter 생성
    - [x] 즐겨찾기 페이지에서는 항상 `isFavorite={true}` 처리 (기존 로직 유지)
    - [x] 즐겨찾기 토글에 3초 딜레이 적용 (기존 기능 유지)
    - [x] InView threshold 0.5로 설정 (기존 기능 유지)
- [x] **stock-info-header.tsx**
    - [x] Container로 유지, `stock-info-header-presenter.tsx` Presenter 생성
    - [x] `shouldShowFavoriteStar` 계산 로직을 Container에서 처리
    - [x] Presenter는 계산된 값만 받아서 렌더링
    - [x] 좌우 영역을 명확히 구분하여 Presenter에 전달

### 2. 실시간 데이터 관리 통합

- [x] **use-signal-card-data.ts 커스텀 훅 신규 생성**
    - [x] `useGetLiveStockInfo`와 `usePercentageCalculation` 로직을 하나의 훅으로 통합
    - [x] 모든 카드 컴포넌트(signal-card, signal-profit-card, signal-favorite-card)에서 동일한 훅 사용
    - [x] 실시간 주식 정보 조회 로직 중앙화
    - [x] 등락률 계산 로직 중앙화
    - [x] 향후 최고가 갱신 로직 추가를 위한 확장 포인트 마련

### 3. 발굴 후 상승률 계산 로직 개선

- [x] **use-post-discovery-increase.ts 커스텀 훅 신규 생성**
    - [x] `calculatePostDiscoveryIncrease` 함수 호출을 커스텀 훅으로 캡슐화
    - [x] `useMemo`를 활용한 계산 결과 메모이제이션
    - [x] `stock-name-section.tsx`에서 커스텀 훅 사용
    - [x] 복잡한 계산 로직을 컴포넌트에서 분리

### 4. 타입 정의 파일 분리

- [x] **signal-card/types.ts 신규 생성**
    - [x] `ChartType`, `LiveStockInfo`, `SignalListItemResponse` 등 공통 타입 정의
    - [x] `StockPriceData`, `SignalCardData` 인터페이스 추가
    - [x] 타입 재사용성 향상 및 타입 안정성 강화

### 5. stock-name-section.tsx 개선

- [x] **로직 최적화**
    - [x] 시그널 시간 체크 로직을 `useMemo`로 메모이제이션
    - [x] `usePostDiscoveryIncrease` 훅 사용으로 상승률 계산 로직 분리
    - [x] 표시할 종목명 결정 로직을 `useMemo`로 메모이제이션
    - [x] 복잡한 조건부 로직을 명확한 변수명으로 개선
    - [x] `getDisplayCodeName`, `getIncreaseDisplay`, `getMaxProfitDisplay` 함수 제거 (useMemo로 대체)
    - [x] 코드 가독성 및 성능 최적화

### 6. price-info-section.tsx 개선

- [x] **컴포넌트 분리**
    - [x] `PremiumBadge` 컴포넌트를 별도 함수 컴포넌트로 분리
    - [x] 조기 반환(early return) 패턴 적용으로 가독성 향상
    - [x] 프리미엄 배지와 가격 정보 표시 로직 명확히 분리
    - [x] 불필요한 중첩 제거

### 7. signal-info-section.tsx 개선

- [x] **컴포넌트 분리 및 로직 개선**
    - [x] 발굴 가격대 계산 로직을 `useMemo`로 메모이제이션
    - [x] `SignalInfoLeft` 컴포넌트 분리 (시그널 배지 + 전략명)
    - [x] `SignalInfoRight` 컴포넌트 분리 (발굴 가격대)
    - [x] `calculatePriceRange` 순수 함수로 가격대 계산 로직 분리
    - [x] KRW/USD 통화별 가격 처리 로직 개선
    - [x] 컴포넌트 책임 분리로 재사용성 향상

### 8. signal-card.tsx 구조 개선

- [x] **코드 구조 개선**
    - [x] 주석으로 섹션 구분 (Local State, InView Detection, Signal Data Extraction 등)
    - [x] `useSignalCardData` 훅으로 실시간 데이터 관리 통합
    - [x] 차트 토글 핸들러를 간결한 형태로 개선 (`prev => prev === 'X' ? null : 'X'`)
    - [x] Presenter에 필요한 props만 전달하도록 정리
    - [x] 코드 가독성 향상

### 9. signal-page.tsx 구조 개선

- [x] **컴포넌트 구조 개선**
    - [x] 페이지 컴포넌트를 Container로 역할 명확화
    - [x] `TabTrigger` 컴포넌트 분리 (탭 버튼 렌더링 담당)
    - [x] `TabContent` 컴포넌트 분리 (탭 콘텐츠 렌더링 담당)
    - [x] 각 컴포넌트가 단일 책임 원칙(SRP) 준수
    - [x] 인터페이스를 명확하게 정의하여 타입 안정성 강화
- [x] **탭 관리 로직 개선**
    - [x] `useTabManager` 커스텀 훅 사용으로 탭 상태 관리 분리
    - [x] `TAB_CONFIGS` 상수로 탭 설정 중앙화
    - [x] `activeTab`, `handleTabChange` 로직을 훅으로 캡슐화
    - [x] 멤버십 타입에 따른 탭 활성화 로직 통합
- [x] **TabTrigger 컴포넌트**
    - [x] `TabTriggerProps` 인터페이스 정의
    - [x] `config`, `activeTab`, `isFreeUser`를 props로 받음
    - [x] `isDisabled` 계산 로직 컴포넌트 내부에서 처리
    - [x] `isFavoriteTab` 판별 로직으로 즐겨찾기 탭 스타일 분리
    - [x] Lock 아이콘 조건부 렌더링
    - [x] 반응형 라벨 표시 (full/short)
- [x] **TabContent 컴포넌트**
    - [x] `TabContentProps` 인터페이스 정의
    - [x] `config`, `activeTab`, `membershipType`을 props로 받음
    - [x] `isActive` 계산 로직 컴포넌트 내부에서 처리
    - [x] 즐겨찾기 탭과 일반 시그널 탭 분기 처리
    - [x] `FavoriteSignalList` 또는 `SignalList` 렌더링
    - [x] `key` prop으로 탭 변경 시 컴포넌트 재생성

### 10. signal-list.tsx 구조 개선

- [x] **컴포넌트 구조 개선**
    - [x] 메인 컴포넌트를 Container로 역할 명확화
    - [x] `SignalListContent` 컴포넌트 분리 (리스트 렌더링 담당)
    - [x] `SignalCardItem` 컴포넌트 분리 (개별 카드 렌더링 담당)
    - [x] 각 컴포넌트가 단일 책임 원칙(SRP) 준수
- [x] **커스텀 훅 활용**
    - [x] `useSignalFilter` 훅으로 필터 상태 관리 분리
    - [x] `useSignalListData` 훅으로 데이터 페칭 로직 분리
    - [x] `useScreenViewLogger` 훅으로 스크린뷰 로깅 분리
    - [x] `useInfiniteScroll` 훅으로 무한 스크롤 로직 분리
    - [x] 복잡한 로직을 컴포넌트에서 제거하여 가독성 향상
- [x] **SignalListContent 컴포넌트**
    - [x] `SignalListContentProps` 인터페이스 정의
    - [x] 로딩/에러/빈 데이터 상태 처리 로직 통합
    - [x] `MobileLoadingState` 컴포넌트로 각 상태 표시
    - [x] 네트워크 에러 체크 로직 (`isNetworkError`)
    - [x] FREE 사용자와 일반 사용자 분기 처리
    - [x] `displayedSignals` 계산 (FREE는 3개만 표시)
    - [x] `UpgradeMembershipButton` 조건부 렌더링
- [x] **SignalCardItem 컴포넌트**
    - [x] `SignalCardItemProps` 인터페이스 정의
    - [x] 상승률 카드 렌더링 분기 처리
    - [x] `isTermProfit && isSignalFavoriteListResponse` 조건으로 판별
    - [x] `SignalProfitCard` 렌더링
    - [x] 일반 시그널 카드 렌더링 분기 처리
    - [x] `isAvailableDisplaySignalInfo` 계산 후 `SignalCard`에 전달
    - [x] 타입 캐스팅 명확화
- [x] **무한 스크롤 처리**
    - [x] FREE 사용자는 무한 스크롤 비활성화
    - [x] `hasNextPage` 조건에 `!isFreeUser` 추가
    - [x] `loadMoreRef` 조건부 렌더링
    - [x] `InfiniteScrollLoading` 컴포넌트 사용
    - [x] `InfiniteScrollComplete` 컴포넌트 사용
- [x] **Pull to Refresh 기능**
    - [x] `PullToRefresh` 컴포넌트 래핑
    - [x] `handleRefresh` 콜백으로 refetch 처리
    - [x] `CustomPullingContent`, `CustomRefreshingContent` 사용
    - [x] resistance와 pullDownThreshold 설정

### 11. favorite-signal-list.tsx 구조 개선

- [x] **컴포넌트 구조 개선**
    - [x] 메인 컴포넌트를 Container로 역할 명확화
    - [x] `FavoriteSignalListContent` 컴포넌트 분리 (리스트 렌더링 담당)
    - [x] 각 컴포넌트가 단일 책임 원칙(SRP) 준수
- [x] **데이터 페칭 로직**
    - [x] `useInfiniteFavoriteSignals` 훅으로 데이터 조회
    - [x] `pageSize: 10` 설정
    - [x] `enabled: isActive` 조건으로 활성 탭일 때만 조회
    - [x] `refetch` 함수로 새로고침 처리
- [x] **FavoriteSignalListContent 컴포넌트**
    - [x] `FavoriteSignalListContentProps` 인터페이스 정의
    - [x] 로딩/에러/빈 데이터 상태 처리 로직 통합
    - [x] `MobileLoadingState` 컴포넌트로 각 상태 표시
    - [x] 시그널 리스트 map으로 렌더링
    - [x] `SignalFavoriteCard` 사용
    - [x] 무한 스크롤 영역 처리
    - [x] `InfiniteScrollLoading`, `InfiniteScrollComplete` 컴포넌트 사용
- [x] **FREE 멤버십 처리**
    - [x] FREE 사용자는 `UpgradeMembershipButton`만 표시
    - [x] early return으로 조기 반환 처리
    - [x] 명확한 분기 처리
- [x] **Pull to Refresh 기능**
    - [x] `PullToRefresh` 컴포넌트 래핑
    - [x] `handleRefresh` 콜백으로 refetch 처리
    - [x] `useCallback`으로 핸들러 메모이제이션
    - [x] `CustomPullingContent`, `CustomRefreshingContent` 사용

### 12. 타입 안정성 강화

- [x] **signal-list.tsx**
    - [x] `SignalListProps` 인터페이스 정의
    - [x] `SignalListContentProps` 인터페이스 정의
    - [x] `SignalCardItemProps` 인터페이스 정의
    - [x] props 타입 명확히 지정
    - [x] `any` 타입 최소화 (signals는 추후 개선 가능)
- [x] **favorite-signal-list.tsx**
    - [x] `FavoriteSignalListProps` 인터페이스 정의
    - [x] `FavoriteSignalListContentProps` 인터페이스 정의
    - [x] `SignalFavoriteListResponse` 타입 명시

### 13. 코드 가독성 향상

- [x] **전반적인 개선**
    - [x] 컴포넌트별로 명확한 주석 추가
    - [x] Props 인터페이스를 각 컴포넌트 위에 배치
    - [x] 조건부 로직을 명확한 변수명으로 개선
    - [x] map 함수 사용으로 반복 코드 제거
    - [x] 섹션별로 구분선 추가 (signal-list.tsx)
    - [x] 중첩 depth 감소
    - [x] early return 패턴으로 조건 분기 명확화

---

## 🔄 변경되지 않은 사항 (기존 동작 유지)

### 기존 훅 사용

- [x] `useGetLiveStockInfo` 훅 동일하게 사용
- [x] `usePercentageCalculation` 훅 동일하게 사용
- [x] `useFavoriteToggle` 훅 동일하게 사용
- [x] `useInView` 훅 동일하게 사용 (threshold 값 포함)
- [x] `useThemeSignalCountChartData` 훅 동일하게 사용
- [x] `useGetUserInfo` 훅 동일하게 사용
- [x] `useLogoutHandler` 훅 동일하게 사용
- [x] `useOpenMembershipDialog` 훅 동일하게 사용
- [x] `useInfiniteFavoriteSignals` 훅 동일하게 사용

### 기존 계산 로직

- [x] `calculatePostDiscoveryIncrease` 함수 동일하게 사용
- [x] `maskCodeName` 함수 동일하게 사용
- [x] `roundToKrxTick` 함수 동일하게 사용
- [x] `formatPrice` 함수 동일하게 사용
- [x] `getMembershipType` 유틸 함수 동일하게 사용
- [x] 1시간 기준 시그널 시간 체크 로직 동일
- [x] `isAvailableDisplaySignalInfo` 판별 로직 동일
- [x] `isSignalFavoriteListResponse` 타입 가드 동일
- [x] `isNetworkError` 체크 로직 동일

### 기존 UI 컴포넌트

- [x] `Card`, `CardContent` 동일하게 사용
- [x] `FavoriteStar` 컴포넌트 동일하게 사용
- [x] `SignalBadges` 컴포넌트 동일하게 사용
- [x] `PriceDisplay` 컴포넌트 동일하게 사용
- [x] `ChartToggle` 컴포넌트 동일하게 사용
- [x] `ChartDisplay` 컴포넌트 동일하게 사용
- [x] `ThemeSignalCountChartSection` 컴포넌트 동일하게 사용
- [x] `SignalProfitChartSection` 컴포넌트 동일하게 사용
- [x] `FavoriteSignalChartSection` 컴포넌트 동일하게 사용
- [x] `SignalHeader` 컴포넌트 동일하게 사용
- [x] `TopGainersSection` 컴포넌트 동일하게 사용
- [x] `SubSearchFilter` 컴포넌트 동일하게 사용
- [x] `MobileLoadingState` 컴포넌트 동일하게 사용
- [x] `InfiniteScrollLoading` 컴포넌트 동일하게 사용
- [x] `InfiniteScrollComplete` 컴포넌트 동일하게 사용
- [x] `UpgradeMembershipButton` 컴포넌트 동일하게 사용
- [x] `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` 동일하게 사용


---

## 📁 신규 생성 파일 (컨테이너, 표현컴포넌트 중심 및 유틸훅)

- [x] `_components/signal-card/signal-card-presenter.tsx`
- [x] `_components/signal-card/types.ts`
- [x] `_components/normal-signal/signal-profit-card-presenter.tsx`
- [x] `_components/favorite-signal/signal-favorite-card-presenter.tsx`
- [x] `_components/signal-part/header/stock-info-header-presenter.tsx`
- [x] `hooks/use-signal-card-data.ts`
- [x] `hooks/use-post-discovery-increase.ts`

---

## 📝 수정된 파일

### Card 컴포넌트

- [x] `_components/normal-signal/signal-card.tsx`
- [x] `_components/normal-signal/signal-profit-card.tsx`
- [x] `_components/favorite-signal/signal-favorite-card.tsx`

### Header 컴포넌트

- [x] `_components/signal-part/header/stock-info-header.tsx`
- [x] `_components/signal-part/header/stock-name-section.tsx`
- [x] `_components/signal-part/header/price-info-section.tsx`

### 기타 컴포넌트

- [x] `_components/signal-part/signal-info-section.tsx`

### 페이지 및 리스트

- [x] `signal-page.tsx`
- [x] `_components/normal-signal/signal-list.tsx`
- [x] `_components/favorite-signal/favorite-signal-list.tsx`
