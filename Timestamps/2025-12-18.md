#### Work
---
- [x] 테이블 컴포넌트 빈 데이터 예외 UI 추가
	- [x] DefaultRingPresenter 수정
	    - `TableCell` import 추가
	    - `items.length === 0` 조건 추가하여 빈 데이터 체크
	    - 빈 데이터일 때 `TableCell`에 `colSpan={3}` 적용하여 3개 컬럼 병합
	    - 빈 데이터 메시지: "데이터가 없습니다"
	    - 스타일: `text-muted-foreground py-3 text-center` 적용
	    - 기존 `handleAudioClick` 로직 유지
	    - 주석 처리된 audioUrl, onClick 코드 모두 유지
	- [x] CampaignRingPresenter 수정
	    - `TableCell` import 추가
	    - `columnCount` 변수 추가: `isNewProductType`에 따라 5 또는 6으로 설정
	    - `items.length === 0` 조건 추가하여 빈 데이터 체크
	    - 빈 데이터일 때 `TableCell`에 `colSpan={columnCount}` 적용하여 동적 컬럼 병합
	    - 빈 데이터 메시지: "데이터가 없습니다"
	    - 스타일: `text-muted-foreground py-3 text-center` 적용
	    - 기존 `handleAudioClick` 로직 유지
	    - 주석 처리된 audioUrl, onClick 코드 모두 유지
	    - `isNewProductType`에 따른 시간대 컬럼 조건부 렌더링 로직 유지
	- [x] SpecificDateRingPresenter 수정
	    - `TableCell` import 추가
	    - `items.length === 0` 조건 추가하여 빈 데이터 체크
	    - 빈 데이터일 때 `TableCell`에 `colSpan={4}` 적용하여 4개 컬럼 병합 (No., 설정일, 설정음원, 듣기)
	    - 빈 데이터 메시지: "데이터가 없습니다"
	    - 스타일: `text-muted-foreground py-3 text-center` 적용
	    - 기존 `handleAudioClick` 로직 유지
	    - `formatDate` 함수 사용 로직 유지
	    - `DELETE_ACTION` 체크 및 스타일 적용 로직 유지
	- [x] WeeklyRingStandardPresenter 수정
	    - `items.length === 0` 조건 추가하여 빈 데이터 체크
	    - 빈 데이터일 때 `TableCell`에 `colSpan={6}` 적용하여 6개 컬럼 병합 (No., 시작요일, 종료요일, 시간대, 설정음원, 듣기)
	    - 빈 데이터 메시지: "데이터가 없습니다"
	    - 스타일: `text-muted-foreground py-3 text-center` 적용
	    - 기존 `handleAudioClick` 로직 유지
	    - 주석 처리된 audioUrl, onClick 코드 모두 유지
	    - `toTime` 함수 사용 로직 유지
	    - `DELETE_ACTION` 체크 및 스타일 적용 로직 유지
	- [x] WeeklyRingGroupedPresenter 수정
	    - `items.length === 0` 조건 추가하여 빈 데이터 체크
	    - 빈 데이터일 때 `TableCell`에 `colSpan={4}` 적용하여 4개 컬럼 병합 (요일, 시간대, 설정음원, 듣기)
	    - 빈 데이터 메시지: "데이터가 없습니다"
	    - 스타일: `text-muted-foreground py-3 text-center` 적용
	    - `TableBody` 내부에서 `items.length === 0` 체크 후 빈 메시지 또는 `rows` 렌더링
	    - 기존 `handleAudioClick` 로직 유지
	    - 기존 `groupByWeek` 함수 로직 유지
	    - 주석 처리된 audioUrl, onClick 코드 모두 유지
	    - `rowSpan`을 사용한 요일별 그룹핑 로직 유지
	    - `DELETE_ACTION` 체크 및 스타일 적용 로직 유지

- [x] approve-types.ts 수정
	- [x] BaseTableProps 타입 수정
		- 기존: previewAudioSrc: string만 존재
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
		- 이 타입을 extends하는 모든 타입(DefaultRingProps, CampaignRingProps, SpecificDateRingProps, WeeklyRingProps)에 자동 적용됨

- [x] settings-form-container.tsx 수정
	- [x] pendingAudio state 추가
		- 타입: { key: string; ver: string } | null
		- 용도: preview 로드 후 재생할 오디오 정보를 임시 저장
	- [x] previewSettings useEffect 수정
		- 기존: previewSettings 로드 시 무조건 'preview' 키로 자동 재생
		- 변경: pendingAudio가 있으면 해당 오디오 재생, 없으면 기본 'preview' 재생
		- pendingAudio가 있을 때: onToggle(pendingAudio.key, `${previewSettings.audioUrl}&ver=${pendingAudio.ver}`) 호출 후 setPendingAudio(null)
		- pendingAudio가 없고 audioState도 없을 때: onToggle('preview', `${previewSettings.audioUrl}&ver=preview`) 호출
	- [x] previewError useEffect 수정
		- 에러 발생 시 setPendingAudio(null) 추가하여 대기 중인 오디오 정보 초기화
	- [x] handleRingAudioPlay 핸들러 함수 추가
		- 매개변수: (key: string, ver: string)
		- previewSettings가 없으면: setPendingAudio({ key, ver }) 후 onLoadPreview() 호출
		- previewSettings가 있으면: onToggle(key, `${previewSettings.audioUrl}&ver=${ver}`) 바로 호출
	- [x] SettingsFormPresenter에 onRingAudioPlay prop 전달 추가

- [x] settings-form-presenter.tsx 수정
	- [x] SettingsFormPresenterProps 인터페이스 수정
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] SettingsFormRing 컴포넌트에 onRingAudioPlay prop 전달
		- 기존: onLoadPreview 전달
		- 변경: onRingAudioPlay 전달
		- 위치: CardContent 내부의 Tabs > TabsContent > SettingsFormRing

- [x] settings-form-ring.tsx 수정
	- [x] SettingFormRingProps 타입 수정
		- 기존: onLoadPreview: () => void
		- 변경: onRingAudioPlay: (key: string, ver: string) => void
	- [x] 함수 매개변수 수정
		- 기존: onLoadPreview
		- 변경: onRingAudioPlay
	- [x] 모든 Container 컴포넌트에 onRingAudioPlay prop 전달
		- DefaultRingContainer에 전달
		- SpecificDateRingContainer에 전달
		- CampaignRingContainer에 전달
		- WeeklyRingContainer에 전달

- [x] default-ring-container.tsx 수정
	- [x] DefaultRingProps 타입은 BaseTableProps를 extends하므로 자동으로 onRingAudioPlay 포함됨
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] 두 개의 DefaultRingPresenter에 모두 onRingAudioPlay prop 전달
		- "기존설정" Presenter에 전달
		- "추가설정" Presenter에 전달

- [x] default-ring-presenter.tsx 수정
	- [x] DefaultRingPresenterProps 타입에 onRingAudioPlay 추가
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] handleAudioClick 핸들러 함수 추가
		- 매개변수: (key: string, mid: string, index: number)
		- isProduction === true (추가설정)인 경우: onRingAudioPlay(key, key) 호출
		- isProduction === false (기존설정)인 경우: onAudioToggle(key, `${audioBaseUrl}?mid=${mid}`) 호출
	- [x] AudioPlayButton의 onClick 수정
		- 기존: () => onAudioToggle(key, audioUrl)
		- 변경: () => handleAudioClick(key, item.mid, index)
	- [x] audioUrl 변수 선언 제거 (더 이상 사용하지 않음)

- [x] specific-date-ring-container.tsx 수정
	- [x] SpecificDateRingProps 타입은 BaseTableProps를 extends하므로 자동으로 onRingAudioPlay 포함됨
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] 두 개의 SpecificDateRingPresenter에 모두 onRingAudioPlay prop 전달
		- "기존설정" Presenter에 전달
		- "추가설정" Presenter에 전달

- [x] specific-date-ring-presenter.tsx 수정
	- [x] SpecificDateRingPresenterProps 타입에 onRingAudioPlay 추가
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] handleAudioClick 핸들러 함수 추가
		- 매개변수: (key: string, mid: string, index: number)
		- isProduction === true인 경우: onRingAudioPlay(key, key) 호출
		- isProduction === false인 경우: onAudioToggle(key, `${audioBaseUrl}?mid=${mid}`) 호출
	- [x] AudioPlayButton의 onClick 수정
		- 기존: () => onAudioToggle(key, audioUrl)
		- 변경: () => handleAudioClick(key, item.mid, index)
	- [x] audioUrl 변수 선언 제거

- [x] campaign-ring-container.tsx 수정
	- [x] CampaignRingProps 타입은 BaseTableProps를 extends하므로 자동으로 onRingAudioPlay 포함됨
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] 두 개의 CampaignRingPresenter에 모두 onRingAudioPlay prop 전달
		- "기존설정" Presenter에 전달
		- "추가설정" Presenter에 전달

- [x] campaign-ring-presenter.tsx 수정
	- [x] CampaignRingPresenterProps 타입에 onRingAudioPlay 추가
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] handleAudioClick 핸들러 함수 추가
		- 매개변수: (key: string, mid: string, index: number)
		- isProduction === true인 경우: onRingAudioPlay(key, key) 호출
		- isProduction === false인 경우: onAudioToggle(key, `${audioBaseUrl}?mid=${mid}`) 호출
	- [x] AudioPlayButton의 onClick 수정
		- 기존: () => onAudioToggle(key, audioUrl)
		- 변경: () => handleAudioClick(key, item.mid, index)
	- [x] audioUrl 변수 선언 제거

- [x] weekly-ring-container.tsx 수정
	- [x] WeeklyRingProps 타입은 BaseTableProps를 extends하므로 자동으로 onRingAudioPlay 포함됨
	- [x] 함수 매개변수에 onRingAudioPlay 추가
	- [x] 두 개의 TableComponent(Presenter)에 모두 onRingAudioPlay prop 전달
		- "기존설정" TableComponent에 전달
		- "추가설정" TableComponent에 전달

- [x] weekly-ring-presenter.tsx 수정
	- [x] WeeklyRingStandardPresenterProps 타입에 onRingAudioPlay 추가
		- 추가: onRingAudioPlay: (key: string, ver: string) => void
	- [x] WeeklyRingStandardPresenter 함수 수정
		- 함수 매개변수에 onRingAudioPlay 추가
		- handleAudioClick 핸들러 함수 추가
			- 매개변수: (key: string, mid: string, index: number)
			- isProduction === true인 경우: onRingAudioPlay(key, key) 호출
			- isProduction === false인 경우: onAudioToggle(key, `${audioBaseUrl}?mid=${mid}`) 호출
		- AudioPlayButton의 onClick 수정
			- 기존: () => onAudioToggle(key, audioUrl)
			- 변경: () => handleAudioClick(key, item.mid, index)
		- audioUrl 변수 선언 제거
	- [x] WeeklyRingGroupedPresenter 함수 수정
		- 함수 매개변수에 onRingAudioPlay 추가
		- handleAudioClick 핸들러 함수 추가
			- 매개변수: (key: string, mid: string)
			- isProduction === true인 경우: onRingAudioPlay(key, key) 호출
			- isProduction === false인 경우: onAudioToggle(key, `${audioBaseUrl}?mid=${mid}`) 호출
		- AudioPlayButton의 onClick 수정
			- 기존: () => onAudioToggle(key, audioUrl)
			- 변경: () => handleAudioClick(key, item.mid)
		- audioUrl 변수 선언 제거

- [x] 동작 흐름 정리
	- [x] 사용자가 추가설정의 오디오 재생 버튼 클릭
		- handleAudioClick 실행 → onRingAudioPlay(key, key) 호출
	- [x] SettingsFormContainer의 handleRingAudioPlay 실행
		- previewSettings가 없으면: setPendingAudio({ key, ver }) + onLoadPreview() 호출
		- previewSettings가 있으면: 즉시 onToggle(key, url) 호출하여 재생
	- [x] previewSettings가 로드되면 useEffect 실행
		- pendingAudio가 있으면: 저장된 key와 ver로 오디오 재생 + pendingAudio 초기화
		- pendingAudio가 없고 audioState도 없으면: 기본 'preview' 키로 재생
	- [x] 최종 결과
		- 클릭한 버튼의 key로 active 상태 관리됨
		- 클릭한 버튼에 해당하는 오디오가 정확히 재생됨
		- 멘트 버튼('preview' key)과 링 설정 버튼들의 active 상태가 독립적으로 관리됨