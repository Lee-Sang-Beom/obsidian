#### Work (프로젝트: AI 검증코어 프론트엔드)
---
- [x] Hosted UI 대체방법 탐색 및 결정
	- Hosted UI가 대신 처리해주는 보안/예외 케이스가 많아서, 단순히 Change email 같은 몇 개 문구 때문에 전체 Custom Auth UI로 가는 방법은 비용이 큼
```
> 직접 구현이 필요한 부분

이메일 입력
비밀번호 로그인
회원가입
이메일 검증
비밀번호 재설정
Google OAuth 시작
callback 처리
에러 처리
세션 저장
보안 처리
```

- [x] 전역 커스텀 스타일 파일 분리
  - `src/app/globals.css`에 섞여 있던 커스텀 유틸리티 성격의 스타일을 별도 파일로 분리
  - 기본 전역 스타일, shadcn-ui 관련 설정과 프로젝트 커스텀 스타일의 책임 구분

- [x] 앱 전역 Provider 구조 정리
  - `src/components/providers` 하위에 있던 앱 전역 provider를 `src/providers`로 이동
  - 컴포넌트 디렉터리는 UI 컴포넌트 중심으로 유지하고, provider는 앱 조립 책임으로 분리

- [x] 랜딩 전용 컴포넌트 구조 정리
  - `src/components` 하위에 있던 랜딩 전용 컴포넌트를 `src/features/landing/components` 하위로 이동
  - 공통 컴포넌트와 특정 feature 전용 컴포넌트의 경계 정리
    
- [x] 인증 및 로딩 구조 정리
  - WorkOS AuthKit 로그인/회원가입 route 공통 처리 로직 분리
  - 추가 파일
    - `src/lib/auth/workos-hosted-ui-route.ts`

  - `sign-in`, `sign-up` route에서 중복된 WorkOS redirect 처리 제거
  - stale PKCE cookie 정리 로직을 공통화
  - 인증 route 응답에 `no-store` cache header 적용

  - 공통 로딩/에러 컴포넌트 추가
    - `src/components/common/loader.tsx`
    - `src/components/common/error.tsx`

  - 채팅 콘솔 구조 분리
    - `chat-console.tsx`는 server wrapper 역할로 축소
    - 실제 client 상태/탭/쿼리 처리는 `chat-console-workspace.tsx`로 분리

- [x] /chat 하위에서 console에 의해 호출되는하는 각 섹션 렌더링 구조 정리
  - `verify`, `retrieve`, `explain`, `compare` view에서 반복되던 결과 렌더링 패턴 공통화
  - 공통화 대상
    - 실행 버튼
    - pending loader
    - error notice
    - empty result
    - result section shell

  - 추가 파일
    - `src/features/chat/components/results/chat-async-result-section.tsx`

  - 적용 view
    - `verify-view.tsx`
    - `retrieve-view.tsx`
    - `explain-view.tsx`
    - `compare-view.tsx`

  - `CertificateView`는 발급/재검증/tamper 상태 흐름이 달라 공통화 대상에서 제외

- [x] JSON 상세 영역 코드 하이라이팅 추가
  - raw JSON 출력 영역에 syntax highlighting 적용
  - `prism-react-renderer` 패키지 설치
  - `JsonDetails`에서 기존 `pre` 텍스트 출력 대신 JSON 토큰 기반 렌더링 적용
