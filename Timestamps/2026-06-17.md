
#### Work (프로젝트: 공통 AI 검증코어)
---
- [x] 코드 구조 패키지화
  - 루트에 흩어져 있던 구현 파일들을 `trustlayer/` 패키지 하위로 이동
  - `app`, `config`, `context`, `infra`, `ingestion`, `legal`, `retrieval`, `utils` 디렉터리로 역할별 분류
  - 각 디렉터리에 `__init__.py` 추가
  - 루트 `main.py`는 CLI 실행 wrapper로 유지

- [x] 법령 청킹 모듈 분리
  - 기존 `loader.py`에 있던 청킹 로직을 `trustlayer/legal/chunking.py`로 분리
  - `loader.py`는 문서 로딩 책임 중심으로 축소
  - 기존 `loader` 기반 import 호환성 유지

- [x] 법령 데이터셋 변환 모듈 정리
  - `trustlayer/legal/legal_dataset.py` 추가 및 확장
  - API item을 검색 인덱스용 문서/metadata로 변환하는 `items_to_chunks()` 이동
  - 조문 단위 검색 문서 생성 함수 `build_law_documents()` 정리
  - `LawChunkMeta`를 데이터셋 모듈로 이동

- [x] 법령 API 파서 모듈 분리
  - `trustlayer/ingestion/parser.py` 추가
  - 법령 목록 XML/JSON 파싱 로직 분리
  - 상세 법령 XML에서 조문 본문 추출 로직 분리
  - 법령상세링크에서 `OC` 파라미터 추출 로직 분리

- [x] 법령 SOT(Source of Truth) 정책 모듈 추가
  - `trustlayer/legal/legal_sot.py` 추가
  - `document_id`, `snapshot_id` 생성 규칙 분리
  - `section_path` 생성 규칙 분리
  - 현재 인덱스 metadata가 최신 스키마인지 판별하는 함수 분리

- [x] 근거 추적 구조 확장
  - `SourceRef`에 `snapshot_id`, `document_id`, span/hash/quote 관련 필드 추가
  - `SourceSnapshot` 구조 추가
  - 검색 후보에서 `SourceRef`, `SourceSnapshot`을 생성하는 `legal_evidence.py` 추가
  - API 응답에 source snapshot 및 근거 metadata가 포함되도록 정리

- [x] ContextPackage 구조 확장
  - `micro_evidence`, `macro_context` 구조 추가
  - context builder에서 근거 블록과 macro context를 분리해 생성하도록 정리
  - API 응답에 context evidence 정보 포함

- [x] Retrieve More 구조 개선
  - 보강 검색 필요 여부를 coverage gap 기반으로 판단하도록 정리
  - 보강 검색 plan 생성 로직 추가
  - 기존 후보와 보강 후보 병합/재랭킹 helper 함수 분리
  - 이미 확보한 SourceRef 후보를 보강 검색에서 제외하도록 처리

- [x] 공통 유틸 모듈 추가
  - `trustlayer/utils/text.py` 추가
  - 공백 정규화, SHA-256 해시 생성 유틸 분리
  - metadata의 `text_hash`, `span_hash`, `block_hash` 생성에 활용

- [x] 검증 로직 강화
  - source ref와 final candidate 간 `chunk_id`, `snapshot_id`, `document_id` 일치 여부 검증
  - quote/hash/span 일관성 검증 추가
  - source snapshot 누락 및 참조 불일치 검증 추가

- [x] import 경로 정리 및 문법 검증
  - 내부 import를 `trustlayer.*` 절대 경로로 변경
  - 전체 패키지 `py_compile` 검증 통과
  - 작업 중 생성된 `__pycache__` 정리 완료

####  Work (프로젝트: AI Assistance)
---
- [x] 검수 리스트 페이지
	- [x] 검색 시작일 2025.03.01로 수정
	- [x] 선택된 일자에 따라 캘린더의 초기 컨텍스트 위치가 고정
