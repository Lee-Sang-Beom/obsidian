
#### Work (프로젝트: 공통 AI 검증코어)
---
### 모듈 추가

**1. 점수 정규화 모듈** (`retrieval_score_normalizer.py`)

- vector distance → vector_similarity 변환
- BM25 원점수 → 0~1 키워드 점수 변환
- 리랭커 원점수 → 0~1 점수 변환

**2. 벡터+키워드 융합 선택 모듈** (`fusion_selector.py`)

- vector_similarity + bm25 → normalized_retrieval 산출
- 리랭커에 넘길 1차 후보 선택

**3. 리랭크 결과 선택 모듈** (`rerank_selector.py`)

- rerank 원점수 → normalized_rerank → final_score 산출
- 최종 근거 후보 선택

---

### 기타 작업

- 2주차 작업 내용 기반 RAG 파이프라인 구현 분석 문서 작성
- AI 검증코어 회의 참여

---

### MongoDB Source Store 연동

**기반 추가**

- `docker-compose.mongo.yml` 추가
- MongoDB를 법령 원문/snapshot/chunk/SourceRef 저장소로 사용
- Chroma는 벡터 검색 index, Elasticsearch는 키워드 검색 index 역할 유지

**설정 및 의존성**

- `.env`에 `USE_MONGO_SOURCE_STORE=1`, `MONGO_URL`, `MONGO_DB` 설정 추가
- `.env.mongo.example` 추가
- `requirements.mongo.txt`에 `pymongo` 추가

**저장 계층 추가**

- `trustlayer/infra/mongo_store.py` 추가
- `trustlayer/legal/source_store.py` 추가
- `law_documents`, `law_snapshots`, `law_chunks`, `index_runs` collection 구성

**reindex 연결**

- `/reindex` 실행 시 법령 chunk를 MongoDB에도 저장하도록 `indexer.py` 수정
- 기존 Chroma/Elasticsearch 색인 흐름 유지

**Health check 및 문서 보강**

- `/health`에 MongoDB 상태 추가
- README에 MongoDB 실행, 환경변수, reindex 절차 기록

**동작 확인** — `mongo.enabled=true`, `mongo.ok=true` 확인