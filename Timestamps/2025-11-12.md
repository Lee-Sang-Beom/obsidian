
#### Work
---
#### 페이지: AI 시그널 
- [x] 당일 발굴 이후 상승률 상위에 대한 금일 정보가 없는 경우에 대한 추가 로직 작성
	- PaginationResult에 포함되는 형태로 interface extends를 통한 구현
	- MobileLoadingState 제거
	- dateDiff 계산 및 기준일자 표시 컴포넌트를 오늘의 시그널 측 컴포넌트와 통일화

- [x] 오늘의 시그널에 대한 금일 정보가 없는 경우에 대한 추가 로직 작성
	- 오늘의 시그널 그래프 visible에 대한 그래프 표시여부 결정
	- MobileLoadingState 제거
	- 수정 1차: PaginationResult에 포함되는 형태로 interface extends를 통한 구현
	- 수정 2차: PaginationResult에 포함되지 않는 형태로 API가 수정됨에 따라 서비스 파일 수정 및 UI 로직 재구현
	- dateDiff 계산 및 기준일자 표시 컴포넌트를 상승률 측 컴포넌트와 통일화

#### 페이지: 팟캐스트
- [x] 국내 뉴스, 글로벌 코인뉴스 탭: 커서 페이지네이션으로 전환
	- 해당 과정에서 불필요한 프론트엔드 `_uniqueKey` 생성하는 내부적 과정 제거

- [x] 실시간 발언 탭: 커서 페이지네이션으로 전환
	- 해당 과정에서 불필요한 프론트엔드 `_uniqueKey` 생성하는 내부적 과정 제거

- [x] news-bottom-section 컴포넌트 리팩토링
	- usePodcastListData 정의
		- 하나의 단일 훅 내에서 분기하여 팟캐스트 서비스의 특정 함수를 호출하는 방식 제거. 해당 훅에 분기로직을 옮기고 각 서비스 함수를 호출하는 단일 기능을 수행하는 hook을 호출할 수 있도록 함
		- 해당 훅에 주요 view쪽에서 발생가능한 이벤트 정보(앱 플레이어 store, 이미지 클릭, 팟캐스트 상세보기 클릭 등) 작성 후, news-bottom-section쪽에서 호출하여 사용할수있도록 export진행
	- 컨테이너/표현 컴포넌트 적용 과정에서 `common-podcast-list-content`컴포넌트 추가

- [x] PodcastListItemTimeDisplay 컴포넌트 리팩토링
	- 시간 포맷팅 및 실시간 업데이트 로직을 Container로 분리
	- Clock 아이콘과 시간 텍스트 표시 UI를 View로 분리
	- needsRealTimeUpdate 함수와 interval 관리 로직 유지
	- '방금 전', '분 전' 텍스트 감지 시 60초마다 자동 업데이트 기능 보존

- [x] PodcastListItem 컴포넌트 리팩토링
	- 이미지 로드/검증, 태그 처리, 마크다운 파싱 로직을 Container로 분리
	- 팟캐스트 카드 UI (이미지, 태그, 제목, 내용, 시간)를 View로 분리
	- extractPlainText 함수로 마크다운 텍스트 변환 로직 유지
	- handleImageClick, handleOnCardClick 이벤트 핸들러 보존
	- 이미지 유효성 검사 및 placeholder 처리 로직 유지
	- 태그 글자 수 계산 후 표시 개수 조정 로직(15자 기준) 유지

- [x] LivePodcastListItem 컴포넌트 리팩토링
	- 향후 확장 가능성을 위한 Container 구조 생성
	- 라이브 피드 카드 UI(빨간색 테마, 실시간 표시)를 View로 분리
	- 현재는 단순 패스스루 구조이지만 일관성 있는 패턴 적용
	- 라이브 상태 표시(빨간 점), 키워드 표시 기능 유지

- [x] PodcastDetail 컴포넌트 리팩토링
	- Zustand store 연결, 플레이어 상태 관리 로직을 Container로 분리
	- 웹/앱 플레이어 UI 및 진행률 바를 View로 분리
	- 웹 플레이어 시간 추적 interval(100ms polling) 로직 유지
	- 마인드맵 JSON 파싱 및 데이터 로드 로직 보존
	- handleWebPlayerPlay/Pause/Resume 이벤트 핸들러 유지
	- 앱 플레이어 활성화 시 상태 표시 로직 보존
	- getCurrentPlayTime, getTotalPlayTime, getProgress 계산 함수 유지
	- 3개 섹션(제목, 마인드맵, 설명) UI 구조 보존
	- ResponsiveModal과 ReactMarkdown 렌더링 기능 유지
