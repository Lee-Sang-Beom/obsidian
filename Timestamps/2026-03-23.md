#### Work (프로젝트: ICMS 제작)
---
- [x] 필요 const값 조사 및 프로젝트 내 정의/정리
	- 기존 `src/data/` 하위에 도메인별로 혼재되어 있던 상수 파일들을 클린 아키텍처 레이어 기준으로 `src/constants/` 하위에 재구성
	- `domain/` : 비즈니스 상태값 상수 정의 (`state`, `video`, `user`, `resource`, `error`)
	- `application/` : 앱 동작 규칙 상수 정의 (`regex`)
	- `infrastructure/` : 외부 연동 및 환경 설정 상수 정의 (`app-config`, `keys`)
	- `presentation/` : UI 표현 관련 상수 정의 (`labels`, `menus`, `paths`)
	- Vue `global-filter`에서 사용하던 숫자 코드값 → 한글 텍스트 변환 로직을 React에서 사용 가능한 label 매핑 객체 및 함수로 전환하여 `presentation/labels.ts`에 통합

- [x] 개발 서버 연결 작업 및 로그인/로그아웃 구현
	- [x] 로그인 스키마 정의
	- [x] 로그인에 필요한 타입 정의 (common, model, dto 등) 
	- [x] 로그인 서비스 및 호출 hooks 구현
	- [x] 로그아웃 시, 스토리지 및 store 초기화

- [x] 로그인 후, 자동 MAIN 페이지 이동 처리 및 MAIN 페이지 구현 진행 


#### Work (프로젝트: 보일러 플레이트)
---
- [x] 패키지 최신 버전 확인 및 업데이트 진행 
- [x] 기존 `src/data/` 하위에 도메인별로 혼재되어 있던 상수 파일들을 클린 아키텍처 레이어 기준으로 `src/constants/` 하위에 재구성
	- 기존 `src/data/` 하위에 도메인별로 혼재되어 있던 상수 파일들을 클린 아키텍처 레이어 기준으로 `src/constants/` 하위에 재구성
	- `domain/` : 비즈니스 상태값 상수 정의 (`error`)
	- `application/` : 앱 동작 규칙 상수 정의 (`regex`)
	- `infrastructure/` : 외부 연동 및 환경 설정 상수 정의 (`app-config`, `keys`)
	- `presentation/` : UI 표현 관련 상수 정의 (`menus`, `paths`)

- [x] 보일러 플레이트 로그인코드 수정
	- 로그인에 필요한 타입 수정 (common, model, dto 등) 
	- 로그인 서비스 및 호출부 수정

