

- [x] 검수목록 페이지: 검수 전 데이터에 대한 검수 팝업의 승인 완료 후속 작업 진행
	- 기존 코드 리팩토링 작업 진행 (컨테이너/표현 컴포넌트 분리 원칙에 집중)
		- settings-form-container
		- settings-form-presenter
		- settings-form-ring
		- settings-approve-form-container
		- settings-approve-form-presenter
		- settings-hold-form-container
		- settings-hold-form-presenter
	
	- 승인 완료 시, 승인 동작에 대한 응답에 대한 팝업 작업 진행 
		- `handleApprove` mutate 이후의 onSuccess, onError에 결과에 따른 별도 ApproveResultType의 객체 생성 후 `useApproveResultModal hook` 호출 (응답의 성공여부 enum와 메시지를 담는 객체)
		- `useApproveResultModal hook`: 승인 완료 결과 메시지를 표시하는 모달 상태 관리 hook
		- `<ApproveResultModal/>`
			* 승인 완료 결과를 표시하는 모달  
			* 서버 응답 메시지를 표시하고 확인 버튼으로 닫을 수 있음  
			* status가 SUCCESS가 아닐 경우 경고 스타일(붉은 배경)로 표시

- [x] 검수목록 페이지: 리스트에 처리상태 column이 표시될 수 있도록 업데이트
	- rmIdx가 No 영역에 표시되도록 롤백
	- `/assistant-api/approvals/search api` response에 bizState 추가