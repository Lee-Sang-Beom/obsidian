#### Work
---
- [x] 코드 리팩토링 및 가독성 개선
	- 검수 리스트 검색영역 컴포넌트들의 중복 로직 제거, 컴포넌트 분리
	- 검색 공통 컴포넌트의 field type별 렌더되는 content 추출함수 별도 생성 및 로직통합
	- `Settings` 관련 컴포넌트들의 중복 로직 제거 및 헬퍼 함수 추출
    - `SettingsApproveFormContainer`: 중복 로직 제거 및 헬퍼 함수 추출
    - `SettingsHoldFormContainer`: 기본값 로직 분리 및 모달 닫기 로직 통합
    - `SettingsFormContainer`: 복잡한 useEffect 로직 단순화 (헬퍼 함수 3개 추출하는 방식으로 진행)

- [x] 캐시 버스터(Cache Buster) 시스템 구현
    - 프로덕션 환경에서 새 버전 배포 시 사용자 브라우저 자동 새로고침 기능 구현
    - 기존 RELEASE 파일 생성 방식의 문제점 파악 (nginx 서버에서 404 에러)
    - HTML 파싱 방식으로 구현 변경: Vite 빌드 시 생성되는 JS 파일의 해시를 버전 key로 활용하여 새로고침 될 수 있도록 진행
    - 3가지 버전 체크 시점: 초기 로드, 30초 주기, 페이지 이동 시

- [ ] 통합테스트과정 적합한 테스트케이스 tool, lib 탐색
- [ ] 코드 리팩토링 공통적이고 근본적인것들 기준으로 진행

