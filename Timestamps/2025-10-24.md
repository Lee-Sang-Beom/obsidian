
#### Work
---
#### 프로젝트: 82ALDA Podcast Service
###### 메뉴: AI 시그널
- [x] '오늘의 시그널' 관련 컴포넌트 수정사항 반영
	- 문제사항
		- 표시되는 블록 개수에 집중하여 UI에 표시하는 방법은 디바이스 너비가 변경됨에 따라 progress의 채워짐정도가 유동적으로 변하지 않는 문제 발생
		- 블록단위로 표시되는 UI 형태를 평행사변형 형태로 만들고, 각 평행사변형 사이의 gap을 줄여야 함. 그리고 해당과정에서 gap과 평행사변형 너비까지 고려하여 전달되는 rate값이 디바이스 너비에 따라 유동적으로 변화해야함
		- 기타 코드 리팩토링
	- 해결
		- 평행사변형 형태의 block shape style을 만드는 별도 스타일 함수 getShapeStyle 추가 -> progress bar 영역을 해당 함수를 이용하여 채우도록 변경
		- hsl방식을 이용하여 진행도에 따른 그라데이션 색상을 계산하는 함수 calculateGradientColor 추가 -> progress bar 영역에 해당하는 평행사변형의 위치(progress) 정보에 따라 유동적으로 적절한 색상을 넣을 수 있도록 변경

---
#### 프로젝트: kringo_assistant

###### README.md 내용 추가 (기존에는 package.json에 기재된 패키지 설명까지만 기재되어 있었음)

> 디렉터리 구조
```

- `public`
	- `audio` : 로컬 저장용 오디오파일 저장
	- `img` : 로컬 저장용 이미지파일 저장

- `src/components`
	- `data-table`: 리스트 화면에서 사용할 테이블 관련 컴포넌트를 모아둔 디렉터리
	- `layout`: 프로젝트 전반적인 레이아웃 관련 컴포넌트를 모아둔 디렉터리
	- `magicui`: `magic-ui`(https://magicui.design/)에서 installation한 컴포넌트를 모아둔 디렉터리 (설치 시, 자동으로 해당 컴포넌트 디렉터리가 생김)
	- `ui`: `shadcn-ui`(https://ui.shadcn.com/)에서 installation한 컴포넌트를 모아둔 디렉터리 (설치 시, 자동으로 해당 컴포넌트 디렉터리가 생김)
	- `audio-player.tsx`: 오디오 플레이어 UI (82alda 팟캐스트 CMS의 팟캐스트 관리 - 팟캐스트 메뉴의 오디오 UI 참고)
	- `debounce-input.tsx`: 디바운스 관리 내용이 포함된 `shadcn-ui`의 `input` 컴포넌트
	- `loading.tsx`: 페이지 전체 로딩, 카드 로딩, 테이블 컴포넌트 로딩 등의 로딩 관련 UI 컴포넌트 (해당 프로젝트에서는 잘 사용하지 않을 것으로 추정)
	- `responsive-modal.tsx`: 프로젝트 전반에서 사용되는 Dialog의 Provider UI (PC와 모바일의 Dialog 형태가 다름.)
	- `search-filter.tsx`: 테이블이 포함된 리스트 페이지에서 검색 기능을 위해 활용하는 검색 필터 UI

- `src/data`
	- `constant.ts`: 프로젝트 전역 상수 정의
	    - 환경변수 기반 도메인 URL (MSW, API, Contents)
	    - 시간 관련 상수 (자동 새로고침, 대기시간, 타임아웃)
	    - 날짜 포맷 타입별 moment.js 포맷 문자열 매핑 (`DATE_FORMAT_MAP`)
	    - TanStack Query 전역 옵션 설정 (재시도, 자동 갱신, 캐시 등)
	- `error.ts`: 에러 처리 관련 정의
	    - `DomainError` 커스텀 에러 클래스
	    - 네트워크 에러, 데이터 에러 등 공통 에러 메시지
	- `key.ts`: 외부 서비스 인증 키 및 로컬 스토리지 키 관리
	    - Google OAuth 설정 (client_id, client_secret, redirect_url)
	    - Kakao OAuth 설정 (javascript_key, redirect_url, logout_redirect_url)
	    - 로컬 스토리지 키 상수 (자동 로그인, JWT 토큰, 유저 정보)
	- `menu.ts`: 사이드바 네비게이션 메뉴 구조 정의
	    - 검수 관리, 통계 관리, 계정 관리, 권한 관리 등 메뉴 그룹별 데이터
	    - 각 메뉴 항목의 제목, URL, 아이콘(Lucide React) 정보
	- `paths.ts`: 라우팅 경로 상수 관리
	    - `PATHS`: 절대 경로 정의 (로그인, 검수, 통계, 계정, 권한, 에러 페이지)
	    - `RELATIVE_PATHS`: 상대 경로 정의 (라우터 설정용)
	    - `BEFORE_LOGIN_ROUTES`: 로그인 전 접근 가능한 경로 목록
	- `regex.ts`: 폼 검증용 정규식 패턴 모음
	    - 비밀번호 검증 (일반/강력/간단)
	    - 이메일 검증 (일반/엄격)
	    - 전화번호 (휴대폰/일반전화/국제전화)
	    - 이름 (한글/영문/사용자명)
	    - 숫자/날짜/주소/특수 용도(IP, 카드번호, 주민번호, 사업자번호 등) 검증 패턴

- `src/hooks`
	- `use-audio.ts`: 오디오 플레이어 제어를 위한 커스텀 훅
	    - Zustand 오디오 스토어를 래핑하여 사용하기 쉬운 인터페이스 제공
	    - seek 디바운싱 처리 (50ms, 슬라이더 드래그 시 성능 최적화)
	    - 시간 포맷팅 (`formatTime`), 진행률 계산 (`getProgress`), 현재 트랙 확인 (`isCurrentTrack`) 유틸리티 함수
	    - 컴포넌트 언마운트 시 타이머 정리 및 오디오 리소스 cleanup
	- `use-auth-initialization.ts`: 인증 상태 초기화 및 관리 훅
	    - JWT 토큰 파싱 및 만료 여부 확인
	    - 앱 초기 로드 시 토큰 유효성 검증 및 자동 로그인 처리
	    - StrictMode 중복 실행 방지 (useRef로 초기화 상태 추적)
	    - 인증 실패 시 로그인 페이지로 리다이렉트
	- `use-confirm.tsx`: 확인 다이얼로그 훅
	    - Promise 기반 확인/취소 다이얼로그 제공
	    - shadcn-ui Dialog 컴포넌트를 활용한 모달 UI
	    - `[ConfirmationDialog, confirm]` 형태로 반환 (컴포넌트와 함수)
	- `use-is-mobile.ts`: 모바일 디바이스 감지 훅
	    - 미디어 쿼리를 활용한 반응형 레이아웃 제어
	    - 768px 미만을 모바일로 판단 (`MOBILE_BREAKPOINT`)
	- `use-performance.ts`: 성능 최적화 관련 훅 모음
	    - `useDebounce`: 값을 지정된 지연 시간만큼 디바운스 (연속 입력 제어)
	    - `useThrottle`: 값을 지정된 시간 간격으로 스로틀링 (이벤트 빈도 제한)
	    - `usePrevious`: 이전 렌더링의 값 추적 (값 변경 감지 및 비교용)
	    - `useIsMountedState`: 컴포넌트 마운트 및 DOM 렌더링 완료 상태 확인

- `src/lib`
	- `utils`: 범용 유틸리티 함수 모음
	    - `common-utils.ts`: 공통 유틸리티 함수
	        - `cn`: Tailwind CSS 클래스명 조건부 병합 (clsx + twMerge)
	        - `getErrorMessage`: 에러 코드로 메시지 반환
	        - `sleep`: 비동기 대기 함수
	        - `checkImageValidity`: 이미지 URL 유효성 검사
	        - `handleImageError`: 이미지 로드 실패 시 기본 이미지로 대체
	        - `getResourceUrl`: 리소스 전체 URL 생성 (CONTENT_URL + 상대경로)
	        - `calculateRowId`: 페이지네이션 역순 번호 계산
	    - `form-utils.ts`: 폼 입력 관련 유틸리티 함수
	        - 비밀번호 강도 체크 (`checkPasswordStrength`)
	        - 전화번호/일반전화/사업자등록번호/주민등록번호/신용카드번호 포맷팅
	        - 금액 천 단위 콤마 포맷팅 (`formatCurrency`)
	        - 한글/영문/숫자 필터링 함수
	        - 이메일 도메인 자동완성 제안
	        - React Hook Form 실시간 포맷팅 핸들러
	        - DateRange를 API 요청 형식(YYYY-MM-DD)으로 변환
	    - `time-utils.ts`: 날짜/시간 관련 유틸리티 함수
	        - `formatDate`: moment.js 기반 다양한 날짜 포맷팅 (DATE_FORMAT_MAP 활용)
	        - `getTimeAgo`: 상대적 시간 표시 (1시간 이내: "n분 전", 이후: "M월 D일 오전/오후 h시 mm분")
	- `axios.data.ts`: Axios 기반 API 클라이언트 설정
	    - `Either` 타입을 활용한 함수형 에러 핸들링 (effect 라이브러리)
	    - JWT 토큰 자동 주입 인터셉터
	    - HTTP 메서드별 함수: `getFetchData`, `postFetchData`, `putFetchData`, `patchFetchData`, `deleteFetchData`
	    - API 에러 타입: `DomainError` (400, 401, 500), `GlobalError` (기타 에러)
	    - 에러 변환 및 처리 유틸리티: `throwIfErrorAndExtractValue`, `convertToError`, `throwEtcError`
	- `form-schema.ts`: Zod 기반 폼 검증 스키마 정의
	    - 재사용 가능한 필드 스키마: `emailSchema`, `passwordSchema`, `phoneNumberSchema`
	    - 폼별 스키마: `loginFormSchema`, `updateAccountFormSchema`
	    - data/regex.ts의 정규식 패턴 활용
	- `types.ts`: 프로젝트 전역 타입 정의
	    - `ActivityStateType`: React 화면 상태 ('hidden' | 'visible')
	    - `ThemeStateType`: 테마 타입 ('light' | 'dark')
	    - `DateFormatType`: 날짜/시간 포맷 타입 (20+ 종류)
	    - `PagingResult<T>`: 페이징 응답 인터페이스 (nowPage, pageSize, totalCount, items, lastPage)
	    - `CreateDialogZustandState`: 생성 다이얼로그 Zustand 상태 인터페이스
	    - `EditDialogZustandState<T>`: 수정 다이얼로그 Zustand 상태 인터페이스 (제네릭)

- `src/pages`
	- `auth`: 로그인 과정을 포함하는 페이지
	- `errors`: 에러 내용을 표시하는 페이지
	- `기타`: 프로젝트에서 요구하는 경로별 페이지
	- `pages/[페이지 경로별 디렉터리명]/_components`: 페이지별로 해당 페이지에서만 사용하는 컴포넌트를 모은 디렉터리
	- `pages/[페이지 경로별 디렉터리명]/hooks`: 페이지별로 해당 페이지에서만 사용하는 컴포넌트를 모은 디렉터리
	- `pages/[페이지 경로별 디렉터리명]/types`: 페이지별 타입정보를 포함하는 디렉터리
		- `internal.ts`: API 엔드포인트 통신과 관계없는 프론트엔드 내부 로직에서만 사용하는 데 필요한 타입정보를 모은 파일
		- `api.ts`: API 엔드포인트 통신에 필요한 타입정보를 모은 파일
	
	- `pages/[페이지 경로별 디렉터리명]/[페이지 경로를 대표하는 적절한 이름]-page.tsx`: 페이지별 메인이 되는 페이지 index 파일

- `src/provider`
- `modal-provider.tsx`: 전역 모달 관리 Provider
	- 프로젝트 전체에서 사용되는 모든 모달 컴포넌트를 한 곳에서 렌더링
	- `useMountedState`로 마운트 상태 확인 후 렌더링 (SSR 호환성)
	- 각 모달은 Zustand 스토어로 open/close 상태 관리
- `query-provider.tsx`: TanStack Query (React Query) Provider
	- `QueryClient` 인스턴스 생성 및 제공
	- SSR/CSR 환경에 따른 QueryClient 싱글톤 관리
	- 브라우저 환경에서는 단일 인스턴스 재사용 (`browserQueryClient`)
	- 기본 옵션: `staleTime: 60초` (1분간 데이터를 fresh 상태로 유지)

- `src/routes`
	- `app-route.tsx`: 애플리케이션 전체 라우트 구조 정의
	    - `createBrowserRouter`를 사용한 라우터 설정
	    - 코드 스플리팅을 위한 lazy loading 적용 (모든 페이지 동적 import)
	    - 라우트 구조(UI 생성 시점의 코드 기준):
	        - `/login`: 로그인 페이지
	        - `/`: 메인 레이아웃 (AppLayout)
	            - `/inspection/list`: 검수 목록
	            - `/statistics/inspection`: 검수 통계
	            - `/account/management`: 계정 관리
	            - `/permission/account-list`: 권한 관리 계정 목록
	        - 에러 페이지: `/403` (IP 제한), `/404` (Not Found)
	        - 와일드카드(`*`): 매칭되지 않는 모든 경로를 404로 리다이렉트
	    - 모든 라우트에 `PrivateRoute`로 인증 보호 적용
	
	- `private-route.tsx`: 인증 기반 라우트 보호 컴포넌트
	    - JWT 토큰 기반 인증 상태 확인 (`useAuthInitialization`)
	    - 인증 로딩 중일 때 null 반환 (또는 로딩 스피너)
	    - 미인증 사용자: 로그인 페이지로 리다이렉트
	    - 인증된 사용자가 로그인 전용 페이지 접근 시: 검수 목록 페이지로 리다이렉트 (`BEFORE_LOGIN_ROUTES` 체크)
	    - 정상 인증 사용자: children 렌더링

- `src/service`
	-  `axios-data.ts`의 HTTP 메서드 함수(`getFetchData`, `postFetchData` 등)를 활용하여 API 호출
	- `Either` 타입을 통한 함수형 에러 핸들링 (성공/실패 분기 처리)
	- API 응답 에러 시 `getErrorMessage`로 사용자 친화적 에러 메시지 생성 후 throw
	- API 성공 시 Zustand 스토어(`useAuthStore`)와 연동하여 상태 업데이트 (토큰 저장, 로그아웃 시 인증 정보 초기화)
	- 각 서비스 함수는 비즈니스 로직에 맞는 타입(Request/Response)과 반환값 제공
	- `auth-service.ts`:  인증 관련 API 통신 서비스
	- `[도메인별 커스텀네임]-service.ts`: `auth-service.ts`처럼 페이지 및 역할별로 생성하여 사용하면 됨

- `src/store`
	- `use-audio-store.ts`: 오디오 플레이어 전역 상태 관리
	    - Howler.js 기반 오디오 재생 제어 및 상태 관리
	    - 상태: `currentAudio` (Howl 인스턴스), `currentUrl`, `isPlaying`, `isLoading`, `duration`, `currentTime`, `volume`, `error`, `timeUpdateInterval`, `isSeeking`, `lastSeekTime`
	    - 액션: `play` (URL 기반 재생), `pause`, `resume`, `stop`, `reset`, `setVolume`, `seek` (시간 이동), `cleanup` (리소스 정리)
	    - 시간 업데이트: 100ms 주기로 재생 시간 추적 (`startTimeUpdate`, `stopTimeUpdate`)
	    - 스마트 재생: 같은 URL 재생 시 새로 로드하지 않고 이어서 재생
	    - seek 최적화: `isSeeking` 상태로 UI 업데이트와 실제 seek 동작 충돌 방지, 중복 seek 방지 (0.1초 미만 차이 무시)
	    - 에러 핸들링: Howler.js 이벤트(`onload`, `onplay`, `onpause`, `onstop`, `onend`, `onseek`, `onloaderror`, `onplayerror`)로 상태 동기화 및 toast 알림
	    - 메모리 관리: `cleanup`으로 타이머 정리 및 Howler.js unload (메모리 누수 방지)
	- `use-auth-store.ts`: 인증 상태 전역 관리 (JWT 토큰, 사용자 정보)
	    - localStorage와 Zustand 상태 동기화 (토큰, 사용자 정보)
	    - 초기 상태: localStorage에서 로드 (`getInitialState`)
	    - 내부 액션: `setTokenInternal`, `setUserInternal`, `clearAuthInternal` (localStorage 읽기/쓰기/삭제 + Zustand 상태 업데이트)
	    - 외부 API: 기존 코드와 호환성 유지를 위해 async 인터페이스 제공
	        - `setToken`, `getToken`: 토큰 설정/조회 (null/undefined 정규화)
	        - `setUser`, `getUser`: 사용자 정보 설정/조회
	        - `isAuthenticated`: 인증 상태 확인 (토큰 존재 여부)
	        - `clearAuth`: 인증 정보 초기화 (로그아웃 시 사용)
	    - 에러 핸들링: localStorage 접근 실패 시 콘솔 경고만 출력하고 계속 진행

- `src/App.tsx`
	- Provider 계층 구조 설정 (외부→내부 순서):
	    - `ThemeProvider` (next-themes): 다크/라이트 모드 관리, localStorage 키: `vite-aitts-ui-theme`, 기본값: `light`
	    - `QueryProvider`: TanStack Query 전역 설정
	    - `ModalProvider`: 전역 모달 컴포넌트 렌더링
	    - `RouterProvider`: React Router 라우팅 (app-route.tsx의 router 사용)
	- `Toaster` (sonner): 전역 토스트 알림 UI
	    - 위치: 상단 중앙 (`top-center`)
	    - 색상 구분 활성화 (`richColors`)
	    - 표시 시간: 1.5초 (`duration={1500}`)
	    - 동시 표시 개수: 1개 (`visibleToasts={1}`)

- `src/App.tsx`
	- Provider 계층 구조 설정 (외부→내부 순서):

- `src/main.tsx: 애플리케이션 진입점 (엔트리 파일)
	- React 18의 `createRoot` API로 루트 DOM 렌더링
	- `StrictMode`로 개발 환경에서 잠재적 문제 감지 (이중 렌더링, 부작용 체크 등)
	- `#root` DOM 요소에 `App` 컴포넌트 마운트

- `src/index.css`: 전역 스타일 정의 (Tailwind CSS v4 + shadcn/ui 테마)
	- **폰트**: Noto Sans KR (Google Fonts, 전체 요소 강제 적용)
	- **Tailwind CSS v4**: `@import "tailwindcss"`, `@import "tw-animate-css"` (애니메이션)
	- **다크모드**: `@custom-variant dark` (`:is(.dark *)` 셀렉터)
	- **CSS 변수 테마**:
	    - `:root`: 라이트 모드 색상 (OKLCH 색공간 사용)
	    - `.dark`: 다크 모드 색상
	    - shadcn/ui 디자인 토큰: `--background`, `--foreground`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--input`, `--ring` 등
	    - 사이드바 전용 토큰: `--sidebar-*` 시리즈
	    - 차트 색상: `--chart-1` ~ `--chart-5`
	- **@layer base**:
	    - 전역 박스 모델: `box-sizing: border-box`
	    - 오버스크롤 방지: `overscroll-behavior: none`
	    - 버튼 커서 스타일: 비활성화 제외 `cursor: pointer`
	    - Input 스타일 정규화: 비밀번호 표시 아이콘 숨김 (Edge), number 타입 화살표 제거 (모든 브라우저)
	- **Toast 인터랙션**: sonner의 모든 요소를 클릭/상호작용 가능하도록 `pointer-events: auto` 강제 적용

- `.env.development`, `.env.production`
	- 해당 프로젝트에서는 MSW, Firebase, Google, Kakao OAuth는 사용하지않을것이고 API 서버 관련 key정도만 사용할 것으로 분석
	- 모든 환경변수는 `data/constants.ts`와 `data/key.ts`에서 import하여 사용
	- **MSW (Mock Service Worker)**:
	    - `VITE_MSW_MOCKING`: MSW 목업 활성화 여부 (개발/테스트용)
	    - `VITE_MSW_DOMAIN_URL`: MSW 목업 서버 도메인
	- **Firebase 설정**:
	    - Firebase 인증, 스토리지, 메시징 등을 위한 설정값
	    - `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`, `VITE_FIREBASE_MEASUREMENT_ID`
	- **Google OAuth**:
	    - `VITE_GOOGLE_CLIENT_ID`: Google OAuth 클라이언트 ID
	    - `VITE_GOOGLE_CLIENT_SECRET`: Google OAuth 클라이언트 시크릿
	    - `VITE_GOOGLE_CLIENT_REDIRECT_CALLBACK_URL`: OAuth 인증 후 리다이렉트 URL
	- **Kakao OAuth**:
	    - `VITE_KAKAO_CLIENT_KEY`: Kakao JavaScript 앱 키
	    - `VITE_KAKAO_CLIENT_REDIRECT_CALLBACK_URL`: Kakao 로그인 후 리다이렉트 URL
	    - `VITE_KAKAO_CLIENT_LOGOUT_REDIRECT_CALLBACK_URL`: Kakao 로그아웃 후 리다이렉트 URL
	- **API 서버**:
	    - `VITE_API_DOMAIN_URL`: 백엔드 API 서버 도메인 (예: [http://192.168.1.13:7591](http://192.168.1.13:7591))
	    - `VITE_CONTENTS_URL`: 정적 리소스(이미지, 오디오 등) CDN/서버 URL

- Lint 관련 코드
	- `eslient.config.js`
		-  **기본 설정**:
		    - 빌드 산출물 제외: `dist` 디렉터리 무시
		    - 대상 파일: `**/*.{ts,tsx}` (TypeScript, TSX 파일)
		    - ECMAScript 버전: ES2020
		    - 환경: 브라우저 globals
		- **확장 규칙**:
		    - `@eslint/js`: JavaScript 권장 규칙
		    - `typescript-eslint`: TypeScript 권장 규칙
		    - `eslint-plugin-react-hooks`: React Hooks 권장 규칙
		    - `eslint-plugin-react-refresh`: Vite React Refresh 규칙
		- **커스텀 규칙**:
		    - `react-refresh/only-export-components`: 경고 수준, 상수 export 허용
		    - `@typescript-eslint/no-explicit-any`: **비활성화** (any 타입 사용 허용)
		    - `@typescript-eslint/no-unused-vars`: 경고 수준, `_`로 시작하는 변수/매개변수 무시
	
	- `tsconfig.app.json`
		- **기본 설정**:
		    - 타겟: ES2022
		    - 모듈 시스템: ESNext
		    - JSX: react-jsx (React 17+ 자동 JSX 변환)
		    - 라이브러리: ES2022, DOM, DOM.Iterable
		    - 타입 정의: vite/client, node
		- **번들러 모드**:
		    - 모듈 해석: bundler (Vite 최적화)
		    - TS 확장자 import 허용
		    - Verbatim 모듈 구문 사용
		    - 강제 모듈 탐지
		    - 빌드 출력 없음 (`noEmit: true`)
		- **엄격한 타입 체크**:
		    - `strict: true`: 모든 엄격 모드 활성화
		    - `noFallthroughCasesInSwitch`: switch문 fallthrough 방지
		    - `noUncheckedSideEffectImports`: 부작용 import 체크
		- **비활성화된 규칙**:
		    - `noUnusedLocals: false`: 사용하지 않는 지역 변수 허용
		    - `noUnusedParameters: false`: 사용하지 않는 매개변수 허용
		- **경로 별칭**:
		    - `@/*`: `./src/*` (절대 경로 import 지원)
		- **포함 대상**: `src` 디렉터리

###### 메뉴: 검수 목록
- [x] 전체/처리완료/검수/보류/보류(완료)에 대한 상태(필터)버튼 UI 구성
- [x] 데스크톱 친화적인 UI를 모바일에서도 확인할 수 있도록 반응형 UI 구현

###### 메뉴: 계정 정보
- [x] 데스크톱 친화적인 UI를 모바일에서도 확인할 수 있도록 반응형 UI 구현

###### 메뉴: 검수 통계
- [x]  API 타입 정의 및 서비스 레이어 구현 (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `StatisticsRequest`: 조회 기간 및 페이징 파라미터
    - `StatisticsResponse`: 처리일시, 처리상태(보류/처리완료), 구분, 건수
    - `StatisticsSummaryResponse`: 전체/보류/처리완료 건수
    - `ProcessingStatusType`: 보류, 처리완료 상태 타입
    - `PendingCategoryType`: 보류 구분 6종 (설정오류, 제작오류, 음원삭제 오류, 제작 및 설정 오류, 문의 오류, 기타)
    - `CompletedCategoryType`: 처리완료 구분 10종 (정확 계열 6종, 기타문의 4종)
- [x]  통계 서비스 함수 개발 (`statistics-service.ts`)  (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `getStatisticsList`: 통계 목록 조회 API 호출
    - `getStatisticsSummary`: 요약 통계 조회 API 호출 (전체/보류/처리완료 건수)
    - Mock 데이터 함수 포함 (`getStatisticsTestList`, `getStatisticsTestSummary`)
- [x]  React Query 훅 구현 (`statistics-queries.ts`)  (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `useStatisticsListQuery`: 통계 목록 조회 훅
    - `useStatisticsSummaryQuery`: 요약 통계 조회 훅
    - 계층적 쿼리 키 구조 설계 (캐싱 최적화)
- [x]  통계 페이지 UI 구현 (`statistics-list-page.tsx`)
    - 검색 필터: 날짜 범위 선택 및 검색/초기화 기능
    - 요약 카드: 전체/보류/처리완료 건수 및 백분율 표시
    - 그래프/리스트 탭 전환 기능
    - 반응형 테이블 및 페이지네이션
- [x]  요약 카드 컴포넌트 (`statistics-summary-cards.tsx`)
    - 전체(파란색), 보류(주황색), 처리완료(초록색) 3개 카드
    - 각 카드에 백분율 자동 계산 및 건수 표시
    - 로딩 상태 Skeleton UI 처리
- [x]  콘텐츠 영역 컴포넌트 (`statistics-content.tsx`)
    - 그래프/리스트 탭 버튼 (lucide-react 아이콘 적용)
    - 그래프 탭: 차트 표시 영역 안내 메시지
    - 리스트 탭: 통계 데이터 테이블 표시
- [x]  테이블 컬럼 정의 (`columns-statistics.tsx`)
    - No, 처리일시, 처리상태, 구분, 건수 컬럼
    - 처리상태별 Badge 색상 처리 (처리완료: default, 보류: secondary)
    - 구분별 Badge 색상 처리 (보류 오류: destructive, 정확: default, 기타문의: outline)
    - 건수 포맷팅 (천단위 구분 및 "건" 단위 표시)
- [x]  그래프 시각화는 진행하지 않음.
	- 작업시간이 크게 소요되니만큼, 실제 Response로 넘어올 데이터를 확인한 후 진행해야 함
	- 대신, active tab 활성화에 따라 차트가 표시될 영역이라고 기재함

###### 메뉴: 권한 관리
- [x]  API 타입 정의 및 서비스 레이어 구현 (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `PermissionUserRequest`: 페이징 파라미터 (nowPage, pageSize)
    - `PermissionUserResponse`: 회사구분, 사용자ID, 이름, 이메일, 전화번호, 등록일, 활성상태, 권한상태
    - `PermissionType`: 권한 타입 3종 (검수, 통계, 계정)
    - `CompanyType`: 회사 구분 2종 (내부, 외부)
    - `ActiveType`: 활성 상태 2종 (활성, 비활성)
- [x]  유저 서비스 함수 확장 (`user-service.ts`) (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `getPermissionUserList`: 권한 관리 유저 목록 조회 API 호출
    - `getPermissionUserTestList`: Mock 데이터 함수 (다양한 권한 조합 시뮬레이션)
    - 기존 유저 서비스에 권한 관리 기능 추가
- [x]  React Query 훅 구현 (`use-permission-actions.ts`) (API명 및 서비스에 있는 모든 코드는 UI 설계과정을 테스트하기 위한 테스트코드임에 주의)
    - `usePermissionUserListQuery`: 권한 관리 유저 목록 조회 훅
    - 계층적 쿼리 키 구조 설계 (`permissionQueryKeys`)
    - TANSTACK_QUERY_OPTION 적용
- [x]  권한 관리 페이지 UI 구현 (`account-list-page.tsx`)
    - 검색 필터 없이 페이징만 지원 (페이지 진입 시 자동 조회)
    - 기본 페이지 사이즈 20건
    - 반응형 테이블 및 페이지네이션
- [x]  테이블 컬럼 정의 (`columns-account.tsx`)
    - No, 회사, 계정, 이름, 이메일, 전화번호, 활성여부, 등록일, 권한상태 컬럼
    - 회사구분 Badge 색상 처리 (내부: default, 외부: secondary)
    - 활성여부 Badge 색상 처리 (활성: green, 비활성: gray)
    - 등록일 CellTimeDisplay 컴포넌트 활용 (날짜만 표시)
    - 권한상태 다중 Badge 표시 (검수: blue, 통계: purple, 계정: orange)
    - 권한 배열 최대 3개까지 표시 가능
- [x]  커스텀 Badge 컴포넌트 활용
    - 회사구분, 활성여부, 권한상태에 다양한 색상 variant 적용
    - green, blue, purple, orange, gray 등 커스텀 색상 활용