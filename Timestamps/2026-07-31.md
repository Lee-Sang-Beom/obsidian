#### Work (프로젝트: 보일러 플레이트)
---
- [x] `src/components/filters` 검색 필터 구조 리팩터링 검토 및 적용
  - 대상: `search-filter`, `input-filter`, `select-filter`, `date-filter`, `date-range-filter`, `date-range-unified-filter`

  - 날짜 필터 계열 공통화 검토 및 적용
    - 이전일 빌드 오류 발생 지점으로 확인된 검색 필터 날짜 계열 구조 점검
    - `Popover`, `Button`, `Calendar`, `CalendarIcon`, `month state`, `format` 로직 중복 확인
    - 변경된 `shadcn-ui` 캘린더 컴포넌트 계약 확인
    - `DateFilter`, `DateRangeFilter`, `DateRangeUnifiedFilter`에서 반복되는 날짜 선택 UI 공통화
    - `date-filter-parts.tsx`를 추가하여 날짜 포맷, 미래 날짜 제한, 날짜 트리거 버튼, 단일 날짜 팝오버 로직 분리
    - `date-range-unified-filter`는 시작일/종료일 각각의 단일 날짜 선택기를 조합하는 방식으로 개선
    - 기존 동작 유지
      - 미래 날짜 선택 불가
      - 시작일은 종료일 이후 선택 불가
      - 종료일은 시작일 이전 선택 불가
      - 날짜 선택 시 팝오버 닫힘

  - 날짜 필터 파일 구조 정리
    - 날짜 관련 파일을 `src/components/filters/date` 디렉터리로 이동
    - `filters` 루트에는 검색 필터 조립 및 비날짜 필터 중심으로 정리
    - 이동된 파일
      - `date/date-filter.tsx`
      - `date/date-range-filter.tsx`
      - `date/date-range-unified-filter.tsx`
      - `date/date-filter-parts.tsx`
      - `date/index.ts`
    - `search-filter.tsx`에서는 날짜 필터를 `./date` 경로를 통해 import하도록 변경

  - `SearchFilter` 타입 구조 개선 적용
    - 기존 `FilterField`에 모든 필터 타입의 속성이 섞여 있던 구조 개선
    - `input`, `select`, `date`, `dateRange`, `dateRangeUnified` 별로 타입 분리
    - discriminated union 구조로 변경하여 필터 타입별 props 안정성 개선
    - 기존 호출 방식과 런타임 동작은 유지

  - `InputFilter` 의존성 개선 적용
    - `InputFilter`가 `SearchFilter`의 `FilterField` 타입에 직접 의존하던 구조 제거
    - 필요한 props만 직접 받도록 변경
    - `value`, `onChange`, `placeholder`, `debounceMs`, `onKeyDown`, `disabled`를 명시적 props로 분리
    - 타입에는 존재했지만 실제로는 `300ms` 고정이던 `debounceMs`를 실제 디바운스 시간에 연결
    - Enter 입력 시 대기 중인 디바운스 취소 후 현재 입력값으로 검색 실행하는 기존 동작 유지

  - 검증
    - `npm run build` 통과
    - 기존 Vite `__dirname` 경고, chunk size 경고, npm env config 경고 외 신규 빌드 오류 없음

  - 커밋
    - `88668e2 refactor: 검색 필터 날짜 컴포넌트 구조 정리`

- [x] `shadcn-ui` 설치 컴포넌트 최신 버전 재설치
  - `pnpm` 기반으로 shadcn 전체 컴포넌트 재설치 진행
  - 실행 명령어
    - `pnpm dlx shadcn@latest add --all --overwrite --yes`

  - 갱신 내용
    - `src/components/ui` 하위 shadcn 컴포넌트 최신 CLI 기준으로 재설치 및 갱신
    - 기존 UI 컴포넌트 다수 업데이트
    - 신규 UI 컴포넌트 추가
      - `attachment.tsx`
      - `bubble.tsx`
      - `marker.tsx`
      - `message.tsx`
      - `message-scroller.tsx`

  - 의존성 변경
    - `package.json`, `pnpm-lock.yaml` 갱신
    - `@shadcn/react` 추가
    - `recharts` 버전이 shadcn 설치 과정에서 `^3.8.0`으로 조정됨

