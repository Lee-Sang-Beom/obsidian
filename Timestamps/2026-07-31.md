#### Work (프로젝트: 보일러 플레이트)
---
- [x] `src/components/filters` 검색 필터 구조 리팩터링
    - 대상: `search-filter`, `input-filter`, `select-filter`, `date-filter`, `date-range-filter`, `date-range-unified-filter`
    - 날짜 필터 공통화
        - 빌드 오류 원인이었던 날짜 필터 구조 점검, `Popover`/`Button`/`Calendar`/`month state`/`format` 로직 중복 확인
        - `shadcn-ui` 캘린더 컴포넌트 계약 변경 반영
        - `date-filter-parts.tsx` 추가로 날짜 포맷, 미래 날짜 제한, 트리거 버튼, 팝오버 로직 공통화
        - `date-range-unified-filter`는 단일 날짜 선택기 2개를 조합하는 방식으로 개선
        - 기존 동작(미래 날짜 불가, 시작일<종료일 제약, 선택 시 팝오버 닫힘) 유지
    - 날짜 필터 파일 구조 정리
        - 날짜 관련 파일을 `src/components/filters/date`로 이동 (`date-filter.tsx`, `date-range-filter.tsx`, `date-range-unified-filter.tsx`, `date-filter-parts.tsx`, `index.ts`)
        - `filters` 루트는 필터 조립 및 비날짜 필터 중심으로 정리, `search-filter.tsx`는 `./date` 경로로 import
    - `SearchFilter` 타입 구조 개선
        - 기존 `FilterField`의 혼재된 속성을 `input`/`select`/`date`/`dateRange`/`dateRangeUnified`별로 분리
        - discriminated union으로 변경해 타입 안정성 개선, 기존 호출 방식·런타임 동작은 유지
    - `InputFilter` 의존성 개선
        - `SearchFilter`의 `FilterField` 타입 직접 의존 제거, 필요한 props(`value`, `onChange`, `placeholder`, `debounceMs`, `onKeyDown`, `disabled`)만 명시적으로 받도록 변경
        - 기존 고정값이던 `debounceMs`를 실제 디바운스 시간에 연결, Enter 시 디바운스 취소 후 즉시 검색하는 기존 동작 유지
    - 검증
        - `npm run build` 통과, 기존 경고(Vite `__dirname`, chunk size, npm env config) 외 신규 오류 없음
    - 커밋
        - `88668e2 refactor: 검색 필터 날짜 컴포넌트 구조 정리`

- [x] `shadcn-ui` 컴포넌트 최신 버전 재설치
    - `pnpm dlx shadcn@latest add --all --overwrite --yes`로 전체 재설치
    - 갱신 내용
        - 기존 UI 컴포넌트 다수 업데이트, 신규 컴포넌트 추가(`attachment.tsx`, `bubble.tsx`, `marker.tsx`, `message.tsx`, `message-scroller.tsx`)
    - 의존성 변경
        - `package.json`, `pnpm-lock.yaml` 갱신, `@shadcn/react` 추가, `recharts`가 `^3.8.0`으로 조정됨
    - 검증
        - `npm run build` 통과, 기존 경고 외 신규 오류 없음
    - 커밋
        - `167ab5b refactor: shadcn-ui 컴포넌트 최신화`
- [x] Vercel 배포 환경 패키지 설치 재현성 개선
    - 로컬은 통과하지만 배포 clean install에서 실패할 수 있는 위험 점검, pnpm 기준 배포/로컬 환경 차이 확인
    - 기존 문제: `packageManager`가 `pnpm@latest`로 지정되어 Corepack/Vercel에서 버전 재현 어려움, 로컬에서도 `Cannot switch to pnpm@latest` 경고 발생
    - 예방 수정
        - `packageManager`를 `pnpm@10.6.3`으로 고정, `engines`에 `node: 24.x`, `pnpm: 10.6.3` 추가
        - `.pnpm-store/`를 `.gitignore`에 추가
    - 검증
        - `pnpm install --frozen-lockfile --strict-peer-dependencies --force` 통과, `npm run build` 통과, `pnpm-lock.yaml` 변경 없음, 기존 경고 외 신규 오류 없음
    - 참고
        - pnpm 10 설치 중 `@firebase/util`, `esbuild`, `msw`, `protobufjs` build script ignored 경고 확인되었으나 빌드에는 영향 없음

#### Work (프로젝트: AI 검증코어 프론트엔드)

---
- [x] 프론트엔드 배포 환경 구성
    - `192.168.1.22:3001`에서 동작하도록 Docker 배포 구성 (3000번은 내부 Git 서비스 사용 중이라 3001 사용)
    - `next.config.ts`에 `output: "standalone"` 설정, `Dockerfile`/`docker-compose.prod.yml`/`.dockerignore` 추가
    - 환경변수 분리: 로컬 `.env.local`, 배포 `.env.prod`(`.gitignore`에 추가)
    - 배포는 `docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build`로 진행

- [x] 패키지 매니저를 `pnpm` 기준으로 정리
    - Docker 빌드 시 `npm ci` 사용 중 플랫폼별 Next SWC 패키지 문제 발생 → `pnpm`으로 통일
    - `package.json`에 `packageManager: pnpm@10.6.3` 반영, `package-lock.json` 제거 후 `pnpm-lock.yaml` 생성
    - Dockerfile 설치 단계를 `pnpm install --frozen-lockfile`로 변경, Linux Docker에서 Windows 전용 optional dependency 문제 해결
