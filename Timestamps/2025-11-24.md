
#### Work (프로젝트: 82ALDA Podcast Service)
---
- [x] src/log 하위에 있는 애널리틱스 로그 mutatation 코드를 hooks/mutations 하위로 이동/수정
- [x] src/pages/signal/store에 있는 수익률 상위 시그널 페이지를 위한 정렬 store 참조 제거 및 관련 파일 모두 수정 (프론트 내에서 정렬하는 로직) 
- [x] find-pw에 해당하는 view 파일 리팩토링
	- src/pages/auth/find-pw-view: 라우팅 로직만 담당할 수 있도록 책임 분리
	- src/pages/auth/sections/find-pw-form-container: 비즈니스 로직 & 상태 관리 (폼 객체 및 onSubmit 생성)
	- src/pages/auth/sections/find-pw-form-presenter: 폼 UI 관리 
	- src/pages/auth/sections/find-pw-success-presenter: 성공 화면에 대한 UI 제공
	- src/pages/auth/components/header/auth-card-header: 성공 화면 및 form 입력 화면에서 공통으로 사용하는 title, description을 표시하는 ui 
- [x] ` _component, _sections, _view`에 해당하는 understore 제거
	- `auth`
	- `my-page`
	- `news`
	- `signal`

- [x] `sections`하위의 `_modals`를 `sections`와 동일한 depth 위치에 `modals`이라는 이름으로 재배치
	- `my-page`
	- `news`
	- `signal`

- [x] `src/domain/` 폴더 생성 및 도메인별 비즈니스 로직 이동
  - `auth`: types.ts, utils.ts
  - `my-page`: constants.ts, types.ts, utils.ts
  - `news`: types.ts, utils.ts
  - `signal`: chart-types.ts, constants.ts, dto.ts, internal-types.ts, market-utils.ts, utils.ts


- [x] 도메인별 타입 파일 구조 개선 및 JSDoc 주석 추가
  - 모든 도메인별 types.ts로묶여있는 단일 타입정보의 부분을 추출하여 types.ts와 dto.ts로 분리함. (아래는 해당내용 설명)
  - `auth/`
    - `types.ts`: 기존 타입 유지 + JSDoc 주석 추가
    - `dto.ts`: API 통신용 타입 분리 + JSDoc 주석 추가
  - `my-page/`
    - `types.ts`: 프론트 내부 타입 + JSDoc 주석 추가
    - `dto.ts`: API 통신용 타입 분리 + JSDoc 주석 추가
  - `news/`
    - `types.ts`: 프론트 내부 타입 + JSDoc 주석 추가
    - `dto.ts`: API 통신용 타입 분리 + JSDoc 주석 추가
  - `signal/`
    - `chart-types.ts` → `chart.ts`: 차트 전용 타입 + JSDoc 주석 추가
    - `internal-types.ts` → `types.ts`: 프론트 내부 타입 + JSDoc 주석 추가
    - `dto.ts`: API 통신용 타입 유지 + JSDoc 주석 추가

- [x] hooks 하위 파일별로 jsDoc 주석 추가 (역할)
	- (완료) auth domain
