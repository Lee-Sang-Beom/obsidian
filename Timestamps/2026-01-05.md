#### Work (프로젝트: AI Assistance)
---
- [x] 검수 정보
	- [x] `/src/pages/approve/sections/approve-settings/settings-form` 하위에 동일 depths로 너무 많이 몰려있는 코드 파일의 분리
		- 기존 문제: 동일 depth에 파일이 평면적으로 배치되어 메인 파일 찾기 어려움
		- 개선 작업: settings-form-presenter에서 호출하는 각 presenter를 기능별 하위 디렉터리로 분리 (approve-settings 디렉터리 하위의 메인 파일이 무엇인지 한눈에 확인할 수 있도록 함)
			- consult/ - consult-info-presenter.tsx 이동
			- footer/ - settings-form-footer-presenter.tsx 이동
			- product-type/ - product-type-tabs-content-presenter.tsx, product-type-tabs-list-presenter.tsx 이동
			- ring/ - 링 설정 관련 파일 그룹화
				- campaign-ring/ - campaign-ring-container.tsx, campaign-ring-presenter.tsx 이동
				- default-ring/ - default-ring-container.tsx, default-ring-presenter.tsx 이동
				- specific-date-ring/ - specific-date-ring-container.tsx, specific-date-ring-presenter.tsx 이동
				- weekly-ring/ - weekly-ring-container.tsx, weekly-ring-presenter.tsx 이동
			- ring-settings-presenter.tsx, settings-form-ring.tsx는 /approve-settings/rings 하위에 메인 파일로 배치
			- settings-form-container.tsx, settings-form-presenter.tsx는 /approve-settings 하위에 메인 파일로 배치
			
	- [x] 오디오 재생 로직 통합
		- [x] 코어 로직인 `use-audio-actions`을 포함하여, 16개 컴포넌트파일 리팩토링 진행
		- [x] use-audio-action의 store 단순화
		- [x] `settings-form-container` 수정
			- [x] 오디오 재생함수 통합 (`playAudio()`)
			- [x] 코드 중복 제거
			- [x] 오디오 재생 시, toast알림 추가
		- [x] `settings-modal-container` 수정
			- [x] `audioRef` 관리주체를 container로 이동하고, 관리코드 개선
			- [x] 재생/정지 useEffect처리
			- [x] modal 닫을 시, 자동 정지
		- [x] `consult-info-presenter` 수정
			- [x] preview 재생로직 단순화
			- [x] 오디오 재생 이벤트함수로 `onPlayAudio` 사용
		- [x] 모든 Ring 컴포넌트에서, `audioBaseUrl`, `previewAudioSrc`, `onRingAudioPlay` 제거
			- [x] `playingAudioKey`, `onPlayAudio` 추가
			- [x] 오디오 재생로직 통일