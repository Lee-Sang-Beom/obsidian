#### Work (프로젝트: 영상 제작 프로젝트)
---
- 기타
	- AI 코딩 에이전트(Codex, Claude Code) 협업을 위한 프로젝트 문서/설정 체계 구축
	    - 기존 작성해둔 프로젝트 개발 가이드(디렉토리 구조, 컨벤션)를 기반으로 AGENTS.md 작성
	        - 디렉토리 구조, 의존 방향, 핵심 규칙, 절대 하지 않을 것 요약 정리
	        - CLAUDE.md는 기존처럼 @AGENTS.md를 참조하도록 유지하여 Codex/Claude Code가 동일 규칙 공유하도록 구성
	    - README.md 작성: 기술 스택, 실행 방법, 라우트 구조 정리
	    - .env.example 작성: 현재 사용 중인 환경변수 키 목록 정리
	    - .codex/config.toml 작성: 경로별 파일 수정 자동/수동 승인 정책 정의
	    - .agents/skills 3종 작성
	        - feature-scaffold: 새 feature 생성 시 디렉토리/파일 구조 가이드
	        - mapper-dto-check: mapper/DTO/도메인 타입 배치 규칙 점검
	        - pr-checklist: 커밋 전 lint/build/test 확인 및 커밋 메시지 컨벤션 체크
	    - docs/testing-convention.md 작성
	        - TDD/DDD 방식 도입에 따른 테스트 우선순위(선순위: selectors/rules/mappers 등 순수 함수), 테스트 파일 위치, mock/fixture 구분 기준 정리
	    - docs/commit-convention.md 작성
	        - feat/chore/refactor/fix/test/docs 커밋 타입 정의
	    - docs/work-log-convention.md 작성
	        - daily_logs/날짜/work-log.md 작성 형식(Goals, Completed work, Issues found, Decisions, Next steps) 및 작성 시점(작업 중 누적, 하루 마지막 커밋에 포함) 정의
	    - 문서 파일명 컨벤션 통일 (*-convention.md)


	- 프론트엔드
		- 프로젝트 아키텍처 문서(docs/architecture.md) 최신화
		    - 예전 develop guide 기준 내용(구 `src/service/queries`, mock 기반 Studio, 예전 chat 파일 경로) 제거
		    - 현재 구조 반영: A4 API, `/api/a4` proxy, feature 내부 query/mutation hook, `service/domain` DTO 경계, `shared/types` 기준
		- 프론트엔드 문서 디렉토리 재구성
		    - 기존 `docs/auth`, `docs/chat-steps`, `docs/deploy`를 `docs/frontend` 하위로 이동하여 기능 영역별로 예측 가능한 구조로 정리
		- `/chat` 영상 생성 단계 문서를 실제 코드 흐름에 맞춰 갱신
		    - 컴포넌트가 `steps/compose/components/layout|sections|dialogs|summary` 등으로 이동했으나 문서는 예전 경로를 가리키던 문제 수정
		    - `handleGenerate` 전환 순서, API config 위치, 프롬프트 5자 검증, `master_error` 실패 판정, result fallback 로직을 실제 코드 기준으로 재작성
		- Chat 실행/결과 단계 컴포넌트 분리 (리팩터링)
		    - `generation-workspace.tsx` → progress panel, summary panel, execution card, status display 등 역할 단위 컴포넌트로 분리
		    - `result-workspace.tsx` → video card, summary panel, empty state 등으로 분리
		    - entry 파일은 workflow 조립 중심으로 읽히도록 하고, UI 조각은 개별 파일 단위로 수정 가능하도록 구조 개선
		    - 분리 결과에 맞춰 관련 단계 문서(`chat-video-generation-process-overview.md`, `executing-workspace-flow.md`, `result-workspace-flow.md`) 파일 참조도 함께 갱신
		- Docker 배포 문서 확장
		    - 재시작 전용 문서(`frontend-docker-restart.md`)를 `frontend-use-docker.md`로 대체
		    - 최초 서버 준비, env 세팅, Dockerfile, compose, 최초 배포, 재시작, 재배포, 타 프로젝트 포트 확장 절차까지 포함
		    - 내부 포트 `3000` 고정, 외부 포트만 프로젝트별로 변경하는 운영 기준으로 정리 (`192.168.1.21` 서버 기준)
		- 신규 프론트엔드 프로젝트 초기 세팅 문서 작성 (`docs/frontend/init/frontend-init-setting.md`)
		    - 현재 package set, shadcn-ui 컴포넌트 설치 기준 정리
		    - `src` 구조, provider, WorkOS proxy, `lib/http`, `service/domain`, feature hook, shared/mapper/rule 배치, Docker 준비 체크리스트 포함
		- 미해결 항목
		    - `pnpm lint`에서 `src/components/shared/media/media-video-player.tsx`의 미사용 `preload` prop warning 1건 확인 (이번 작업 범위 밖이라 후속 처리 예정)


#### Work (프로젝트: 제주 렌터카)
---
- 기타
	- AI 코딩 에이전트 협업을 위한 프로젝트 기본 문서/설정 체계 구성
	    - `AGENTS.md`, `CLAUDE.md` 기반 작업 지침 정리
	    - `docs/architecture.md`, `docs/commit-convention.md`, `docs/testing-convention.md`, `docs/work-log-convention.md` 작성
	    - 프론트엔드 초기 세팅 기준 문서 작성 (`docs/init/frontend-init-setting.md`)
	    - Docker 배포 기준 문서 작성 (`docs/deploy/frontend-use-docker.md`)

- 프론트엔드
	- Next.js App Router 프로젝트 초기 구조 세팅
		- 기존 root `app/` 구조를 `src/app/` 구조로 이동
		- `@/*` alias를 `./src/*` 기준으로 정리
		- `src/components`, `src/features`, `src/hooks`, `src/lib`, `src/providers`, `src/service`, `src/shared` 기본 디렉토리 구조 생성
	- pnpm 기반 패키지 관리 체계 구성
		- `packageManager`를 `pnpm@10.6.3`으로 고정
		- `package-lock.json` 제거 및 `pnpm-lock.yaml` 생성
		- `dev`, `build`, `start`, `lint`, `test`, `format`, `format:check` script 정리
	- shadcn-ui 초기 세팅
		- `components.json` 생성
		- shadcn 전체 컴포넌트 일괄 설치
		- `src/components/ui` 하위에 공통 UI primitive 구성
		- shadcn 생성 코드 중 lint 규칙에 걸리는 `carousel`, `use-mobile` 처리 방식 보정
	- 공통 Provider 구성
		- TanStack Query용 `QueryProvider` 추가
		- 앱 전체 Provider 조립용 `AppProviders` 추가
		- Root Layout은 Server Component로 유지하고 provider만 감싸도록 구성
	- 공통 유틸 및 HTTP 계층 초기 구성
		- `src/lib/utils.ts`에 `cn` utility 추가
		- `src/shared/utils/idempotency-key.ts` 추가
		- `src/lib/http/http-client.ts`에 JSON/Blob 요청, HTTP Result, unwrap helper 구성
		- idempotency key 기본 테스트 추가
	- 환경변수 및 배포 파일 구성
		- `.env.example` 작성
		- `next.config.ts`에 standalone output 설정
		- `Dockerfile`, `.dockerignore`, `docker-compose.prod.yml` 작성
	- WorkOS 인증 제거
		- 프로젝트에서 로그인 기능을 사용하지 않기로 하여 WorkOS 관련 패키지 제거
		- `@workos-inc/authkit-nextjs`, `@workos-inc/node` 제거
		- `AuthKitProvider`, `src/proxy.ts`, `/auth/callback`, `/sign-in`, `/sign-up` route handler 삭제
		- `.env.example`, `.env.prod`, 관련 문서에서 WorkOS 환경변수 및 인증 기준 제거
	- 초기 검증
		- `pnpm lint` 통과
		- `pnpm build` 통과
		- `pnpm test` 통과
		- Docker build는 Docker daemon 미실행으로 확인 불가
