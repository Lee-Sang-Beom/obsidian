```
<Star AI - 프론트엔드>

- 계정 관리 access token refresh/retry 보강  
    : 계정 관리 보호 API 호출을 `useAccountAuthRequest` business hook으로 정리  
    : `AUTH_UNAUTHORIZED` 발생 시 `/api/auth/refresh` 호출 후 새 access token으로 원 요청을 1회 재시도하도록 구현  
    : refresh session이 유효하면 계정 목록/생성/수정/삭제 작업을 이어갈 수 있도록 처리  
    : refresh 실패 시에만 로컬 세션을 정리하도록 분리

- 계정 관리 인증 helper hook 컨벤션 정리  
    : 기존 root helper였던 `account-auth-token.ts`, `account-auth-request.ts` 제거  
    : 인증 요청 흐름을 `hooks/business/use-account-auth-request.ts` 기준으로 재정리  
    : hooks 하위 파일명/export 함수명은 `use-*` / `use*`로 시작해야 한다는 컨벤션 추가  
    : `AGENTS.md`, `docs/architecture.md`에 해당 컨벤션 문서화

- 계정 관리 business hook 디렉터리 세분화  
    : flat하게 나열되던 account business hooks를 역할별 하위 디렉터리로 이동  
    : `auth`, `forms`, `table`, `workflow` 구조로 분리  
    : `business/README.md`를 추가해 각 디렉터리의 책임을 정리  
    : 계정 관리 인증 요청, form, table, 전체 workflow hook을 더 쉽게 탐색할 수 있도록 개선

- 프론트 프롬프트 가드 강화  
    : 한국어 기반 직접 복제/우회 표현 감지를 강화  
    : 유명 IP denylist 없이도 `"OO" 그대로 만들어줘` 계열 요청을 차단하도록 보강  
    : 공백/특수문자 우회 표현을 잡기 위해 정규화 문자열 검사를 추가  
    : prompt injection, 개인정보, 직접 복제 요청 등 hard block rule을 확장

- 프롬프트 가드 score 구조 리팩터링  
    : 직접 복제 감지를 signal/score/threshold 기반으로 분리  
    : issue 결과에 `severity`를 추가해 hard block과 soft warning을 구분할 수 있도록 정리  
    : 향후 탐지 점수 조정과 warning 확장이 쉬운 구조로 개선

- 프롬프트 가드 soft warning 적용  
    : 과장 광고 표현은 제출 차단 대신 warning으로 표시하도록 처리  
    : 전화번호, 이메일, URL 등 연락 채널 입력은 hard block하지 않고 공식 예약 채널 확인 warning으로 안내  
    : `/videos/create` 추가 요청사항 입력 영역에 warning UI를 연결  
    : warning UI를 `PromptGuardWarningMessages` 공통 컴포넌트로 분리

- 프롬프트 가드 결과 의미 명확화  
    : `hasBlockIssues`, `hasWarningIssues` 파생 필드를 추가  
    : `ok`는 “이슈 없음”이 아니라 “hard block 없이 제출 가능함”을 의미하도록 정리  
    : warning만 있는 입력은 `ok: true`로 유지하고 세부 상태는 severity/파생 필드로 판단하도록 문서화


---

<Star AI - 백엔드>

- 완료 영상-계정 연결 구조 단순화  
    : 기존 영상 요청 전체에 유저 소유권을 붙이던 방식이 현재 목표보다 과하다고 판단  
    : 최종 완료된 master video asset에만 사용자 정보를 연결하는 방향으로 구조 재정리  
    : 기존 영상 생성/worker/asset 저장 로직은 유지하고, auth 계층에서만 최소 연결 정보를 관리하도록 정리
    
- 완료 영상 소유권 테이블 및 연결 로직 구현  
    : 영상 요청 생성 시 로그인 사용자를 임시 creator로 기록하는 테이블 추가  
    : master video 생성 완료 시 완료 영상 소유권 테이블로 연결 정보 이동  
    : 사용자별 완료 영상 목록/상세/재생 API(`/api/videos`) 추가  
    : 기존 request lifecycle API의 과한 소유권 guard 제거
    
- 완료 영상 접근 보호 보강  
    : 로그인 사용자가 소유한 완료 영상만 목록/상세/content 조회 가능하도록 처리  
    : 기존 `/v1/video-assets/{asset_id}/content`는 호환용 경로로 유지  
    : 프론트 사용자 화면에서는 보호 경로인 `/api/videos/{asset_id}/content`를 우선 사용하도록 문서화
    
- 장애 상황 복구 흐름 보완  
    : master video는 완성됐지만 DB 일시 오류로 ownership 연결이 실패할 수 있는 케이스 검토  
    : 이미 master video가 기록된 요청도 worker가 다시 지나가면 ownership callback을 재시도하도록 보강  
    : ownership 기록 실패가 영상 생성 완료 자체를 실패시키지 않도록 분리
    

```