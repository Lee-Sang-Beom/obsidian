
#### Work (AI Assiantance)
---
- [x] search-filter 기존기능 유지하면서 재설계 (1차)
	- 3가지 필터 타입 지원 (dateRange, select, input)
	- FilterField 인터페이스를 통한 동적 필터 구성
	- dateRange: Popover + Calendar 컴포넌트, 한국어 로케일, 미래날짜 비활성화
	- select: Select 컴포넌트, options 배열 기반 드롭다운
	- input: DebouncedInput 컴포넌트, Enter 키 검색 지원
	- 검색/초기화 버튼 표시 여부 제어 (showSearchButton, showResetButton)
	- onSearch, onReset 콜백 함수 지원
	- 반응형 레이아웃: PC(가로배치, 버튼 우측정렬), 모바일(세로배치, 버튼 하단)
	- children 슬롯으로 추가 컨텐츠(정렬 버튼 등) 삽입 가능
	- 각 필터별 label, placeholder, value, onChange 핸들러 개별 설정
	- md breakpoint 기준 반응형 전환 (모바일 xs, PC sm 텍스트)
	- 필터 너비: 모바일 100%, PC 350px 고정
	- fields 배열이 비어있으면 컴포넌트 렌더링하지 않음

- [x] search-filter 기존기능 유지하면서 재설계 (2차)
	- SearchFilter를 5개의 독립 컴포넌트로 분리 (date-range-filter, select-filter, input-filter, filter-field-wrapper, search-filter)
	- DateRangeFilter: Popover + Calendar 기반 날짜 범위 선택 UI, getDateRangeText 로직 내부화, yyyy-MM-dd 포맷 유지, 한국어 로케일 적용, 미래날짜 비활성화
	- SelectFilter: shadcn Select 컴포넌트 래핑, SelectOption 타입 정의, options 배열 맵핑, value/onChange 핸들러 전달
	- InputFilter: DebouncedInput 래퍼, FilterField 객체 구성하여 전달, onKeyDown 핸들러 전달, debounceMs 설정 지원
	- FilterFieldWrapper: label과 children을 받아 공통 레이아웃 제공, PC 350px 고정폭, 모바일 100% 가변폭, label 텍스트 크기 반응형 처리
	- RefactorSearchFilter (컨테이너): renderFilterContent에서 필터 타입별 컴포넌트 분기, renderField에서 FilterFieldWrapper로 감싸기, handleKeyDown 로직 유지하여 Enter키 검색 지원
	- PC 레이아웃: flex-wrap 가로배치, 필터 좌측정렬, 검색/초기화 버튼 우측정렬(ml-auto), items-end로 하단정렬
	- 모바일 레이아웃: space-y-2 세로배치, 버튼 flex gap-2로 가로 나란히, flex-1로 동일 너비
	- fields 배열 비어있으면 null 반환하여 아무것도 렌더링 안함
	- showSearchButton, showResetButton props로 버튼 표시 여부 개별 제어 가능
	- children prop으로 추가 컨텐츠(정렬 버튼 등) 하단에 삽입 가능
	- onSearch, onReset 콜백 함수 유지
	- FilterField 인터페이스 export하여 외부에서 타입 사용 가능
	- DebouncedInput의 handleKeyDown을 optional로 변경 필요 (타입 안정성)
	- InputFilter에서 FilterField 구성 시 options: undefined 지정 가능하도록 변경
	- 일단, 통합 전에는 기존 SearchFilter를 사용할 수 있도록 하고 구현 마지막단계에서 변경할 수 있도록 별도 `/component/filters` 디렉터리를 만들어서 분리진행 (파일명은 refactor-search-filter로 명명)

- [x] search-filter 기존기능 유지하면서 재설계 (3차: 기능/동작오류수정)
	- 12/04 11:30기준 업데이트 및 병합과정에서 발생한 수정요소에 따라 리팩토링된 search-filter를 한번더 재구성
	- 기능변경은 해당 기능에 맞게 변경하며, 그 외에는 아래와 같이 리팩토링을 진행
		- input-filter를 원본과 동일하게 전체 field객체를 props로 받게끔하여, debounced-input에 전체 객체를 전달함 (안정성)
		- 타입 안정성 개선: as DateRange 강제 캐스팅 제거 (date-range-filter.tsx)
		- refactor-search-filter에서 각 단일책임원칙을 수행하는 컴포넌트를 호출할 때 하위컴포넌트 내부에서 다시 field 객체를 재조립하면서 일부 속성이 누락되지않도록 전체 filed 객체전체를 보낼수있도록 함


- [x] 검수통계 UI 코드 container/presenter패턴으로 분할
	- `<StatisticsApprovePage/>` 
		- 라우트 레벨 페이지 컴포넌트
		- StatisticsApproveView를 렌더링하는 최상위 엔트리 포인트
		- 라우팅 시스템과 뷰 레이어를 연결하는 역할
	
	- `<StatisticsApproveView/>` 
		- 페이지 전체의 상태 관리 및 레이아웃 구성
		- 필터 상태(dateRange, nowPage, pageSize) 관리
		- 탭 상태(graph/list) 관리
		- 3개의 주요 섹션(필터, 요약, 콘텐츠)을 조합하여 전체 페이지 구성
		- Card 레이아웃으로 전체를 감싸고 각 섹션에 필요한 상태와 콜백 전달
	
	- `<StatisticsFilterContainer/>` 
		- 필터 관련 비즈니스 로직 처리
		- 날짜 범위 변경 시 nowPage를 1로 초기화하는 로직
		- 검색 버튼 클릭 시 nowPage를 1로 리셋하는 로직
		- 초기화 버튼 클릭 시 모든 필터를 기본값으로 되돌리는 로직
		- handleDateRangeChange, handleSearch, handleReset 핸들러 구현
	
	- `<StatisticsFilterPresenter/>` 
		- 순수 UI 렌더링, 필터 인터페이스 표시
		- SearchFilter 컴포넌트에 dateRange 필드 설정 전달
		- 날짜 범위 선택 UI 구성 (type: 'dateRange', label: '조회 기간')
		- Container로부터 받은 이벤트 핸들러를 SearchFilter에 연결
	
	- `<StatisticsSummaryContainer/>` 
		- 요약 통계 데이터 fetching 담당
		- formatDateRange 유틸로 DateRange를 API 형식으로 변환
		- useStatisticsSummaryQuery 훅으로 요약 데이터 조회 (seStartDate, seEndDate 파라미터 전달)
		- 로딩 상태 관리 및 Presenter에 전달
	
	- `<StatisticsSummaryPresenter/>` 
		- 요약 카드 3개(전체/보류/처리완료) UI 렌더링
		- createCardConfigs 함수로 카드 설정 데이터 생성 (title, percentage, count, 테마 색상)
		- calculatePercentage 유틸로 백분율 계산 (보류율, 완료율)
		- STAT_CARD_THEMES 상수에서 각 카드의 그라데이션, 아이콘, 색상 테마 가져오기
		- 로딩 시 CardLoader 스켈레톤 3개 표시
		- md:grid-cols-3 그리드 레이아웃으로 반응형 배치
	
	- `<StatisticsContentContainer/>` 
		- 탭별 콘텐츠 데이터 fetching 및 페이지네이션 로직 처리
		- formatDateRange로 날짜 범위 포맷팅
		- useStatisticsListQuery 훅으로 리스트 데이터 조회 (nowPage, pageSize, seStartDate, seEndDate)
		- handlePaginationChange로 페이지/페이지 사이즈 변경 시 필터 상태 업데이트
		- 데이터, 로딩/에러 상태, refetch 함수를 Presenter에 전달
	
	- `<StatisticsContentPresenter/>` 
		- 탭 전환 UI와 콘텐츠 영역 렌더링
		- StatisticsTabButtons 컴포넌트로 graph/list 탭 버튼 표시
		- activeTab에 따라 조건부 렌더링:
			- graph: StatisticsGraphPlaceholder (그래프 플레이스홀더)
			- list: StatisticsTable (통계 테이블)
		- Card 레이아웃으로 콘텐츠 영역 감싸기
	
	- `<StatisticsTable/>` 
		- 통계 리스트 테이블 렌더링
		- ResponsiveTable 래퍼 컴포넌트 사용
		- columnsStatistics 컬럼 정의 전달 (No, 처리일시, 처리상태, 구분, 건수)
		- 컬럼 정의 상세:
			- No: RowNumberCell로 순번 표시
			- 처리일시: CellTimeDisplay로 날짜/시간 포맷팅
			- 처리상태: Badge 컴포넌트로 표시 (처리완료/보류)
			- 구분: Badge variant 조건부 적용 (정확/기타문의/오류별 색상)
			- 건수: 숫자 포맷팅 + "건" 텍스트
		- 페이지네이션 데이터 및 핸들러 전달
		- 로딩/에러 상태 처리 및 refetch 기능 제공