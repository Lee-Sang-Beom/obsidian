

#### Work
---
- 검수 목록 리팩토링
	- 코드값에 따른 승인 팝업 배경색 수정
	- 코드값에 따른 분기처리 추가

- 검수 목록 리스트 페이지에 대한 코드분석 진행

- 통계 페이지 개발 및 리팩토링
	- [x]  `statisticsService` 공통 로직 추출 및 중복 제거
	    - URL 파라미터 생성 로직 중복 제거 → `buildQueryParams` 헬퍼 함수 추출
	    - 에러 처리 로직 중복 제거 → `handleApiResponse` 헬퍼 함수 추출
	    - 결과: 약 40줄 → 약 25줄로 감소 (37.5% 코드 감소)
	
	- [x]  `columnsStatistics` 컬럼 정의 개선
	    - 승인 상태(`approvalStatus`): `APPROVE_STATUS_OPTIONS` 활용하여 enum → 한글 label 변환 (예: `"PENDING"` → `"처리 중"`)
	    - 보류 구분(`approvalHoldType`): `APPROVE_HOLD_TYPE_OPTIONS` 활용하여 숫자 → 한글 label 변환 (예: `"6"` → `"기타"`)
	    - `null` 값은 `"-"` 표시 (Badge 없이 회색 텍스트)
	    - 건수는 천 단위 콤마 + "건" 단위 추가
	
	- [x]  작업 상태 필터(`workStatus`) 추가
	    - `WorkStatusType`: `'' | 'COMPLETED' | 'HOLD'` (전체/완료/보류)
	    - `StatisticsFilter` 인터페이스에 `workStatus` 필드 추가
	    - 기본값: `''` (전체)
	
	- [x]  View 레벨 상태 관리 (`StatisticsApproveView`)
	    - 데이터 조회 쿼리를 View 최상단에 배치 (중요)
	    - `useStatisticsSummaryQuery`: 요약 데이터 조회
	    - `useStatisticsListQuery`: 테이블 리스트 조회 (workStatus 포함)
	    - 이유: `filters` 상태 변경 시 React Query 의존성 자동 감지로 데이터 재조회
	    - 통계 카드 클릭 시: workStatus 필터 변경 + 페이지 리셋 + 리스트 탭 자동 전환
	
	- [x]  `StatisticsFilterContainer`: 비즈니스 로직 처리
	    - 날짜 범위 변경, 작업 상태 변경 시 페이지 1로 리셋
	    - 함수형 업데이트 사용 (`prev => ...`)으로 최신 상태 보장
	
	- [x]  `StatisticsFilterPresenter`: UI 렌더링
	    - `SearchFilter` 컴포넌트 활용
	    - 필드 2개: dateRange (기존) + workStatus (신규 Select)
	    - Select 옵션: 전체/완료/보류
	
	- [x]  카드 클릭 시 필터 연동
	    - 전체 카드 → `workStatus: ''`
	    - 보류 카드 → `workStatus: 'HOLD'`
	    - 처리완료 카드 → `workStatus: 'COMPLETED'`
	    - 선택된 카드는 파란색 링으로 시각적 강조
	    - 클릭 시 자동으로 리스트 탭 전환
	
	- [x]  `SummaryCard` 컴포넌트 개선
	    - `onClick`, `isSelected` props 추가
	    - `cursor-pointer` 클래스로 클릭 가능 표시
	    - 호버 효과 + 선택 상태 UI 추가
	
	- [x]  `StatisticsContentContainer` 데이터 조회 로직 제거
	    - 기존: Container에서 쿼리 호출 (문제: 상태 변경 시 즉시 반영 안 됨)
	    - 개선: View에서 쿼리 호출 후 결과를 props로 전달
	    - 역할 축소: 단순 props 전달 레이어로 변경
	
	- [x]  단일 진실 공급원 (Single Source of Truth) 원칙
	    - `filters` 상태를 View 레벨에서 단일 관리
	    - React Query가 `filters` 의존성 자동 추적
	    - 필터 변경 → 상태 업데이트 → React Query 자동 재조회
	
	- [x]  타입 정의 중앙화
	    - `dto.ts`: API 타입
	    - `types.ts`: 프론트엔드 내부 타입
	    - `constants.ts`: 상수 정의
	
	- [x]  헬퍼 함수 활용
		- statistics-service 파일에만 적용되어있지만, 차후 공통화가능한 요소는 묶어 처리할 예정
	    - `buildQueryParams`, `handleApiResponse`, `getApprovalStatusLabel`, `getApprovalHoldTypeLabel`
	
	- [x]  관심사 분리
	    - View: 라우팅, 상태 관리, 쿼리 호출
	    - Container: 비즈니스 로직, 이벤트 핸들링
	    - Presenter: 순수 UI 렌더링
	
	- [x]  즉각적인 피드백
	    - 카드 클릭 시 선택 상태 시각적 표시
	    - 자동 탭 전환으로 결과 즉시 확인
	    - 필터 변경 시 페이지 리셋
	    
	- [x]  일관된 필터 동작
	    - 모든 필터 변경 시 첫 페이지로 리셋
	    - 초기화 버튼으로 기본 상태 복원
	    - 로딩/에러 상태 UI 제공