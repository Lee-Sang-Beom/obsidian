
#### Work (프로젝트: ICMS)
---
- [x] 가공 컨텐츠 작성 진행
	- [x] 구현필요: 가공 컨텐츠 검색영역 css 개선
	- [ ] 확인필요: 검색 제대로 잘 되는지, 
	- [x] 검색필터 너비 스타일 입력 가능하도록 변경 및 리팩토링 진행

- [x] 보일러 플레이트
	- [x] 검색필터 너비 스타일 입력 가능하도록 변경 및 리팩토링 진행


```
이거 롤백해야함
const handleAuthFailure = useCallback(() => {  
  // setIsAuthenticated(false)  
  // useAuthStore.clearAuth()  
  // navigate(PATHS.LOGIN, { replace: true })  
  setIsAuthenticated(true);  
}, [navigate]);

아래로 롤백
const handleAuthFailure = useCallback(() => {  
  setIsAuthenticated(false)  
  useAuthStore.clearAuth()  
  navigate(PATHS.LOGIN, { replace: true })  
}, [navigate]);


```