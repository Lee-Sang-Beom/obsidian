```
일일 업무보고 요약

<제주 렌터카 - 백엔드>

- 영상 생성 요청을 유저 계정과 연결하는 방안 검토 및 설계안 작성
- 영상(마스터) 파일 접근 보안 범위 정리, 단계별 적용 계획 문서화

<제주 렌터카 - 프론트엔드>

- 계정 관리 화면의 데이터 변환 로직 구조 정리 및 목록/hook 코드 정리
- 계정 입력 폼 필수값 표시 추가, 권한 없을 때 화면 처리 보강, 이미지 업로드 검증 문서 정리

[오늘 한 일]

<제주 렌터카 - 백엔드>

- 영상-계정 연결 방안 검토  
    : 영상 생성 요청은 파일 형태로 저장되고 계정 정보는 별도 DB로 관리되어 서로 연결되어 있지 않은 구조임을 확인  
    : 기존 영상 생성 코드를 건드리지 않고 연결할 수 있는 방법 검토
    
- 연결 설계안 작성  
    : 3가지 방식(요청 파일에 직접 표시 / 계정 단위로 전환 / 별도 연결 테이블) 비교 후, 충돌 위험이 적은 "연결 테이블 추가" 방식을 권장안으로 결론  
    : 영상 요청 생성 시 유저와 연결하고, 조회/제출/영상 열람 시마다 소유자 확인하는 흐름 설계  
    : 여러 유저가 동시에 써도 중복 요청 방지 키가 충돌하지 않도록 유저별로 구분하는 방식 제안
    
- 영상 접근 보안 정리  
    : 기존 방식은 파일 고유번호만 알면 접근 가능해 소유권 확인이 약함을 확인  
    : 요청 단위로 소유자를 확인하는 새 접근 경로 추가 제안  
    : 타인 소유 데이터에 접근 시도 시 "없음"으로 처리해 존재 여부 자체를 노출하지 않도록 정리
    
- 단계별 적용 계획 문서화  
    : 1단계 연결 테이블 추가 및 생성/조회/제출 보호, 2단계 영상 열람 보호 endpoint 추가, 3단계 나머지 기능까지 보호 확장, 4단계 과거 데이터 정리 여부 검토  
    : 필수 테스트 항목, 위험 요소, 미결정 사항을 별도 문서로 정리
    

<제주 렌터카 - 프론트엔드>

- 계정 관리 코드 구조 정리  
    : 폼 입력값을 API 요청 형태로 바꾸는 로직을 별도 파일로 분리해, 화면 코드는 API 호출/새로고침 처리에만 집중하도록 정리  
    : 관련 변환 규칙(수정 시 빈 비밀번호는 제외, 새 비밀번호는 포함 등)을 테스트로 고정
    
- 계정 목록/hook 정리  
    : 실제로 쓰이지 않는 코드와 테스트 제거, 화면에서 안 쓰는 반환값 정리  
    : 코드 주석 문구를 실제 동작(추가+수정 공용)에 맞게 수정
    
- 계정 입력 폼 필수값 표시  
    : 필수 입력 항목 옆에 * 표시 추가  
    : 계정 생성 시엔 비밀번호 필수, 수정 시엔 비밀번호 선택(비워두면 기존 유지) 처리 분기
    
- 권한 없을 때 화면 처리 보강  
    : 목록 조회 권한이 없을 때 이전에 불러온 데이터가 화면에 남아있던 문제 해결 (데이터 숨기고 추가 버튼 비활성화)  
    : 삭제 실패와 조회 권한 실패를 구분해서 각각 다르게 처리
    
- 이미지 업로드 검증 문서 정리  
    : 현재 실제로 존재하지 않는 업로드 기능에 대한 내용이 문서에 섞여 있던 것을 정리해, 지금 프론트에서 실제로 하는 검증 범위만 남기도록 축약  
    : 업로드 기능 자체는 추후 별도 백엔드 문서에서 다룰 것으로 전제
    

[결과 / 현재 상태]

- 영상-계정 연결은 기존 구조를 크게 바꾸지 않는 "연결 테이블 추가" 방식으로 정리됨
- 영상 접근 보안은 요청 단위 보호 방식이 적합하다고 결론
- 계정 관리 화면 코드 구조 정리, 필수값 표시, 권한 차단 UX 개선 완료
- 이미지 업로드 문서는 프론트 담당 범위로 목적을 좁혀 정리
- 계정 관리 관련 변경사항은 테스트/검증 통과

[이슈 / 블로커]

- 없음

[내일 할 일 (09-18)]

- 계정 관리 화면 리팩터링 후보 검토  
    : 에러/로딩/목록 표시 분기 가독성 점검  
    : 공통 폼 옵션이 늘어나고 있어 별도 설정으로 분리할지 검토
    
- 계정 관리 UX 수동 검증  
    : 권한별(관리자/운영자) 목록 접근, 생성, 수정, 삭제 동작 확인  
    : 에러 상황별 화면 표시와 버튼 상태 확인  
    : 캐시 남은 상태에서 권한 변경 시 이전 데이터 노출 안 되는지 재확인
    
- 이미지 업로드 검증 후속 정리 검토  
    : 특수 파일 형식(비정상 확장자/포맷 등) 허용 범위 정책 검토  
    : 백엔드 처리 시 필요한 에러 형식을 별도 문서로 분리할지 검토  
    : 문서 위치와 다른 영상 생성 문서 간 충돌 여부 확인
    
- 영상-계정 연결 백엔드 설계 후속 검토  
    : 연결 테이블 설계안 기준 필요한 API 변경 범위 정리  
    : 영상 열람 보호 방식이 프론트 재생 흐름에 미치는 영향 확인  
    : 유저별 목록 조회로 전환 시 필요한 프론트 변경 후보 정리
```

