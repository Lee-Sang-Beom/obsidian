

#### Work (프로젝트: AI Assistance)
---
- [x] src/components 개선
	- `component/ui` 하위 `error.tsx`, `loader.tsx` 추가 (에러 UI, 로딩 UI)
	- `response-modal.tsx`를 `modals` 디렉터리 하위로 이동

- [x] src/domain 추가
	- `/domain/auth`추가: auth 도메인 constant 정보와, type 정보를 저장 (type의 경우`dto.ts`, `types.ts`로 분리하여 저장)

- [x] src/hooks 개선
	- 하위 디렉터리를 queries, mutataions, ui, modals, business 등으로 분리하고 파일 이동

- [x] pages/auth 하위 코드 리팩토링
    - 디렉터리 구조 재설계
        - View/Section/Component 3계층 구조로 분리하여 명확한 책임 분리
        - Container/Presenter 패턴 적용으로 로직과 UI 분리
        - 기존 단일 파일 구조를 역할별로 모듈화하여 유지보수성 향상
          
    - View 계층 구현 (views/auth/login/)
        - login-view.tsx: 페이지 레벨 최상위 컴포넌트로 라우팅과 직접 연결
        - 모바일/데스크톱 환경 감지 및 애니메이션 타이밍 제어 (3.5초, 3.6초 딜레이)
        - LoginBannerContainer와 LoginFormContainer 조합하여 완전한 로그인 화면 구성
        - React 19의 Activity 기능을 활용한 조건부 렌더링 처리
          
    - Section 계층 구현 (sections/auth/)
        - login-form/ 섹션: 로그인 폼 전체를 담당하는 기능 단위 블록
            - login-form-container.tsx: 폼 상태 관리, 로컬스토리지 처리, API 호출 로직 담당
            - login-form-presenter.tsx: 폼 UI 구조 렌더링, 하위 컴포넌트 조합
        - login-banner/ 섹션: 좌측 브랜드 배너를 담당하는 독립적인 기능 블록
            - login-banner-container.tsx: 시계 업데이트, Feature 슬라이더 상태 관리
            - login-banner-presenter.tsx: 복잡한 애니메이션과 브랜드 요소 렌더링
        - constants.ts: Feature 데이터 등 섹션 간 공유 상수 정의
          
    - Component 계층 구현 (components/auth/form/)
        - 순수 UI 컴포넌트로 재사용 가능하고 도메인 독립적인 단위로 분리
        - login-header-presenter.tsx: 로그인 폼 헤더 (아이콘, 타이틀, 설명)
        - login-id-field-presenter.tsx: 아이디 입력 필드 UI
        - login-password-field-container.tsx: 비밀번호 표시/숨김 상태 관리
        - login-password-field-presenter.tsx: 비밀번호 입력 필드 UI
        - login-options-presenter.tsx: 아이디 저장 체크박스 UI
        - login-button-presenter.tsx: 로그인 버튼 UI (로딩 상태 표시)
        - login-footer-presenter.tsx: 폼 하단 안내 메시지
    
	- Container/Presenter 패턴 적용
        - Container 컴포넌트: 비즈니스 로직, 상태 관리, 데이터 fetching 담당
            - login-form-container.tsx: useForm, localStorage, API mutation 처리
            - login-banner-container.tsx: 시간/Feature 상태 관리
            - login-password-field-container.tsx: 비밀번호 표시 토글 상태
        - Presenter 컴포넌트: 순수 UI 렌더링, props를 통해 데이터 수신
            - 모든 -presenter.tsx 파일: UI 렌더링만 담당, 상태를 직접 관리하지 않음
            - 재사용 가능하고 테스트하기 쉬운 구조
    
	- 타입 안정성 강화
        - Control 타입을 any에서 `Control<LoginFormValues>`로 명시적 정의
        - 모든 Props 인터페이스 명확히 정의하여 타입 체크 강화
        - Feature 타입을 constants.ts에서 export하여 재사용
    
    - 코드 중복 제거 및 관심사 분리
        - FEATURES 배열을 constants.ts로 추출하여 중앙 관리
        - 하드코딩된 매직넘버(3)를 FEATURES_LENGTH 상수로 대체
        - 각 컴포넌트가 단일 책임 원칙(SRP)을 준수하도록 분리
 
    - 유지보수성 개선
        - 파일명에 -container/-presenter 접미사로 역할 명확히 구분
        - 각 파일이 200줄 이하로 관리 가능한 크기로 분할
        - 수정 시 영향 범위가 명확하여 사이드 이펙트 최소화
        - 새로운 필드 추가 시 해당 컴포넌트만 생성하면 되는 확장성ㅁ