
#### Work (프로젝트: AI Assistance)
---
- [x] statistics/ 하위의 디렉터리 요소 디렉터리/코드구조 리팩토링 진행
	- **Container/Presenter 패턴 적용**
		- 모든 섹션을 Container/Presenter로 분리하여 관심사 분리
		- Container는 비즈니스 로직, 상태 관리, 데이터 fetching 담당
		- Presenter는 순수 UI 렌더링만 담당 (props 기반)
		- 파일명 접미사를 `-container.tsx`, `-presenter.tsx`로 통일
	- **계층적 디렉터리 구조 재설계**
		- `views/statistics/` - 페이지 레벨 뷰 (라우팅 연결, 상태 관리)
		- `sections/statistics/` - 기능 단위 섹션 (필터, 요약, 콘텐츠)
		- `sections/statistics/*/components/` - 섹션별 하위 컴포넌트
		- 도메인 로직과 UI를 명확히 분리한 3-tier 구조
	- **statistics-filter 섹션 분리**
		- `statistics-filter-container.tsx` 생성
			- 필터 상태 관리 및 이벤트 핸들링
			- 날짜 범위 변경, 검색, 초기화 로직 처리
			- 페이지 상태(nowPage)를 부모로부터 props로 전달받아 관리
		- `statistics-filter-presenter.tsx` 생성
			- SearchFilter 컴포넌트를 래핑한 순수 UI
			- dateRange, 이벤트 핸들러를 props로 전달받음
			- 검색 필드 설정(type, key, label, placeholder) 정의
	- **statistics-summary 섹션 분리 및 컴포넌트화**
		- `statistics-summary-container.tsx` 생성
			- useStatisticsSummaryQuery를 통한 요약 데이터 fetching
			- dateRange를 props로 받아 formatDateRange로 변환
			- 로딩 상태 관리 및 Presenter에 전달
		- `statistics-summary-presenter.tsx` 생성
			- 3개 요약 카드 그리드 레이아웃 렌더링
			- createCardConfigs로 전체/보류/처리완료 카드 설정 생성
			- calculatePercentage로 백분율 계산 로직 포함
			- 로딩 시 스켈레톤 UI 표시
		- `components/summary-card.tsx` 생성
			- 개별 요약 카드 UI 컴포넌트
			- 아이콘, 제목, 백분율, 건수, 진행바 표시
			- 호버 효과 및 그라데이션 애니메이션 적용
			- CardConfig 타입을 props로 받아 동적 렌더링
		- `components/summary-card-skeleton.tsx` 생성
			- 로딩 중 표시될 스켈레톤 UI
			- Skeleton 컴포넌트를 활용한 3개 영역 표시
	- **statistics-content 섹션 분리 및 컴포넌트화**
		- `statistics-content-container.tsx` 생성
			- useStatisticsListQuery를 통한 리스트 데이터 fetching
			- filters(dateRange, nowPage, pageSize)를 props로 받아 처리
			- activeTab 상태를 부모로부터 props로 전달받음
			- 페이지네이션 변경 이벤트를 부모의 onFiltersChange로 전달
		- `statistics-content-presenter.tsx` 생성
			- 탭 버튼과 콘텐츠 영역을 포함한 레이아웃
			- activeTab에 따라 그래프/리스트 조건부 렌더링
			- 하위 컴포넌트(TabButtons, GraphPlaceholder, Table)로 구성
		- `components/statistics-tab-buttons.tsx` 생성
			- 그래프/리스트 탭 전환 버튼 UI
			- activeTab에 따른 variant, 스타일 변경
			- lucide-react 아이콘(BarChart3, List) 활용
		- `components/statistics-graph-placeholder.tsx` 생성
			- 그래프 탭 선택 시 표시되는 플레이스홀더
			- 향후 실제 차트 컴포넌트로 교체 예정인 영역
			- 안내 메시지와 아이콘 표시
		- `components/statistics-table/statistics-table.tsx` 생성
			- ResponsiveTable 컴포넌트 래퍼
			- 데이터, 로딩/에러 상태, 페이지네이션 props 전달
			- columnsStatistics를 import하여 컬럼 정의 적용
		- `components/statistics-table/columns-statistics.tsx` 분리
			- 테이블 컬럼 정의(ColumnDef) 파일로 독립
			- No, 처리일시, 처리상태, 구분, 건수 컬럼 정의
			- Badge variant 로직(처리상태, 카테고리별 색상) 포함
			- RowNumberCell, CellTimeDisplay 등 재사용 컴포넌트 활용
	- **상태 관리 방식 개선**
		- 초기 Zustand Store 적용 시도 후 제거 결정
			- 단일 페이지 내 로컬 상태로 충분하다고 판단
			- 불필요한 전역 상태 관리로 인한 복잡도 증가 방지
			- 페이지 언마운트 시 자동 초기화 보장
		- useState로 페이지 레벨에서 상태 통합 관리
			- filters: { dateRange, nowPage, pageSize }
			- activeTab: 'graph' | 'list'
			- props drilling을 통한 명확한 데이터 흐름 유지
	- **statistics-page.tsx 단순화**
		- 페이지 컴포넌트는 상태 관리와 섹션 조합만 담당
		- 3개 섹션(Filter, Summary, Content)을 Container로 import
		- 각 Container에 필요한 상태와 이벤트 핸들러를 props로 전달
		- Card, CardHeader, CardTitle로 페이지 레이아웃 구성
	- **파일 및 함수 주석 추가**
		- 모든 컴포넌트에 JSDoc 스타일 주석 추가
		- 각 파일의 역할과 책임을 명시
		- 함수 파라미터 및 반환값 설명 포함

 - [x] pages/admin 경로(권한 관리 계정 목록) 리팩토링 코드 수정: 테이블 컬럼 정의에서 React Hooks 규칙 위반 문제 해결
	- 각 Cell 렌더러를 columns파일 내 별도 React 함수 컴포넌트로 분리
	- CompanyNameCell, LoginIdCell, NameCell, EmailCell, UserStatusCell, MenuPermissionsCell에 대해 별도 파일로 분할할 필요가 없다고 판단
	- 각 Cell 컴포넌트 내부에서 필요한 React Hooks 호출

 - [x] pages/admin 경로(권한 관리 계정 목록) 리팩토링 코드 수정 
	 - 단일 컴포넌트로는 코드가 복잡해보임
	 - 단일 admin-list-view를 admin-list-view-container, admin-list-view-presenter로 분리
		 - admin-list-view-container: useAdminListQuery, useOpenCreateAdminModal 호출 후, AdminListViewPresenter로 data props 전달
		 - admin-list-view-presenter: admin-list-view-container에서 받은 props로 신규계정 추가 이벤트리스너 연결, admin-table-section에 데이터 전달
		 - admin-table-section의 구조 간소화
			 - 테이블에 출력 관련 컴포넌트가 궁극적으로 admin-list-view-container에서 응답받은 데이터를 표시하는 방식으로 코드를 간단하게 바꾸는 것이 목적
			 - admin-table-section: admin-table-section-container로 컴포넌트 네이밍 변경 및 admin-table-section-presenter로 props 그대로 전달 (데이터 추출과정이 따로 필요없음)
			 -  admin-table-section-presenter: `<ResponsiveTable/>`에 적합한 데이터정보를 전달하여 '테이블을 표시'하는 역할 수행
			 - 두 파일 모두 admin-list-view-container에서 생성한 데이터를 '전달만 받도록' 변경

- [x] layout/sidebar 하위의 앱 사이드바 디렉터리/코드구조 리팩토링 진행
	- **Container/Presenter 패턴 적용**
		- 모든 사이드바 컴포넌트를 Container/Presenter로 분리하여 관심사 분리
		- Container는 상태 관리, 이벤트 핸들링, 권한 체크, API 호출 담당
		- Presenter는 순수 UI 렌더링만 담당 (props 기반)
		- 파일명 접미사를 `-container.tsx`, `-presenter.tsx`로 통일
	- **계층적 디렉터리 구조 재설계**
		- `components/layout/sidebar/` - 사이드바 메인 구조
		- `components/layout/sidebar/components/` - 사이드바 하위 컴포넌트
		- `components/layout/sidebar/components/menu-item/` - 메뉴 아이템 관련
		- `components/layout/user-menu/` - 사용자 메뉴 분리
		- 도메인 로직과 UI를 명확히 분리한 구조
	- **app-sidebar 메인 컴포넌트 분리**
		- `app-sidebar-container.tsx` 생성
			- useSidebar, useIsMobile, useAudio 훅 사용
			- useMyInfoQuery로 사용자 정보 fetching
			- useLogout으로 로그아웃 처리
			- openGroups 상태 관리 (메뉴 열기/닫기)
			- handleLogout: 모바일 사이드바 닫기 + 로그아웃 처리
			- handleToggleGroup: 메뉴 그룹 토글 상태 관리
			- handleMenuClick: 모바일 사이드바 닫기 + refetch + 오디오 중지
			- useAccountCheck로 계정 상태 체크 (에러 및 비활성)
		- `app-sidebar-presenter.tsx` 생성
			- Sidebar 레이아웃 구성 (Header, Content, Footer, Rail)
			- visibleMenuSections 필터링 (disabled 제외)
			- SidebarLogo, MenuSection, UserMenuContainer 조합
			- user, userPermissions, openGroups 등을 props로 전달
	- **sidebar-logo 컴포넌트 분리**
		- `sidebar-logo.tsx` 생성
			- AI CMS 로고 및 타이틀 표시
			- framer-motion 애니메이션 효과 (회전, 페이드인)
			- 독립적인 순수 UI 컴포넌트로 재사용 가능
			- whileHover로 로고 회전 효과
			- initial/animate로 텍스트 페이드인 효과
	- **menu-section 컴포넌트 분리**
		- `menu-section.tsx` 생성
			- SidebarGroup, SidebarGroupLabel, SidebarGroupContent 구성
			- NavMenu 타입의 section을 props로 받음
			- 메뉴 그룹 단위로 아이템 렌더링
			- section.data를 map하여 MenuItemContainer에 전달
			- groupLabel 조건부 렌더링
	- **menu-item 섹션 분리 및 컴포넌트화**
		- `menu-item-container.tsx` 생성
			- useNavigate로 페이지 이동 처리
			- hasPermission으로 권한 체크
			- handleClick: 권한 없으면 toast.error, 있으면 navigate + onMenuClick
			- handleToggle: 메뉴 그룹 열기/닫기 처리
			- isOpen, hasAccess 상태 계산
			- MenuItemPresenter에 props 전달
		- `menu-item-presenter.tsx` 생성
			- 하위 메뉴(item.items) 유무에 따른 분기 렌더링
			- 하위 메뉴 있으면 CollapsibleMenu 렌더링
			- 하위 메뉴 없으면 SingleMenu 렌더링
			- 순수 UI 분기 로직만 담당
		- `collapsible-menu.tsx` 생성
			- Collapsible, CollapsibleTrigger, CollapsibleContent 활용
			- SidebarMenuButton으로 메뉴 헤더 구성
			- isOpen에 따른 조건부 스타일 (bg-accent/40, shadow-sm)
			- ChevronRight 아이콘 회전 애니메이션 (0도 → 90도)
			- AnimatePresence로 하위 메뉴 열기/닫기 애니메이션
			- SubMenuItem을 map하여 렌더링
			- 각 서브메뉴에 stagger 애니메이션 (delay: index * 0.05)
		- `single-menu.tsx` 생성
			- 단일 링크 메뉴 버튼 UI
			- hasAccess에 따른 조건부 스타일
			- 권한 없으면 opacity-50, cursor-not-allowed
			- 권한 있으면 호버 시 그라데이션 배경
			- 아이콘 호버 효과 (scale: 1.1)
			- active:scale-95로 클릭 피드백
		- `sub-menu-item.tsx` 생성
			- 3차 메뉴 아이템 컴포넌트
			- useNavigate, toast, hasPermission 사용
			- handleClick: 권한 체크 후 navigate + onMenuClick
			- framer-motion 진입/퇴장 애니메이션 (x: -5 → 0)
			- 점(dot) 인디케이터 표시 (mr-3 h-1.5 w-1.5 rounded-full)
			- 권한 있을 때만 우측 점 표시 (whileHover: opacity 1)
			- 권한 없으면 opacity-50, hover 효과 제거
	- **user-menu 섹션 분리 및 컴포넌트화**
		- `user-menu-container.tsx` 생성
			- useState로 드롭다운 isOpen 상태 관리
			- useNavigate로 계정 관리 페이지 이동
			- handleAccountManagement: PATHS.ACCOUNT.MANAGEMENT로 navigate
			- user null 체크하여 early return
			- UserMenuPresenter에 props 전달
		- `user-menu-presenter.tsx` 생성
			- DropdownMenu, DropdownMenuTrigger, DropdownMenuContent 구성
			- UserTriggerButton을 트리거로 사용
			- UserDropdownContent를 콘텐츠로 사용
			- align="end", sideOffset={8} 설정
			- onOpenChange로 isOpen 상태 동기화
		- `user-trigger-button.tsx` 생성
			- forwardRef로 래핑하여 DropdownMenuTrigger의 asChild와 호환
			- UserAvatar 및 사용자 정보 표시 (name, email)
			- ChevronDown 회전 애니메이션 (isOpen ? 180 : 0)
			- 그라데이션 배경 (from-blue-50 to-indigo-50)
			- 호버 효과 (scale: 1.02, shadow-md)
			- whileTap으로 클릭 피드백 (scale: 0.98)
			- displayName 설정으로 디버깅 용이
		- `user-avatar.tsx` 생성
			- size prop으로 sm/md/lg 크기 조절 (h-8/h-10/h-12)
			- showStatus prop으로 활성/비활성 상태 표시 여부 결정
			- userInitial 계산 (name.charAt(0).toUpperCase())
			- isActive 계산 (userStatus === 'ACTIVE')
			- statusColor 계산 (green-500 / gray-400)
			- Avatar 컴포넌트에 ring-2, ring-blue-200 스타일
			- 상태 인디케이터를 absolute 위치로 배치 (-right-1 -bottom-1)
		- `user-dropdown-content.tsx` 생성
			- DropdownMenuLabel로 사용자 정보 카드 구성
			- UserAvatar (size="lg") 표시
			- 사용자 이름, 전화번호, 회사명 표시
			- formatPhoneNumber로 전화번호 포맷팅
			- Phone, Building2 아이콘 사용
			- DropdownMenuItem으로 계정 관리 메뉴 (User 아이콘)
			- DropdownMenuItem으로 로그아웃 메뉴 (LogOut 아이콘)
			- 각 메뉴별 아이콘 배경색 및 호버 효과
			- framer-motion 애니메이션 (opacity, y 변화)
	- **커스텀 훅 통합 및 개선**
		- `use-account-check.ts` 생성
			- 기존 분리되어 있던 두 개의 계정 체크 로직 통합
			- 체크 1: API 조회 에러 발생 시 로그아웃 처리
			- 체크 2: userStatus === 'INACTIVE' 시 로그아웃 처리
			- useEffect 내부에서 user, error, onLogout 의존성 관리
			- 하나의 훅으로 모든 계정 상태 체크 처리
		- 기존 `use-inactive-account-check.ts` 제거
			- 불필요한 분리 제거로 코드 일관성 향상

	- **파일 및 함수 주석 추가**
		- 모든 컴포넌트에 JSDoc 스타일 주석 추가
		- 각 파일의 역할과 책임을 명시
		- Props 인터페이스에 설명 포함
		- 중요 로직에 인라인 주석 추가
	- **props drilling 최소화**
		- Container에서 Presenter로 필요한 props만 전달
		- 불필요한 중간 전달 제거
		- 명확한 데이터 흐름 유지
	- **성능 최적화**
		- useCallback으로 핸들러 메모이제이션
		- 불필요한 리렌더링 방지
		- 조건부 렌더링 최적화 (early return)


---
#### Work (프로젝트: 보일러 플레이트 CMS Branch)
- [x] layout/sidebar 하위의 보일러플레이트 CMS branch 사이드바 디렉터리/코드구조 리팩토링 진행
	- **Container/Presenter 패턴 적용**
		- 모든 사이드바 컴포넌트를 Container/Presenter로 분리하여 관심사 분리
		- Container는 상태 관리, 이벤트 핸들링, 인증 처리 담당
		- Presenter는 순수 UI 렌더링만 담당 (props 기반)
		- 파일명 접미사를 `-container.tsx`, `-presenter.tsx`로 통일
	- **메뉴 검색 기능 제거**
		- searchTerm 관련 상태 제거
		- filteredMenus 필터링 로직 제거
		- SearchInput 컴포넌트 제거
		- useEffect 검색 필터링 로직 제거
		- 단순화된 메뉴 표시로 성능 향상
	- **app-sidebar 메인 컴포넌트 분리**
		- `app-sidebar-container.tsx` 생성
			- useNavigate로 페이지 이동
			- useSidebar, useIsMobile, useAudio 훅 사용
			- useAuthStore로 인증 상태 관리
			- openGroups 상태 관리 (메뉴 열기/닫기)
			- handleToggleGroup: 메뉴 그룹 토글 상태 관리
			- handleMenuClick: 모바일 사이드바 닫기 + 오디오 중지
			- handleLogout: 오디오 중지 + clearAuth + LOGIN 페이지로 이동
		- `app-sidebar-presenter.tsx` 생성
			- Sidebar 레이아웃 구성 (Header, Content, Footer, Rail)
			- menus를 Object.values로 순회하여 MenuSection 렌더링
			- SidebarLogo, MenuSection, UserMenuContainer 조합
			- openGroups, onToggleGroup, onMenuClick, onLogout props 전달
	- **sidebar-logo 컴포넌트 분리**
		- `sidebar-logo.tsx` 생성
			- PN CMS 로고 및 SubPN 타이틀 표시
			- framer-motion 애니메이션 효과 (회전, 페이드인)
			- 독립적인 순수 UI 컴포넌트로 재사용 가능
			- whileHover로 로고 회전 효과
			- initial/animate로 텍스트 페이드인 효과
			- 프로젝트명 커스터마이징 가능 (PN, SubPN)
	- **menu-section 컴포넌트 분리**
		- `menu-section.tsx` 생성
			- SidebarGroup, SidebarGroupLabel, SidebarGroupContent 구성
			- NavSection 타입의 section을 props로 받음
			- section.groupLabel.length > 0 조건부 렌더링
			- section.data를 map하여 MenuItemContainer에 전달
			- 메뉴 그룹 단위로 아이템 렌더링
	- **menu-item 섹션 분리 및 컴포넌트화**
		- `menu-item-container.tsx` 생성
			- openGroups에서 isOpen 상태 추출
			- handleToggle: 메뉴 그룹 열기/닫기 처리
			- MenuItemPresenter에 props 전달
			- 권한 체크 로직 없음 (보일러플레이트)
		- `menu-item-presenter.tsx` 생성
			- 하위 메뉴(item.items) 유무에 따른 분기 렌더링
			- 하위 메뉴 있으면 CollapsibleMenu 렌더링
			- 하위 메뉴 없으면 SingleMenu 렌더링
			- 순수 UI 분기 로직만 담당
		- `collapsible-menu.tsx` 생성
			- Collapsible, CollapsibleTrigger, CollapsibleContent 활용
			- SidebarMenuButton으로 메뉴 헤더 구성
			- isOpen에 따른 조건부 스타일 (bg-accent/40, shadow-sm)
			- ChevronRight 아이콘 회전 애니메이션 (0도 → 90도)
			- AnimatePresence로 하위 메뉴 열기/닫기 애니메이션
			- SubMenuItem을 map하여 렌더링
			- 각 서브메뉴에 stagger 애니메이션 (delay: index * 0.05)
		- `single-menu.tsx` 생성
			- SidebarMenuButton with Link 조합
			- React Router Link 컴포넌트 사용
			- 단일 링크 메뉴 UI
			- 호버 시 그라데이션 배경
			- 아이콘 호버 효과 (scale: 1.1)
			- 우측 점 인디케이터 (opacity 애니메이션)
			- active:scale-95로 클릭 피드백
		- `sub-menu-item.tsx` 생성
			- 3차 메뉴 아이템 컴포넌트
			- React Router Link 사용
			- framer-motion 진입/퇴장 애니메이션 (x: -5 → 0)
			- 점(dot) 인디케이터 표시 (mr-3 h-1.5 w-1.5)
			- 우측 점 표시 (whileHover: opacity 1)
			- searchTerm 하이라이트 로직 제거
	- **user-menu 섹션 분리 및 컴포넌트화**
		- `user-menu-container.tsx` 생성
			- useState로 isMounted, secretKey, isOpen 상태 관리
			- useEffect로 마운트 상태 및 토큰 로드
			- useAuthStore.getToken()으로 인증 토큰 확인
			- secretKey 없으면 early return (null)
			- UserMenuPresenter에 props 전달
		- `user-menu-presenter.tsx` 생성
			- DropdownMenu, DropdownMenuTrigger, DropdownMenuContent 구성
			- UserTriggerButton을 트리거로 사용
			- UserDropdownContent를 콘텐츠로 사용
			- align="end", sideOffset={8} 설정
			- onOpenChange로 isOpen 상태 동기화
		- `user-trigger-button.tsx` 생성
			- forwardRef로 래핑하여 DropdownMenuTrigger의 asChild와 호환
			- 관리자 아바타 표시 (이니셜 "A")
			- 온라인 상태 인디케이터 (green-500)
			- "관리자", "Admin User" 정보 표시
			- ChevronDown 회전 애니메이션 (isOpen ? 180 : 0)
			- 그라데이션 배경 (from-blue-50 to-indigo-50)
			- 호버 효과 (scale: 1.02, shadow-md)
			- whileTap으로 클릭 피드백 (scale: 0.98)
			- displayName 설정으로 디버깅 용이
		- `user-dropdown-content.tsx` 생성
			- DropdownMenuLabel로 관리자 정보 카드 구성
			- 아바타 (size 12, 이니셜 "A")
			- 관리자 이름, 이메일 (admin@admin.com)
			- 온라인 상태 표시 (green dot + "온라인" 텍스트)
			- 프로필 메뉴 (User 아이콘, toast.info)
			- 설정 메뉴 (Settings 아이콘, toast.info)
			- 로그아웃 메뉴 (LogOut 아이콘, onLogout 호출)
			- 각 메뉴별 아이콘 배경색 및 호버 효과
			- framer-motion 애니메이션 (opacity, y, delay)
	- **기존 기능 100% 유지 검증**
		- 사이드바 로고 애니메이션 동일
		- 메뉴 그룹 열기/닫기 동작 동일
		- 모바일 사이드바 자동 닫기 동일
		- 오디오 중지 동일
		- 사용자 메뉴 드롭다운 동일
		- 로그아웃 처리 동일 (clearAuth + navigate)
		- 인증 토큰 체크 동일
		- Link 네비게이션 동일
	- **검색 기능 제거로 인한 단순화**
		- searchTerm 상태 제거로 불필요한 리렌더링 감소
		- filteredMenus 계산 로직 제거로 성능 향상
		- useEffect 의존성 감소
		- MenuItem에 searchTerm props 제거
		- SubMenuItem에 isHighlighted props 제거
		- 메뉴 구조가 더 명확해짐
	- **타입 안정성 강화**
		- NavItem, NavSubItem, NavSection 타입 활용
		- Props 인터페이스 명시적 정의
		- forwardRef 타입 명시 (HTMLButtonElement)
		- React.MouseEvent 타입 명시
	- **코드 재사용성 향상**
		- SidebarLogo: 독립 컴포넌트로 다른 레이아웃에서도 사용 가능
		- SubMenuItem: 순수 UI 컴포넌트로 재사용 가능
		- SingleMenu: props 기반으로 조합 가능
		- UserTriggerButton: forwardRef로 다른 드롭다운에서도 사용 가능
	- **파일 및 함수 주석 추가**
		- 모든 컴포넌트에 JSDoc 스타일 주석 추가
		- 각 파일의 역할과 책임을 명시
		- Props 인터페이스에 설명 포함
	- **애니메이션 효과 유지**
		- framer-motion을 활용한 모든 애니메이션 유지
		- 로고 회전 효과 (whileHover, repeat: Infinity)
		- 메뉴 열기/닫기 애니메이션 (height, opacity)
		- ChevronRight 회전 애니메이션 (rotate: 0 → 90)
		- 서브메뉴 진입 애니메이션 (x: -5 → 0)
		- 드롭다운 진입 애니메이션 (y: -10 → 0)
		- 호버 효과 (scale, opacity 변화)
		- 클릭 피드백 (whileTap: scale 0.98)
	- **보일러플레이트 특화 기능**
		- useAuthStore 통합
		- 토큰 기반 인증 체크
		- 간단한 관리자 UI (권한 시스템 없음)
		- toast.info로 준비중 기능 표시
		- 프로젝트명 커스터마이징 가능
	- **props drilling 최소화**
		- Container에서 Presenter로 필요한 props만 전달
		- 불필요한 중간 전달 제거
		- 명확한 데이터 흐름 유지
	- **성능 최적화**
		- useCallback으로 핸들러 메모이제이션
		- 검색 필터링 제거로 렌더링 최적화
		- 조건부 렌더링 최적화 (early return)