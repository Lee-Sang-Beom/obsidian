#### Work (프로젝트: 법률 AI 검증코어 프론트엔드)
---
- 공통 구조/클린아키텍처 리팩터링
	- 앱 셸, 채팅, 테스트베드 컴포넌트 구조를 역할 기준으로 재분류
	- `src/components/common` 하위를 `brand`, `code`, `metrics`, `table`, `typography`로 정리
	- mapper 내부 표시용 타입을 외부 export하지 않도록 private type으로 정리
	- 주요 타입, 데이터, hook, mapper, schema에 JSDoc 주석 추가

- 배포 및 문서 정리
	- Next.js Docker 배포 설정 추가 및 `192.168.1.21:3003` 환경 배포
	- `192.168.1.21:3003` 기준 프론트 실행 절차 문서화 


#### Work (프로젝트: 영상 제작 프로젝트)
---
- 백엔드 프로젝트 테스트환경 실행
	- http://127.0.0.1:8080/ 환경 CreativeBrief 분석 실패(LLM_ANALYSIS_FAILED) 원인 확인 
		- 원인: LLM이 만든 CreativeBrief 결과에서 requires_confirmation: true인 assumptions 항목이 있었는데, 같은 path를 가진 필수 clarification_questions(required: true)가 같이 생성되지 않았음
		- 해결: LLM이 다음 응답에서는 matching clarification_questions를 제대로 만들면 통과 (다시 시도)
	
	- 실제 영상 생성은 프론트가 아닌 Local ComfyUI(192.168.1.21:8188)와 local_t2v_preview.py CLI로 분리되어 있음을 확인
	- ComfyUI 서버를 통한 생성 runner 사용 테스트
	  
- 프론트엔드
  - 랜딩페이지 애니메이션 구조 검토 및 정리
    - 섹션 전체를 가리는 가위/필름 전환 연출은 스크롤 타이밍, sticky 영역, 콘텐츠 잘림 등 예외가 많아 제거
    - 실제 랜딩 콘텐츠를 중복 렌더링하지 않는 방향으로 구조 정리
    - `영상 제작의 앞단을 빠르게 정리합니다` 제목 문구에 한정된 강조 애니메이션으로 변경
    - `AnimatedEmphasisTitle` 컴포넌트 추가

  - 랜딩페이지 첫 진입 연출 적용
    - 첫 화면 진입 시 영화 슬레이트 액션 느낌의 인트로 연출 적용
    - 스크롤/키 입력 시 암전 화면에서 랜딩 첫 섹션이 드러나는 흐름 구성
    - 헤더가 인트로 몰입을 깨지 않도록 첫 진입 overlay 기준으로 구조 조정
