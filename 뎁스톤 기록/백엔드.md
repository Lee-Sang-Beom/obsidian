> serviceimpl
```java

  <Polygon
                key={pol.seq}
                positions={filtedCoordinates}
                pathOptions={pathOptions}
                eventHandlers={{
                  mouseover: () => {
                    setHoveredPolygonSeq(pol.seq);
                  },
                  mouseout: () => {
                    setHoveredPolygonSeq(null);
                  },
                }}
              >
                <Popup>
                  <div className={styles.form_box}>
                    <div className={styles.button_box}>
                      <Button
                        color={"white"}
                        title={"통, 반 데이터 삭제"}
                        id={"tongBanDelete"}
                        size="lg"
                        onClickEvent={(e) => {
                          e.stopPropagation(); // 이벤트 전파 중지
                          console.log("현재 클릭한 데이터의 정보 ", pol);
                        }}
                      >
                        삭제하기
                      </Button>
                    </div>
                  </div>
                </Popup>
              </Polygon>

```


